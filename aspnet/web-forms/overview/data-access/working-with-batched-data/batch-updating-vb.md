---
uid: web-forms/overview/data-access/working-with-batched-data/batch-updating-vb
title: 批更新 (VB) |Microsoft Docs
author: rick-anderson
description: 了解如何更新单个操作中的多个数据库记录。 在用户界面层我们构建一个 GridView，其中每行都是可编辑。 在数据中...
ms.author: aspnetcontent
manager: wpickett
ms.date: 06/26/2007
ms.topic: article
ms.assetid: d191a204-d7ea-458d-b81c-0b9049ecb55f
ms.technology: dotnet-webforms
msc.legacyurl: /web-forms/overview/data-access/working-with-batched-data/batch-updating-vb
msc.type: authoredcontent
ms.openlocfilehash: 7830e010ea8dbf6ce9bd59154c10eb4c30a3dceb
ms.sourcegitcommit: 953ff9ea4369f154d6fd0239599279ddd3280009
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 07/03/2018
ms.locfileid: "37363308"
---
<a name="batch-updating-vb"></a><span data-ttu-id="e584c-105">批量更新 (VB)</span><span class="sxs-lookup"><span data-stu-id="e584c-105">Batch Updating (VB)</span></span>
====================
<span data-ttu-id="e584c-106">通过[Scott Mitchell](https://twitter.com/ScottOnWriting)</span><span class="sxs-lookup"><span data-stu-id="e584c-106">by [Scott Mitchell](https://twitter.com/ScottOnWriting)</span></span>

<span data-ttu-id="e584c-107">[下载代码](http://download.microsoft.com/download/3/9/f/39f92b37-e92e-4ab3-909e-b4ef23d01aa3/ASPNET_Data_Tutorial_64_VB.zip)或[下载 PDF](batch-updating-vb/_static/datatutorial64vb1.pdf)</span><span class="sxs-lookup"><span data-stu-id="e584c-107">[Download Code](http://download.microsoft.com/download/3/9/f/39f92b37-e92e-4ab3-909e-b4ef23d01aa3/ASPNET_Data_Tutorial_64_VB.zip) or [Download PDF](batch-updating-vb/_static/datatutorial64vb1.pdf)</span></span>

> <span data-ttu-id="e584c-108">了解如何更新单个操作中的多个数据库记录。</span><span class="sxs-lookup"><span data-stu-id="e584c-108">Learn how to update multiple database records in a single operation.</span></span> <span data-ttu-id="e584c-109">在用户界面层我们构建一个 GridView，其中每行都是可编辑。</span><span class="sxs-lookup"><span data-stu-id="e584c-109">In the User Interface Layer we build a GridView where each row is editable.</span></span> <span data-ttu-id="e584c-110">数据访问层中，我们将以确保所有更新都成功或所有更新都将都回滚的事务中的多个更新操作。</span><span class="sxs-lookup"><span data-stu-id="e584c-110">In the Data Access Layer we wrap the multiple Update operations within a transaction to ensure that all updates succeed or all updates are rolled back.</span></span>


## <a name="introduction"></a><span data-ttu-id="e584c-111">介绍</span><span class="sxs-lookup"><span data-stu-id="e584c-111">Introduction</span></span>

<span data-ttu-id="e584c-112">在中[前面的教程](wrapping-database-modifications-within-a-transaction-vb.md)我们已了解如何扩展数据访问层，以便添加对数据库事务的支持。</span><span class="sxs-lookup"><span data-stu-id="e584c-112">In the [preceding tutorial](wrapping-database-modifications-within-a-transaction-vb.md) we saw how to extend the Data Access Layer to add support for database transactions.</span></span> <span data-ttu-id="e584c-113">数据库事务保证一系列的数据修改语句将被视为一个原子操作，它可确保所有修改将都失败或所有将会成功。</span><span class="sxs-lookup"><span data-stu-id="e584c-113">Database transactions guarantee that a series of data modification statements will be treated as one atomic operation, which ensures that all modifications will fail or all will succeed.</span></span> <span data-ttu-id="e584c-114">使用此低级别 DAL 功能开，我们重新准备我们把注意力转向创建数据修改界面的批处理。</span><span class="sxs-lookup"><span data-stu-id="e584c-114">With this low-level DAL functionality out of the way, we re ready to turn our attention to creating batch data modification interfaces.</span></span>

<span data-ttu-id="e584c-115">在本教程中我们将构建一个 GridView，其中每个行是可编辑 （请参阅图 1）。</span><span class="sxs-lookup"><span data-stu-id="e584c-115">In this tutorial we'll build a GridView where each row is editable (see Figure 1).</span></span> <span data-ttu-id="e584c-116">由于无需进行编辑的列，每行均呈现在其编辑界面中，那里 s，更新和取消按钮。</span><span class="sxs-lookup"><span data-stu-id="e584c-116">Since each row is rendered in its editing interface, there s no need for a column of Edit, Update, and Cancel buttons.</span></span> <span data-ttu-id="e584c-117">相反，有两个更新的产品按钮的页上，单击时，枚举 GridView 行并更新数据库。</span><span class="sxs-lookup"><span data-stu-id="e584c-117">Instead, there are two Update Products buttons on the page that, when clicked, enumerate the GridView rows and update the database.</span></span>


<span data-ttu-id="e584c-118">[![在 GridView 中的每一行都是可编辑](batch-updating-vb/_static/image1.gif)](batch-updating-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-118">[![Each Row in the GridView is Editable](batch-updating-vb/_static/image1.gif)](batch-updating-vb/_static/image1.png)</span></span>

<span data-ttu-id="e584c-119">**图 1**： 在 GridView 中的每个行是可编辑 ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image2.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-119">**Figure 1**: Each Row in the GridView is Editable ([Click to view full-size image](batch-updating-vb/_static/image2.png))</span></span>


<span data-ttu-id="e584c-120">让我们来开始 ！</span><span class="sxs-lookup"><span data-stu-id="e584c-120">Let s get started!</span></span>

> [!NOTE]
> <span data-ttu-id="e584c-121">在中[执行批处理更新](../editing-and-deleting-data-through-the-datalist/performing-batch-updates-vb.md)教程中，我们创建批处理编辑接口使用 DataList 控件。</span><span class="sxs-lookup"><span data-stu-id="e584c-121">In the [Performing Batch Updates](../editing-and-deleting-data-through-the-datalist/performing-batch-updates-vb.md) tutorial we created a batch editing interface using the DataList control.</span></span> <span data-ttu-id="e584c-122">本教程中不同于上一个中的其中一个是使用 GridView 和事务的作用域内执行批处理更新。</span><span class="sxs-lookup"><span data-stu-id="e584c-122">This tutorial differs from the previous one in that is uses a GridView and the batch update is performed within the scope of a transaction.</span></span> <span data-ttu-id="e584c-123">完成本教程后我鼓励您返回到早期的教程，并将其更新为使用在前面的教程中已添加的数据库与事务相关功能。</span><span class="sxs-lookup"><span data-stu-id="e584c-123">After completing this tutorial I encourage you to return to the earlier tutorial and update it to use the database transaction-related functionality added in the preceding tutorial.</span></span>


## <a name="examining-the-steps-for-making-all-gridview-rows-editable"></a><span data-ttu-id="e584c-124">检查建立所有 GridView 行可编辑的步骤</span><span class="sxs-lookup"><span data-stu-id="e584c-124">Examining the Steps for Making All GridView Rows Editable</span></span>

<span data-ttu-id="e584c-125">如中所述[概述的插入、 更新和删除数据](../editing-inserting-and-deleting-data/an-overview-of-inserting-updating-and-deleting-data-vb.md)教程中，GridView 提供了用于编辑每个行按其基础数据的内置支持。</span><span class="sxs-lookup"><span data-stu-id="e584c-125">As discussed in the [An Overview of Inserting, Updating, and Deleting Data](../editing-inserting-and-deleting-data/an-overview-of-inserting-updating-and-deleting-data-vb.md) tutorial, the GridView offers built-in support for editing its underlying data on a per-row basis.</span></span> <span data-ttu-id="e584c-126">在内部，GridView 说明哪些行是可通过编辑其[`EditIndex`属性](https://msdn.microsoft.com/library/system.web.ui.webcontrols.gridview.editindex(VS.80).aspx)。</span><span class="sxs-lookup"><span data-stu-id="e584c-126">Internally, the GridView notes what row is editable through its [`EditIndex` property](https://msdn.microsoft.com/library/system.web.ui.webcontrols.gridview.editindex(VS.80).aspx).</span></span> <span data-ttu-id="e584c-127">正在将 GridView 绑定到其数据源，因为它会检查每个行可查看的行的索引是否等于的值`EditIndex`。</span><span class="sxs-lookup"><span data-stu-id="e584c-127">As the GridView is being bound to its data source, it checks each row to see if the index of the row equals the value of `EditIndex`.</span></span> <span data-ttu-id="e584c-128">如果是这样，该行 s 呈现字段使用其编辑界面。</span><span class="sxs-lookup"><span data-stu-id="e584c-128">If so, that row s fields are rendered using their editing interfaces.</span></span> <span data-ttu-id="e584c-129">对于 BoundFields，编辑界面是文本框中其`Text`属性分配由 BoundField s 指定的数据字段的值`DataField`属性。</span><span class="sxs-lookup"><span data-stu-id="e584c-129">For BoundFields, the editing interface is a TextBox whose `Text` property is assigned the value of the data field specified by the BoundField s `DataField` property.</span></span> <span data-ttu-id="e584c-130">为 Templatefield，`EditItemTemplate`来代替使用`ItemTemplate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-130">For TemplateFields, the `EditItemTemplate` is used in place of the `ItemTemplate`.</span></span>

<span data-ttu-id="e584c-131">回想一下在编辑工作流启动时在用户单击行的编辑按钮。</span><span class="sxs-lookup"><span data-stu-id="e584c-131">Recall that the editing workflow starts when a user clicks a row s Edit button.</span></span> <span data-ttu-id="e584c-132">这将导致回发，设置 GridView 的`EditIndex`s 被单击的行索引和重新绑定次数到网格的数据的属性。</span><span class="sxs-lookup"><span data-stu-id="e584c-132">This causes a postback, sets the GridView s `EditIndex` property to the clicked row s index, and rebinds the data to the grid.</span></span> <span data-ttu-id="e584c-133">当单击行 s 取消按钮，在回发`EditIndex`设置的值为`-1`之前重新绑定到网格的数据。</span><span class="sxs-lookup"><span data-stu-id="e584c-133">When a row s Cancel button is clicked, on postback the `EditIndex` is set to a value of `-1` before rebinding the data to the grid.</span></span> <span data-ttu-id="e584c-134">由于 GridView 的行开始编制索引为零，因此设置`EditIndex`到`-1`GridView 显示在只读模式下的影响。</span><span class="sxs-lookup"><span data-stu-id="e584c-134">Since the GridView s rows start indexing at zero, setting `EditIndex` to `-1` has the effect of displaying the GridView in read-only mode.</span></span>

<span data-ttu-id="e584c-135">`EditIndex`属性非常适用于每个行编辑，但不是用于批处理编辑。</span><span class="sxs-lookup"><span data-stu-id="e584c-135">The `EditIndex` property works well for per-row editing, but is not designed for batch editing.</span></span> <span data-ttu-id="e584c-136">若要使整个 GridView 可编辑，我们需要能够使用其编辑界面呈现每个行。</span><span class="sxs-lookup"><span data-stu-id="e584c-136">To make the entire GridView editable, we need to have each row render using its editing interface.</span></span> <span data-ttu-id="e584c-137">若要实现此目的的最简单方法是创建每个可编辑字段实现具有其编辑界面 TemplateField 中定义的位置`ItemTemplate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-137">The easiest way to accomplish this is to create where each editable field is implemented as a TemplateField with its editing interface defined in the `ItemTemplate`.</span></span>

<span data-ttu-id="e584c-138">在接下来的几个步骤我们将创建一个完全可编辑的 GridView。</span><span class="sxs-lookup"><span data-stu-id="e584c-138">Over the next several steps we'll create a completely editable GridView.</span></span> <span data-ttu-id="e584c-139">步骤 1 中我们首先创建 GridView 和其对象数据源，并将其 BoundFields 和 CheckBoxField 转换为 Templatefield。</span><span class="sxs-lookup"><span data-stu-id="e584c-139">In Step 1 we'll start by creating the GridView and its ObjectDataSource and convert its BoundFields and CheckBoxField into TemplateFields.</span></span> <span data-ttu-id="e584c-140">步骤 2 和 3 中，我们将从 Templatefield 继续编辑接口`EditItemTemplate`向其`ItemTemplate`s。</span><span class="sxs-lookup"><span data-stu-id="e584c-140">In Steps 2 and 3 we'll move the editing interfaces from the TemplateFields `EditItemTemplate` s to their `ItemTemplate` s.</span></span>

## <a name="step-1-displaying-product-information"></a><span data-ttu-id="e584c-141">步骤 1： 显示产品信息</span><span class="sxs-lookup"><span data-stu-id="e584c-141">Step 1: Displaying Product Information</span></span>

<span data-ttu-id="e584c-142">我们担心如何创建 GridView 之前所在的行都是可编辑，让我们来首先只需显示产品信息。</span><span class="sxs-lookup"><span data-stu-id="e584c-142">Before we worry about creating a GridView where are rows are editable, let s start by simply displaying the product information.</span></span> <span data-ttu-id="e584c-143">打开`BatchUpdate.aspx`页中`BatchData`文件夹，然后拖动 GridView 从工具箱拖到设计器。</span><span class="sxs-lookup"><span data-stu-id="e584c-143">Open the `BatchUpdate.aspx` page in the `BatchData` folder and drag a GridView from the Toolbox onto the Designer.</span></span> <span data-ttu-id="e584c-144">设置 GridView s`ID`到`ProductsGrid`，并从其智能标记，选择要绑定到名为新 ObjectDataSource `ProductsDataSource`。</span><span class="sxs-lookup"><span data-stu-id="e584c-144">Set the GridView s `ID` to `ProductsGrid` and, from its smart tag, choose to bind it to a new ObjectDataSource named `ProductsDataSource`.</span></span> <span data-ttu-id="e584c-145">配置要检索其数据从 ObjectDataSource`ProductsBLL`类的`GetProducts`方法。</span><span class="sxs-lookup"><span data-stu-id="e584c-145">Configure the ObjectDataSource to retrieve its data from the `ProductsBLL` class s `GetProducts` method.</span></span>


<span data-ttu-id="e584c-146">[![配置对象数据源以使用 ProductsBLL 类](batch-updating-vb/_static/image2.gif)](batch-updating-vb/_static/image3.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-146">[![Configure the ObjectDataSource to Use the ProductsBLL Class](batch-updating-vb/_static/image2.gif)](batch-updating-vb/_static/image3.png)</span></span>

<span data-ttu-id="e584c-147">**图 2**： 配置为使用 ObjectDataSource`ProductsBLL`类 ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image4.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-147">**Figure 2**: Configure the ObjectDataSource to Use the `ProductsBLL` Class ([Click to view full-size image](batch-updating-vb/_static/image4.png))</span></span>


<span data-ttu-id="e584c-148">[![检索产品数据使用 GetProducts 方法](batch-updating-vb/_static/image3.gif)](batch-updating-vb/_static/image5.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-148">[![Retrieve the Product Data Using the GetProducts Method](batch-updating-vb/_static/image3.gif)](batch-updating-vb/_static/image5.png)</span></span>

<span data-ttu-id="e584c-149">**图 3**： 检索产品数据使用`GetProducts`方法 ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-149">**Figure 3**: Retrieve the Product Data Using the `GetProducts` Method ([Click to view full-size image](batch-updating-vb/_static/image6.png))</span></span>


<span data-ttu-id="e584c-150">如 GridView，ObjectDataSource 的修改功能旨在针对每个行而工作的。</span><span class="sxs-lookup"><span data-stu-id="e584c-150">Like the GridView, the ObjectDataSource s modification features are designed to work on a per-row basis.</span></span> <span data-ttu-id="e584c-151">若要更新的一组记录，我们将需要进行批处理数据，并将其传递给 BLL 的 ASP.NET 页面 + s 代码隐藏类中编写一些代码。</span><span class="sxs-lookup"><span data-stu-id="e584c-151">In order to update a set of records, we'll need to write a bit of code in the ASP.NET page s code-behind class that batches the data and passes it to the BLL.</span></span> <span data-ttu-id="e584c-152">因此，设置下拉列表中的 ObjectDataSource 的更新、 插入和删除选项卡添加到 （无）。</span><span class="sxs-lookup"><span data-stu-id="e584c-152">Therefore, set the drop-down lists in the ObjectDataSource s UPDATE, INSERT, and DELETE tabs to (None).</span></span> <span data-ttu-id="e584c-153">单击完成以完成向导。</span><span class="sxs-lookup"><span data-stu-id="e584c-153">Click Finish to complete the wizard.</span></span>


<span data-ttu-id="e584c-154">[![设置下拉列表中插入、 更新和删除选项卡为 （无）](batch-updating-vb/_static/image4.gif)](batch-updating-vb/_static/image7.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-154">[![Set the Drop-Down Lists in the UPDATE, INSERT, and DELETE Tabs to (None)](batch-updating-vb/_static/image4.gif)](batch-updating-vb/_static/image7.png)</span></span>

<span data-ttu-id="e584c-155">**图 4**： 设置下拉列表列出了在更新、 插入和删除选项卡中为 （无） ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image8.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-155">**Figure 4**: Set the Drop-Down Lists in the UPDATE, INSERT, and DELETE Tabs to (None) ([Click to view full-size image](batch-updating-vb/_static/image8.png))</span></span>


<span data-ttu-id="e584c-156">完成配置数据源向导后，在 ObjectDataSource s 声明性标记应如下所示：</span><span class="sxs-lookup"><span data-stu-id="e584c-156">After completing the Configure Data Source wizard, the ObjectDataSource s declarative markup should look like the following:</span></span>


[!code-aspx[Main](batch-updating-vb/samples/sample1.aspx)]

<span data-ttu-id="e584c-157">完成配置数据源向导还会导致 Visual Studio 在 GridView 中创建 BoundFields 和产品数据字段 CheckBoxField。</span><span class="sxs-lookup"><span data-stu-id="e584c-157">Completing the Configure Data Source wizard also causes Visual Studio to create BoundFields and a CheckBoxField for the product data fields in the GridView.</span></span> <span data-ttu-id="e584c-158">对于本教程，让我们来仅允许用户查看和编辑产品的名称、 类别、 价格和已停止使用的状态。</span><span class="sxs-lookup"><span data-stu-id="e584c-158">For this tutorial, let s only allow the user to view and edit the product s name, category, price, and discontinued status.</span></span> <span data-ttu-id="e584c-159">删除所有除`ProductName`， `CategoryName`， `UnitPrice`，和`Discontinued`字段和重命名`HeaderText`的前三个属性字段设置为产品、 类别和价格，分别。</span><span class="sxs-lookup"><span data-stu-id="e584c-159">Remove all but the `ProductName`, `CategoryName`, `UnitPrice`, and `Discontinued` fields and rename the `HeaderText` properties of the first three fields to Product, Category, and Price, respectively.</span></span> <span data-ttu-id="e584c-160">最后，检查 GridView s 智能标记中的启用分页和启用排序复选框。</span><span class="sxs-lookup"><span data-stu-id="e584c-160">Lastly, check the Enable Paging and Enable Sorting checkboxes in the GridView s smart tag.</span></span>

<span data-ttu-id="e584c-161">这个 GridView 现在有三个 BoundFields (`ProductName`， `CategoryName`，并`UnitPrice`) 和 CheckBoxField (`Discontinued`)。</span><span class="sxs-lookup"><span data-stu-id="e584c-161">At this point the GridView has three BoundFields (`ProductName`, `CategoryName`, and `UnitPrice`) and a CheckBoxField (`Discontinued`).</span></span> <span data-ttu-id="e584c-162">我们需要将这四个字段转换为 Templatefield，然后将编辑界面移动从 TemplateField s`EditItemTemplate`到其`ItemTemplate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-162">We need to convert these four fields into TemplateFields and then move the editing interface from the TemplateField s `EditItemTemplate` to its `ItemTemplate`.</span></span>

> [!NOTE]
> <span data-ttu-id="e584c-163">我们探讨了创建和自定义中的 Templatefield[自定义数据修改界面](../editing-inserting-and-deleting-data/customizing-the-data-modification-interface-vb.md)教程。</span><span class="sxs-lookup"><span data-stu-id="e584c-163">We explored creating and customizing TemplateFields in the [Customizing the Data Modification Interface](../editing-inserting-and-deleting-data/customizing-the-data-modification-interface-vb.md) tutorial.</span></span> <span data-ttu-id="e584c-164">我们将演示 BoundFields 和 CheckBoxField 转换为 Templatefield 的步骤，并定义其编辑界面中其`ItemTemplate`s，但如果有疑问或需要刷新程序，不要欢迎回头参考此早期的教程。</span><span class="sxs-lookup"><span data-stu-id="e584c-164">We'll walk through the steps of converting the BoundFields and CheckBoxField into TemplateFields and defining their editing interfaces in their `ItemTemplate` s, but if you get stuck or need a refresher, don t hesitate to refer back to this earlier tutorial.</span></span>


<span data-ttu-id="e584c-165">从 GridView s 智能标记，单击编辑列链接以打开字段对话框。</span><span class="sxs-lookup"><span data-stu-id="e584c-165">From the GridView s smart tag, click the Edit Columns link to open the Fields dialog box.</span></span> <span data-ttu-id="e584c-166">接下来，选择每个字段，并单击转换此字段转换为 TemplateField 链接。</span><span class="sxs-lookup"><span data-stu-id="e584c-166">Next, select each field and click the Convert this field into a TemplateField link.</span></span>


![将现有 BoundFields 和 CheckBoxField 转换为 Templatefield](batch-updating-vb/_static/image5.gif)

<span data-ttu-id="e584c-168">**图 5**： 现有 BoundFields 和 CheckBoxField 转换为 Templatefield</span><span class="sxs-lookup"><span data-stu-id="e584c-168">**Figure 5**: Convert the Existing BoundFields and CheckBoxField Into TemplateFields</span></span>


<span data-ttu-id="e584c-169">现在，每个字段都为 TemplateField，我们准备就绪后，若要移动的编辑界面`EditItemTemplate`到`ItemTemplate`s。</span><span class="sxs-lookup"><span data-stu-id="e584c-169">Now that each field is a TemplateField, we re ready to move the editing interface from the `EditItemTemplate` s to the `ItemTemplate` s.</span></span>

## <a name="step-2-creating-theproductnameunitprice-anddiscontinuedediting-interfaces"></a><span data-ttu-id="e584c-170">步骤 2： 创建`ProductName`，`UnitPrice`，和`Discontinued`编辑接口</span><span class="sxs-lookup"><span data-stu-id="e584c-170">Step 2: Creating the`ProductName`,`UnitPrice`, and`Discontinued`Editing Interfaces</span></span>

<span data-ttu-id="e584c-171">创建`ProductName`， `UnitPrice`，并`Discontinued`编辑接口是此步骤的主题，每个接口已定义中的 TemplateField s 是相当简单， `EditItemTemplate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-171">Creating the `ProductName`, `UnitPrice`, and `Discontinued` editing interfaces are the topic of this step and are pretty straightforward, as each interface is already defined in the TemplateField s `EditItemTemplate`.</span></span> <span data-ttu-id="e584c-172">创建`CategoryName`编辑界面是稍微要复杂，因为我们需要创建 DropDownList 适用的类别。</span><span class="sxs-lookup"><span data-stu-id="e584c-172">Creating the `CategoryName` editing interface is a bit more involved since we need to create a DropDownList of the applicable categories.</span></span> <span data-ttu-id="e584c-173">这`CategoryName`编辑界面在步骤 3 中处理。</span><span class="sxs-lookup"><span data-stu-id="e584c-173">This `CategoryName` editing interface is tackled in Step 3.</span></span>

<span data-ttu-id="e584c-174">让我们来开始`ProductName`TemplateField。</span><span class="sxs-lookup"><span data-stu-id="e584c-174">Let s start with the `ProductName` TemplateField.</span></span> <span data-ttu-id="e584c-175">单击 GridView s 智能标记中的编辑模板链接，然后向下钻取`ProductName`TemplateField 的`EditItemTemplate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-175">Click on the Edit Templates link from the GridView s smart tag and drill down to the `ProductName` TemplateField s `EditItemTemplate`.</span></span> <span data-ttu-id="e584c-176">选择文本框中，将其复制到剪贴板，然后再粘贴到`ProductName`TemplateField 的`ItemTemplate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-176">Select the TextBox, copy it to the clipboard, and then paste it to the `ProductName` TemplateField s `ItemTemplate`.</span></span> <span data-ttu-id="e584c-177">更改文本框 s`ID`属性设置为`ProductName`。</span><span class="sxs-lookup"><span data-stu-id="e584c-177">Change the TextBox s `ID` property to `ProductName`.</span></span>

<span data-ttu-id="e584c-178">接下来，添加到一个 RequiredFieldValidator`ItemTemplate`以确保用户为每个产品的名称提供一个值。</span><span class="sxs-lookup"><span data-stu-id="e584c-178">Next, add a RequiredFieldValidator to the `ItemTemplate` to ensure that the user provides a value for each product s name.</span></span> <span data-ttu-id="e584c-179">设置`ControlToValidate`属性设置为产品名称`ErrorMessage`属性设置为你必须提供产品的名称。</span><span class="sxs-lookup"><span data-stu-id="e584c-179">Set the `ControlToValidate` property to ProductName, the `ErrorMessage` property to You must provide the product's name.</span></span> <span data-ttu-id="e584c-180">并`Text`属性设置为\*。</span><span class="sxs-lookup"><span data-stu-id="e584c-180">and the `Text` property to \*.</span></span> <span data-ttu-id="e584c-181">建立到这些新增功能后`ItemTemplate`，屏幕应类似于图 6。</span><span class="sxs-lookup"><span data-stu-id="e584c-181">After making these additions to the `ItemTemplate`, your screen should look similar to Figure 6.</span></span>


<span data-ttu-id="e584c-182">[![ProductName TemplateField 现在包含一个文本框和一个 RequiredFieldValidator](batch-updating-vb/_static/image6.gif)](batch-updating-vb/_static/image9.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-182">[![The ProductName TemplateField Now Includes a TextBox and a RequiredFieldValidator](batch-updating-vb/_static/image6.gif)](batch-updating-vb/_static/image9.png)</span></span>

<span data-ttu-id="e584c-183">**图 6**: `ProductName` TemplateField 现在包含一个文本框和一个 RequiredFieldValidator ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image10.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-183">**Figure 6**: The `ProductName` TemplateField Now Includes a TextBox and a RequiredFieldValidator ([Click to view full-size image](batch-updating-vb/_static/image10.png))</span></span>


<span data-ttu-id="e584c-184">有关`UnitPrice`编辑界面，首先复制从文本框`EditItemTemplate`到`ItemTemplate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-184">For the `UnitPrice` editing interface, start by copying the TextBox from the `EditItemTemplate` to the `ItemTemplate`.</span></span> <span data-ttu-id="e584c-185">接下来，将 $ 前面的文本框中并设置其`ID`属性设置为单价并将其`Columns`属性设置为 8。</span><span class="sxs-lookup"><span data-stu-id="e584c-185">Next, place a $ in front of the TextBox and set its `ID` property to UnitPrice and its `Columns` property to 8 .</span></span>

<span data-ttu-id="e584c-186">此外将添加到 CompareValidator `UnitPrice` s`ItemTemplate`以确保用户输入的值是有效的货币值大于或等于为 0.00 美元。</span><span class="sxs-lookup"><span data-stu-id="e584c-186">Also add a CompareValidator to the `UnitPrice` s `ItemTemplate` to ensure that the value entered by the user is a valid currency value greater than or equal to $0.00.</span></span> <span data-ttu-id="e584c-187">设置验证程序 s`ControlToValidate`属性设置为单价，其`ErrorMessage`属性设置为您必须输入有效的货币值。</span><span class="sxs-lookup"><span data-stu-id="e584c-187">Set the validator s `ControlToValidate` property to UnitPrice, its `ErrorMessage` property to You must enter a valid currency value.</span></span> <span data-ttu-id="e584c-188">请省略任何货币符号。，其`Text`属性设置为\*，将其`Type`属性设置为`Currency`，将其`Operator`属性设置为`GreaterThanEqual`，并将其`ValueToCompare`属性设为 0。</span><span class="sxs-lookup"><span data-stu-id="e584c-188">Please omit any currency symbols., its `Text` property to \*, its `Type` property to `Currency`, its `Operator` property to `GreaterThanEqual`, and its `ValueToCompare` property to 0 .</span></span>


<span data-ttu-id="e584c-189">[![添加 CompareValidator，确保价格输入是一个非负货币值](batch-updating-vb/_static/image7.gif)](batch-updating-vb/_static/image11.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-189">[![Add a CompareValidator to Ensure the Price Entered is a Non-Negative Currency Value](batch-updating-vb/_static/image7.gif)](batch-updating-vb/_static/image11.png)</span></span>

<span data-ttu-id="e584c-190">**图 7**： 添加 CompareValidator，确保价格输入是一个非负货币值 ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image12.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-190">**Figure 7**: Add a CompareValidator to Ensure the Price Entered is a Non-Negative Currency Value ([Click to view full-size image](batch-updating-vb/_static/image12.png))</span></span>


<span data-ttu-id="e584c-191">有关`Discontinued`TemplateField 可以使用已在中定义的复选框`ItemTemplate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-191">For the `Discontinued` TemplateField you can use the CheckBox already defined in the `ItemTemplate`.</span></span> <span data-ttu-id="e584c-192">只需设置其`ID`到已中断并且其`Enabled`属性设置为`True`。</span><span class="sxs-lookup"><span data-stu-id="e584c-192">Simply set its `ID` to Discontinued and its `Enabled` property to `True`.</span></span>

## <a name="step-3-creating-thecategorynameediting-interface"></a><span data-ttu-id="e584c-193">步骤 3： 创建`CategoryName`编辑接口</span><span class="sxs-lookup"><span data-stu-id="e584c-193">Step 3: Creating the`CategoryName`Editing Interface</span></span>

<span data-ttu-id="e584c-194">中的编辑界面`CategoryName`TemplateField s`EditItemTemplate`包含一个文本框，用于显示的值`CategoryName`数据字段。</span><span class="sxs-lookup"><span data-stu-id="e584c-194">The editing interface in the `CategoryName` TemplateField s `EditItemTemplate` contains a TextBox that displays the value of the `CategoryName` data field.</span></span> <span data-ttu-id="e584c-195">我们需要此项替换 DropDownList，其中列出了可能的类别。</span><span class="sxs-lookup"><span data-stu-id="e584c-195">We need to replace this with a DropDownList that lists the possible categories.</span></span>

> [!NOTE]
> <span data-ttu-id="e584c-196">[自定义数据修改界面](../editing-inserting-and-deleting-data/customizing-the-data-modification-interface-vb.md)教程包含自定义模板以包括 DropDownList 而不是文本框的更全面、 完整讨论。</span><span class="sxs-lookup"><span data-stu-id="e584c-196">The [Customizing the Data Modification Interface](../editing-inserting-and-deleting-data/customizing-the-data-modification-interface-vb.md) tutorial contains a more thorough and complete discussion on customizing a template to include a DropDownList as opposed to a TextBox.</span></span> <span data-ttu-id="e584c-197">尽管此处的步骤都完成后，他们将看到 tersely。</span><span class="sxs-lookup"><span data-stu-id="e584c-197">While the steps here are complete, they are presented tersely.</span></span> <span data-ttu-id="e584c-198">有关创建和配置类别 DropDownList 的更深入信息，请参阅重新[自定义数据修改界面](../editing-inserting-and-deleting-data/customizing-the-data-modification-interface-vb.md)教程。</span><span class="sxs-lookup"><span data-stu-id="e584c-198">For a more in-depth look at creating and configuring the categories DropDownList, refer back to the [Customizing the Data Modification Interface](../editing-inserting-and-deleting-data/customizing-the-data-modification-interface-vb.md) tutorial.</span></span>


<span data-ttu-id="e584c-199">从工具箱拖到拖 DropDownList `CategoryName` TemplateField s `ItemTemplate`，并设置其`ID`到`Categories`。</span><span class="sxs-lookup"><span data-stu-id="e584c-199">Drag a DropDownList from the Toolbox onto the `CategoryName` TemplateField s `ItemTemplate`, setting its `ID` to `Categories`.</span></span> <span data-ttu-id="e584c-200">此时我们将通常定义 Dropdownlist 的数据源通过其智能标记，创建新对象数据源。</span><span class="sxs-lookup"><span data-stu-id="e584c-200">At this point we would usually define the DropDownLists s data source through its smart tag, creating a new ObjectDataSource.</span></span> <span data-ttu-id="e584c-201">但是，这将添加在 ObjectDataSource `ItemTemplate`，这将导致为每个 GridView 行创建 ObjectDataSource 实例。</span><span class="sxs-lookup"><span data-stu-id="e584c-201">However, this will add the ObjectDataSource within the `ItemTemplate`, which will result in an ObjectDataSource instance created for each GridView row.</span></span> <span data-ttu-id="e584c-202">相反，允许创建外部 GridView 的 Templatefield ObjectDataSource 的 s。</span><span class="sxs-lookup"><span data-stu-id="e584c-202">Instead, let s create the ObjectDataSource outside of the GridView s TemplateFields.</span></span> <span data-ttu-id="e584c-203">结束模板编辑，并将从工具箱拖到设计器下方的 ObjectDataSource`ProductsDataSource`对象数据源。</span><span class="sxs-lookup"><span data-stu-id="e584c-203">End the template editing and drag an ObjectDataSource from the Toolbox onto the Designer beneath the `ProductsDataSource` ObjectDataSource.</span></span> <span data-ttu-id="e584c-204">命名新 ObjectDataSource`CategoriesDataSource`并将其配置为使用`CategoriesBLL`类的`GetCategories`方法。</span><span class="sxs-lookup"><span data-stu-id="e584c-204">Name the new ObjectDataSource `CategoriesDataSource` and configure it to use the `CategoriesBLL` class s `GetCategories` method.</span></span>


<span data-ttu-id="e584c-205">[![配置对象数据源以使用 CategoriesBLL 类](batch-updating-vb/_static/image8.gif)](batch-updating-vb/_static/image13.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-205">[![Configure the ObjectDataSource to Use the CategoriesBLL Class](batch-updating-vb/_static/image8.gif)](batch-updating-vb/_static/image13.png)</span></span>

<span data-ttu-id="e584c-206">**图 8**： 配置为使用 ObjectDataSource`CategoriesBLL`类 ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image14.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-206">**Figure 8**: Configure the ObjectDataSource to Use the `CategoriesBLL` Class ([Click to view full-size image](batch-updating-vb/_static/image14.png))</span></span>


<span data-ttu-id="e584c-207">[![检索类别数据使用 GetCategories 方法](batch-updating-vb/_static/image9.gif)](batch-updating-vb/_static/image15.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-207">[![Retrieve the Category Data Using the GetCategories Method](batch-updating-vb/_static/image9.gif)](batch-updating-vb/_static/image15.png)</span></span>

<span data-ttu-id="e584c-208">**图 9**： 检索类别数据使用`GetCategories`方法 ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image16.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-208">**Figure 9**: Retrieve the Category Data Using the `GetCategories` Method ([Click to view full-size image](batch-updating-vb/_static/image16.png))</span></span>


<span data-ttu-id="e584c-209">由于此 ObjectDataSource 只是用于检索数据，设置为 （无） 更新和删除选项卡中的下拉列表。</span><span class="sxs-lookup"><span data-stu-id="e584c-209">Since this ObjectDataSource is used merely to retrieve data, set the drop-down lists in the UPDATE and DELETE tabs to (None).</span></span> <span data-ttu-id="e584c-210">单击完成以完成向导。</span><span class="sxs-lookup"><span data-stu-id="e584c-210">Click Finish to complete the wizard.</span></span>


<span data-ttu-id="e584c-211">[![集更新和删除选项卡添加到 （无） 中的下拉列表](batch-updating-vb/_static/image10.gif)](batch-updating-vb/_static/image17.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-211">[![Set the Drop-Down Lists in the UPDATE and DELETE Tabs to (None)](batch-updating-vb/_static/image10.gif)](batch-updating-vb/_static/image17.png)</span></span>

<span data-ttu-id="e584c-212">**图 10**： 设置的下拉列表中的更新和删除选项卡为 （无） ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image18.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-212">**Figure 10**: Set the Drop-Down Lists in the UPDATE and DELETE Tabs to (None) ([Click to view full-size image](batch-updating-vb/_static/image18.png))</span></span>


<span data-ttu-id="e584c-213">完成向导后`CategoriesDataSource`s 声明性标记应如下所示：</span><span class="sxs-lookup"><span data-stu-id="e584c-213">After completing the wizard, the `CategoriesDataSource` s declarative markup should look like the following:</span></span>


[!code-aspx[Main](batch-updating-vb/samples/sample2.aspx)]

<span data-ttu-id="e584c-214">与`CategoriesDataSource`创建并配置，返回到`CategoryName`TemplateField 的`ItemTemplate`，再从 DropDownList s 智能标记，单击选择数据源链接。</span><span class="sxs-lookup"><span data-stu-id="e584c-214">With the `CategoriesDataSource` created and configured, return to the `CategoryName` TemplateField s `ItemTemplate` and, from the DropDownList s smart tag, click on the Choose Data Source link.</span></span> <span data-ttu-id="e584c-215">在数据源配置向导中，选择`CategoriesDataSource`选项从第一个下拉列表，然后选择能够`CategoryName`用于显示和`CategoryID`作为值。</span><span class="sxs-lookup"><span data-stu-id="e584c-215">In the Data Source Configuration wizard, select the `CategoriesDataSource` option from the first drop-down list and choose to have `CategoryName` used for the display and `CategoryID` as the value.</span></span>


<span data-ttu-id="e584c-216">[![绑定 DropDownList 到 CategoriesDataSource](batch-updating-vb/_static/image11.gif)](batch-updating-vb/_static/image19.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-216">[![Bind the DropDownList to the CategoriesDataSource](batch-updating-vb/_static/image11.gif)](batch-updating-vb/_static/image19.png)</span></span>

<span data-ttu-id="e584c-217">**图 11**： 将绑定到 DropDownList `CategoriesDataSource` ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image20.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-217">**Figure 11**: Bind the DropDownList to the `CategoriesDataSource` ([Click to view full-size image](batch-updating-vb/_static/image20.png))</span></span>


<span data-ttu-id="e584c-218">此时`Categories`DropDownList 列出了所有类别，但它不会自动选择适当的绑定到 GridView 行的产品类别。</span><span class="sxs-lookup"><span data-stu-id="e584c-218">At this point the `Categories` DropDownList lists all of the categories, but it does not yet automatically select the appropriate category for the product bound to the GridView row.</span></span> <span data-ttu-id="e584c-219">若要完成此我们需要设置`Categories`DropDownList s`SelectedValue`产品 s`CategoryID`值。</span><span class="sxs-lookup"><span data-stu-id="e584c-219">To accomplish this we need to set the `Categories` DropDownList s `SelectedValue` to the product s `CategoryID` value.</span></span> <span data-ttu-id="e584c-220">单击编辑 DataBindings 链接从 DropDownList s 智能标记，并将关联`SelectedValue`具有属性`CategoryID`数据字段，如图 12 中所示。</span><span class="sxs-lookup"><span data-stu-id="e584c-220">Click on the Edit DataBindings link from the DropDownList s smart tag and associate the `SelectedValue` property with the `CategoryID` data field as shown in Figure 12.</span></span>


![将产品的 CategoryID 值绑定到 DropDownList 的 SelectedValue 属性](batch-updating-vb/_static/image12.gif)

<span data-ttu-id="e584c-222">**图 12**： 将绑定产品 s`CategoryID`值为 DropDownList 的`SelectedValue`属性</span><span class="sxs-lookup"><span data-stu-id="e584c-222">**Figure 12**: Bind the Product s `CategoryID` Value to the DropDownList s `SelectedValue` Property</span></span>


<span data-ttu-id="e584c-223">最后一个问题保持为一个： 如果产品不具有`CategoryID`指定值然后数据绑定语句上`SelectedValue`将导致异常。</span><span class="sxs-lookup"><span data-stu-id="e584c-223">One last problem remains: if the product doesn t have a `CategoryID` value specified then the databinding statement on `SelectedValue` will result in an exception.</span></span> <span data-ttu-id="e584c-224">这是因为 DropDownList 只包含项的类别，并且不提供对具有这些产品的选项`NULL`数据库值`CategoryID`。</span><span class="sxs-lookup"><span data-stu-id="e584c-224">This is because the DropDownList contains only items for the categories and does not offer an option for those products that have a `NULL` database value for `CategoryID`.</span></span> <span data-ttu-id="e584c-225">若要解决此问题，设置 DropDownList s`AppendDataBoundItems`属性设置为`True`并将新项添加到下拉列表中，省略`Value`从声明性语法的属性。</span><span class="sxs-lookup"><span data-stu-id="e584c-225">To remedy this, set the DropDownList s `AppendDataBoundItems` property to `True` and add a new item to the DropDownList, omitting the `Value` property from the declarative syntax.</span></span> <span data-ttu-id="e584c-226">也就是说，请确保`Categories`DropDownList s 声明性语法看起来如下所示：</span><span class="sxs-lookup"><span data-stu-id="e584c-226">That is, make sure that the `Categories` DropDownList s declarative syntax looks like the following:</span></span>


[!code-aspx[Main](batch-updating-vb/samples/sample3.aspx)]

<span data-ttu-id="e584c-227">请注意如何`<asp:ListItem Value="">`-选择一个-具有其`Value`属性显式设置为空字符串。</span><span class="sxs-lookup"><span data-stu-id="e584c-227">Note how the `<asp:ListItem Value="">` -- Select One -- has its `Value` attribute explicitly set to an empty string.</span></span> <span data-ttu-id="e584c-228">回头[自定义数据修改界面](../editing-inserting-and-deleting-data/customizing-the-data-modification-interface-vb.md)教程，了解为什么需要此额外的 DropDownList 项处理的更全面讨论`NULL`用例和原因的分配`Value`属性为空字符串是必不可少的。</span><span class="sxs-lookup"><span data-stu-id="e584c-228">Refer back to the [Customizing the Data Modification Interface](../editing-inserting-and-deleting-data/customizing-the-data-modification-interface-vb.md) tutorial for a more thorough discussion on why this additional DropDownList item is needed to handle the `NULL` case and why assignment of the `Value` property to an empty string is essential.</span></span>

> [!NOTE]
> <span data-ttu-id="e584c-229">没有一个潜在性能和可伸缩性问题此处值得一提的。</span><span class="sxs-lookup"><span data-stu-id="e584c-229">There is a potential performance and scalability issue here that is worth mentioning.</span></span> <span data-ttu-id="e584c-230">因为每一行都具有使用 DropDownList`CategoriesDataSource`作为其数据源，`CategoriesBLL`类 s`GetCategories`将调用方法*n*页每次访问时，其中*n*是数在 GridView 中的行。</span><span class="sxs-lookup"><span data-stu-id="e584c-230">Since each row has a DropDownList that uses the `CategoriesDataSource` as its data source, the `CategoriesBLL` class s `GetCategories` method will be called *n* times per page visit, where *n* is the number of rows in the GridView.</span></span> <span data-ttu-id="e584c-231">这些*n*调用`GetCategories`导致*n*对数据库的查询。</span><span class="sxs-lookup"><span data-stu-id="e584c-231">These *n* calls to `GetCategories` result in *n* queries to the database.</span></span> <span data-ttu-id="e584c-232">通过缓存返回的类别在每个请求缓存中或通过使用 SQL 缓存依赖项或在非常短的时间基于过期的缓存层，并且导致这种影响在数据库上的可能会降低。</span><span class="sxs-lookup"><span data-stu-id="e584c-232">This impact on the database could be lessened by caching the returned categories either in a per-request cache or through the Caching Layer using a SQL caching dependency or a very short time-based expiry.</span></span> <span data-ttu-id="e584c-233">有关每个请求的详细信息缓存选项，请参阅[`HttpContext.Items`每个请求缓存存储](http://aspnet.4guysfromrolla.com/articles/060904-1.aspx)。</span><span class="sxs-lookup"><span data-stu-id="e584c-233">For more information on the per-request caching option, see [`HttpContext.Items` a Per-Request Cache Store](http://aspnet.4guysfromrolla.com/articles/060904-1.aspx).</span></span>


## <a name="step-4-completing-the-editing-interface"></a><span data-ttu-id="e584c-234">步骤 4： 完成编辑界面</span><span class="sxs-lookup"><span data-stu-id="e584c-234">Step 4: Completing the Editing Interface</span></span>

<span data-ttu-id="e584c-235">我们已对所做的大量更改 GridView 的模板而不暂停以查看我们的进度。</span><span class="sxs-lookup"><span data-stu-id="e584c-235">We ve made a number of changes to the GridView s templates without pausing to view our progress.</span></span> <span data-ttu-id="e584c-236">花点时间查看我们通过浏览器的进度。</span><span class="sxs-lookup"><span data-stu-id="e584c-236">Take a moment to view our progress through a browser.</span></span> <span data-ttu-id="e584c-237">如图 13 所示，每行均呈现使用其`ItemTemplate`，其中包含的单元格 s 编辑界面。</span><span class="sxs-lookup"><span data-stu-id="e584c-237">As Figure 13 shows, each row is rendered using its `ItemTemplate`, which contains the cell s editing interface.</span></span>


<span data-ttu-id="e584c-238">[![每个 GridView 行是可编辑](batch-updating-vb/_static/image13.gif)](batch-updating-vb/_static/image21.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-238">[![Each GridView Row is Editable](batch-updating-vb/_static/image13.gif)](batch-updating-vb/_static/image21.png)</span></span>

<span data-ttu-id="e584c-239">**图 13**： 每个 GridView 行是可编辑 ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image22.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-239">**Figure 13**: Each GridView Row is Editable ([Click to view full-size image](batch-updating-vb/_static/image22.png))</span></span>


<span data-ttu-id="e584c-240">有一些我们需要采取措施的此时次要格式设置问题。</span><span class="sxs-lookup"><span data-stu-id="e584c-240">There are a few minor formatting issues that we should take care of at this point.</span></span> <span data-ttu-id="e584c-241">首先，请注意，`UnitPrice`值包含小数点后有 4。</span><span class="sxs-lookup"><span data-stu-id="e584c-241">First, note that the `UnitPrice` value contains four decimal points.</span></span> <span data-ttu-id="e584c-242">若要解决此问题，返回到`UnitPrice`TemplateField 的`ItemTemplate`，再从文本框 s 智能标记，单击编辑 DataBindings 链接。</span><span class="sxs-lookup"><span data-stu-id="e584c-242">To fix this, return to the `UnitPrice` TemplateField s `ItemTemplate` and, from the TextBox s smart tag, click on the Edit DataBindings link.</span></span> <span data-ttu-id="e584c-243">接下来，指定`Text`属性的格式应为数字。</span><span class="sxs-lookup"><span data-stu-id="e584c-243">Next, specify that the `Text` property should be formatted as a number.</span></span>


![格式的 Text 属性设置为一个数字](batch-updating-vb/_static/image14.gif)

<span data-ttu-id="e584c-245">**图 14**： 格式`Text`数字形式的属性</span><span class="sxs-lookup"><span data-stu-id="e584c-245">**Figure 14**: Format the `Text` Property as a Number</span></span>


<span data-ttu-id="e584c-246">其次，let s 中心中的复选框`Discontinued`列 （而不是让其左对齐）。</span><span class="sxs-lookup"><span data-stu-id="e584c-246">Second, let s center the checkbox in the `Discontinued` column (rather than having it left-aligned).</span></span> <span data-ttu-id="e584c-247">单击编辑列的 GridView s 智能标记，然后选择`Discontinued`TemplateField 从左下角中的字段列表。</span><span class="sxs-lookup"><span data-stu-id="e584c-247">Click on Edit Columns from the GridView s smart tag and select the `Discontinued` TemplateField from the list of fields in the bottom left corner.</span></span> <span data-ttu-id="e584c-248">向下钻取到`ItemStyle`并设置`HorizontalAlign`到中心，如图 15 中所示的属性。</span><span class="sxs-lookup"><span data-stu-id="e584c-248">Drill down into `ItemStyle` and set the `HorizontalAlign` property to Center as shown in Figure 15.</span></span>


![中心已停止使用复选框](batch-updating-vb/_static/image15.gif)

<span data-ttu-id="e584c-250">**图 15**: Center`Discontinued`复选框</span><span class="sxs-lookup"><span data-stu-id="e584c-250">**Figure 15**: Center the `Discontinued` CheckBox</span></span>


<span data-ttu-id="e584c-251">接下来，向页面添加验证摘要控件并设置其`ShowMessageBox`属性设置为`True`并将其`ShowSummary`属性设置为`False`。</span><span class="sxs-lookup"><span data-stu-id="e584c-251">Next, add a ValidationSummary control to the page and set its `ShowMessageBox` property to `True` and its `ShowSummary` property to `False`.</span></span> <span data-ttu-id="e584c-252">此外添加按钮 Web 控件，单击时，将更新用户 s 做的更改。</span><span class="sxs-lookup"><span data-stu-id="e584c-252">Also add the Button Web controls that, when clicked, will update the user s changes.</span></span> <span data-ttu-id="e584c-253">具体而言，添加一个上面 GridView，它下面的一个设置这两个控件的两个按钮 Web 控件`Text`更新产品的属性。</span><span class="sxs-lookup"><span data-stu-id="e584c-253">Specifically, add two Button Web controls, one above the GridView and one below it, setting both controls `Text` properties to Update Products .</span></span>

<span data-ttu-id="e584c-254">因为 GridView s 编辑接口中定义其 Templatefield `ItemTemplate` s，`EditItemTemplate`进行多余，可能会被删除。</span><span class="sxs-lookup"><span data-stu-id="e584c-254">Since the GridView s editing interface is defined in its TemplateFields `ItemTemplate` s, the `EditItemTemplate` s are superfluous and may be deleted.</span></span>

<span data-ttu-id="e584c-255">使上面提到的格式设置更改后，将按钮控件添加和删除不必要`EditItemTemplate`s，页面 + s 声明性语法应如下所示：</span><span class="sxs-lookup"><span data-stu-id="e584c-255">After making the above mentioned formatting changes, adding the Button controls, and removing the unnecessary `EditItemTemplate` s, your page s declarative syntax should look like the following:</span></span>


[!code-aspx[Main](batch-updating-vb/samples/sample4.aspx)]

<span data-ttu-id="e584c-256">图 16 显示此页上，当后已添加按钮 Web 控件的浏览器查看和格式设置所做的更改。</span><span class="sxs-lookup"><span data-stu-id="e584c-256">Figure 16 shows this page when viewed through a browser after the Button Web controls have been added and the formatting changes made.</span></span>


<span data-ttu-id="e584c-257">[![页现在包含两个更新的产品按钮](batch-updating-vb/_static/image16.gif)](batch-updating-vb/_static/image23.png)</span><span class="sxs-lookup"><span data-stu-id="e584c-257">[![The Page Now Includes Two Update Products Buttons](batch-updating-vb/_static/image16.gif)](batch-updating-vb/_static/image23.png)</span></span>

<span data-ttu-id="e584c-258">**图 16**: 页现在包含两个更新的产品按钮 ([单击以查看实际尺寸的图像](batch-updating-vb/_static/image24.png))</span><span class="sxs-lookup"><span data-stu-id="e584c-258">**Figure 16**: The Page Now Includes Two Update Products Buttons ([Click to view full-size image](batch-updating-vb/_static/image24.png))</span></span>


## <a name="step-5-updating-the-products"></a><span data-ttu-id="e584c-259">步骤 5： 更新产品</span><span class="sxs-lookup"><span data-stu-id="e584c-259">Step 5: Updating the Products</span></span>

<span data-ttu-id="e584c-260">用户访问此页时它们将使其修改，然后单击两个更新产品按钮之一。</span><span class="sxs-lookup"><span data-stu-id="e584c-260">When a user visits this page they will make their modifications and then click one of the two Update Products buttons.</span></span> <span data-ttu-id="e584c-261">此时，我们需要以某种方式保存到每个行的用户输入值`ProductsDataTable`实例，然后将其传递给将然后将其传递的 BLL 方法`ProductsDataTable`实例与 DAL 的`UpdateWithTransaction`方法。</span><span class="sxs-lookup"><span data-stu-id="e584c-261">At that point we need to somehow save the user-entered values for each row into a `ProductsDataTable` instance and then pass that to a BLL method that will then pass that `ProductsDataTable` instance to the DAL s `UpdateWithTransaction` method.</span></span> <span data-ttu-id="e584c-262">`UpdateWithTransaction`方法，我们在中创建[前面的教程](wrapping-database-modifications-within-a-transaction-vb.md)，可以确保批更改，将更新作为一个原子操作。</span><span class="sxs-lookup"><span data-stu-id="e584c-262">The `UpdateWithTransaction` method, which we created in the [preceding tutorial](wrapping-database-modifications-within-a-transaction-vb.md), ensures that the batch of changes will be updated as an atomic operation.</span></span>

<span data-ttu-id="e584c-263">创建一个名为方法`BatchUpdate`在`BatchUpdate.aspx.vb`并添加以下代码：</span><span class="sxs-lookup"><span data-stu-id="e584c-263">Create a method named `BatchUpdate` in `BatchUpdate.aspx.vb` and add the following code:</span></span>


[!code-vb[Main](batch-updating-vb/samples/sample5.vb)]

<span data-ttu-id="e584c-264">此方法首先获取的所有产品回到`ProductsDataTable`通过为 BLL 的调用`GetProducts`方法。</span><span class="sxs-lookup"><span data-stu-id="e584c-264">This method starts out by getting all of the products back in a `ProductsDataTable` via a call to the BLL s `GetProducts` method.</span></span> <span data-ttu-id="e584c-265">然后，它枚举`ProductGrid`GridView s [ `Rows`集合](https://msdn.microsoft.com/library/system.web.ui.webcontrols.gridview.rows(VS.80).aspx)。</span><span class="sxs-lookup"><span data-stu-id="e584c-265">It then enumerates the `ProductGrid` GridView s [`Rows` collection](https://msdn.microsoft.com/library/system.web.ui.webcontrols.gridview.rows(VS.80).aspx).</span></span> <span data-ttu-id="e584c-266">`Rows`集合中包含[`GridViewRow`实例](https://msdn.microsoft.com/library/system.web.ui.webcontrols.gridviewrow.aspx)GridView 中显示每个行。</span><span class="sxs-lookup"><span data-stu-id="e584c-266">The `Rows` collection contains a [`GridViewRow` instance](https://msdn.microsoft.com/library/system.web.ui.webcontrols.gridviewrow.aspx) for each row displayed in the GridView.</span></span> <span data-ttu-id="e584c-267">因为我们还展示了最多十个行，每页、 GridView 的`Rows`集合将会有不能超过 10 个项。</span><span class="sxs-lookup"><span data-stu-id="e584c-267">Since we are showing at most ten rows per page, the GridView s `Rows` collection will have no more than ten items.</span></span>

<span data-ttu-id="e584c-268">每个行`ProductID`获取从`DataKeys`集合和相应`ProductsRow`从选择`ProductsDataTable`。</span><span class="sxs-lookup"><span data-stu-id="e584c-268">For each row the `ProductID` is grabbed from the `DataKeys` collection and the appropriate `ProductsRow` is selected from the `ProductsDataTable`.</span></span> <span data-ttu-id="e584c-269">四个 TemplateField 输入的控件以编程方式进行引用，而它们的值分配给`ProductsRow`实例 s 属性。</span><span class="sxs-lookup"><span data-stu-id="e584c-269">The four TemplateField input controls are programmatically referenced and their values assigned to the `ProductsRow` instance s properties.</span></span> <span data-ttu-id="e584c-270">在每个 GridView 后行的值已用于更新`ProductsDataTable`，它传递给 BLL s`UpdateWithTransaction`方法，正如我们看到在上一教程中，只是调用向下 DAL 的`UpdateWithTransaction`方法。</span><span class="sxs-lookup"><span data-stu-id="e584c-270">After each GridView row s values have been used to update the `ProductsDataTable`, it s passed to the BLL s `UpdateWithTransaction` method which, as we saw in the preceding tutorial, simply calls down into the DAL s `UpdateWithTransaction` method.</span></span>

<span data-ttu-id="e584c-271">本教程使用的批处理更新算法更新中的每一行`ProductsDataTable`GridView，无论是否已更改产品的信息中的行相对应。</span><span class="sxs-lookup"><span data-stu-id="e584c-271">The batch update algorithm used for this tutorial updates each row in the `ProductsDataTable` that corresponds to a row in the GridView, regardless of whether the product s information has been changed.</span></span> <span data-ttu-id="e584c-272">而此类 blind 更新计划运行 t 通常性能问题，它们可能会导致多余记录如果您重新审核更改为数据库表。</span><span class="sxs-lookup"><span data-stu-id="e584c-272">While such blind updates aren t usually a performance issue, they can lead to superfluous records if you re auditing changes to the database table.</span></span> <span data-ttu-id="e584c-273">回到[执行批处理更新](../editing-and-deleting-data-through-the-datalist/performing-batch-updates-vb.md)教程，我们探讨了使用 DataList 更新接口一批，并添加了代码，只会更新实际已由用户修改这些记录。</span><span class="sxs-lookup"><span data-stu-id="e584c-273">Back in the [Performing Batch Updates](../editing-and-deleting-data-through-the-datalist/performing-batch-updates-vb.md) tutorial we explored a batch updating interface with the DataList and added code that would only update those records that were actually modified by the user.</span></span> <span data-ttu-id="e584c-274">可使用从技术[执行批处理更新](../editing-and-deleting-data-through-the-datalist/performing-batch-updates-vb.md)更新代码，在本教程中，如果所需的。</span><span class="sxs-lookup"><span data-stu-id="e584c-274">Feel free to use the techniques from [Performing Batch Updates](../editing-and-deleting-data-through-the-datalist/performing-batch-updates-vb.md) to update the code in this tutorial, if desired.</span></span>

> [!NOTE]
> <span data-ttu-id="e584c-275">当数据源绑定到 GridView 通过其智能标记，Visual Studio 会自动向数据源 s 主值 GridView 的`DataKeyNames`属性。</span><span class="sxs-lookup"><span data-stu-id="e584c-275">When binding the data source to the GridView through its smart tag, Visual Studio automatically assigns the data source s primary key value(s) to the GridView s `DataKeyNames` property.</span></span> <span data-ttu-id="e584c-276">如果您未绑定 ObjectDataSource 到 GridView s 智能标记通过 GridView 步骤 1 中所述，则将需要手动设置 GridView s`DataKeyNames`属性设置为产品 id 才能访问`ProductID`通过每一行的值`DataKeys`集合。</span><span class="sxs-lookup"><span data-stu-id="e584c-276">If you did not bind the ObjectDataSource to the GridView through the GridView s smart tag as outlined in Step 1, then you will need to manually set the GridView s `DataKeyNames` property to ProductID in order to access the `ProductID` value for each row through the `DataKeys` collection.</span></span>


<span data-ttu-id="e584c-277">中使用的代码`BatchUpdate`类似于 BLL s 中用`UpdateProduct`方法，主要区别在于，在`UpdateProduct`方法只有一个`ProductRow`从体系结构中检索实例。</span><span class="sxs-lookup"><span data-stu-id="e584c-277">The code used in `BatchUpdate` is similar to that used in the BLL s `UpdateProduct` methods, the main difference being that in the `UpdateProduct` methods only a single `ProductRow` instance is retrieved from the architecture.</span></span> <span data-ttu-id="e584c-278">分配的属性的代码`ProductRow`之间是相同的`UpdateProducts`方法和中的代码`For Each`循环中`BatchUpdate`，如是总体模式。</span><span class="sxs-lookup"><span data-stu-id="e584c-278">The code that assigns the properties of the `ProductRow` is the same between the `UpdateProducts` methods and the code within the `For Each` loop in `BatchUpdate`, as is the overall pattern.</span></span>

<span data-ttu-id="e584c-279">若要完成本教程，我们必须具备`BatchUpdate`方法时调用任一更新产品按钮单击。</span><span class="sxs-lookup"><span data-stu-id="e584c-279">To complete this tutorial, we need to have the `BatchUpdate` method invoked when either of the Update Products buttons is clicked.</span></span> <span data-ttu-id="e584c-280">创建事件处理程序`Click`事件这两个按钮控件，然后在事件处理程序中添加以下代码：</span><span class="sxs-lookup"><span data-stu-id="e584c-280">Create event handlers for the `Click` events of these two Button controls and add the following code in the event handlers:</span></span>


[!code-vb[Main](batch-updating-vb/samples/sample6.vb)]

<span data-ttu-id="e584c-281">第一次调用`BatchUpdate`。</span><span class="sxs-lookup"><span data-stu-id="e584c-281">First a call is made to `BatchUpdate`.</span></span> <span data-ttu-id="e584c-282">下一步， [ `ClientScript`属性](https://msdn.microsoft.com/library/system.web.ui.page.clientscript(VS.80).aspx)用于注入 JavaScript，它将显示已更新的产品中读取一个消息框。</span><span class="sxs-lookup"><span data-stu-id="e584c-282">Next, the [`ClientScript` property](https://msdn.microsoft.com/library/system.web.ui.page.clientscript(VS.80).aspx) is used to inject JavaScript that will display a messagebox that reads The products have been updated.</span></span>

<span data-ttu-id="e584c-283">花点时间来测试此代码。</span><span class="sxs-lookup"><span data-stu-id="e584c-283">Take a minute to test out this code.</span></span> <span data-ttu-id="e584c-284">请访问`BatchUpdate.aspx`通过浏览器中，编辑的行数，并单击其中一个更新产品按钮。</span><span class="sxs-lookup"><span data-stu-id="e584c-284">Visit `BatchUpdate.aspx` through a browser, edit a number of rows, and click one of the Update Products buttons.</span></span> <span data-ttu-id="e584c-285">假设没有任何输入的验证错误，应看到已更新的产品中读取一个消息框。</span><span class="sxs-lookup"><span data-stu-id="e584c-285">Assuming there are no input validation errors, you should see a messagebox that reads The products have been updated.</span></span> <span data-ttu-id="e584c-286">若要验证更新的原子性，请考虑添加一个随机`CHECK`约束，不允许类似`UnitPrice`1234.56 的值。</span><span class="sxs-lookup"><span data-stu-id="e584c-286">To verify the atomicity of the update, consider adding a random `CHECK` constraint, like one that disallows `UnitPrice` values of 1234.56.</span></span> <span data-ttu-id="e584c-287">然后从`BatchUpdate.aspx`，编辑对多条记录，并确保设置一个产品的`UnitPrice`禁止使用的值 (1234.56) 的值。</span><span class="sxs-lookup"><span data-stu-id="e584c-287">Then from `BatchUpdate.aspx`, edit a number of records, making sure to set one of the product s `UnitPrice` value to the forbidden value ( 1234.56 ).</span></span> <span data-ttu-id="e584c-288">在该批处理操作期间所做的更改与单击更新产品回滚到其原始值时，应导致错误。</span><span class="sxs-lookup"><span data-stu-id="e584c-288">This should result in an error when clicking Update Products with the other changes during that batch operation rolled back to their original values.</span></span>

## <a name="an-alternativebatchupdatemethod"></a><span data-ttu-id="e584c-289">一种替代方法`BatchUpdate`方法</span><span class="sxs-lookup"><span data-stu-id="e584c-289">An Alternative`BatchUpdate`Method</span></span>

<span data-ttu-id="e584c-290">`BatchUpdate`方法，我们只需检查检索*所有*BLL s 中的产品的`GetProducts`方法，然后更新在 GridView 中显示的那些记录。</span><span class="sxs-lookup"><span data-stu-id="e584c-290">The `BatchUpdate` method we just examined retrieves *all* of the products from the BLL s `GetProducts` method and then updates just those records that appear in the GridView.</span></span> <span data-ttu-id="e584c-291">如果 GridView 不使用分页，但如果是这样，可能有数百、 数千个或成千上万的产品，但在 GridView 中的十个行，这种方法是理想之选。</span><span class="sxs-lookup"><span data-stu-id="e584c-291">This approach is ideal if the GridView does not use paging, but if it does, there may be hundreds, thousands, or tens of thousands of products, but only ten rows in the GridView.</span></span> <span data-ttu-id="e584c-292">在这种情况下，从仅要修改其中 10 家的数据库中获取的所有产品都是不太理想。</span><span class="sxs-lookup"><span data-stu-id="e584c-292">In such a case, getting all of the products from the database only to modify 10 of them is less than ideal.</span></span>

<span data-ttu-id="e584c-293">对于这些类型的情况下，请考虑使用以下`BatchUpdateAlternate`方法改为：</span><span class="sxs-lookup"><span data-stu-id="e584c-293">For those types of situations, consider using the following `BatchUpdateAlternate` method instead:</span></span>


[!code-vb[Main](batch-updating-vb/samples/sample7.vb)]

<span data-ttu-id="e584c-294">`BatchMethodAlternate` 首先创建一个新的空`ProductsDataTable`名为`products`。</span><span class="sxs-lookup"><span data-stu-id="e584c-294">`BatchMethodAlternate` starts by creating a new empty `ProductsDataTable` named `products`.</span></span> <span data-ttu-id="e584c-295">然后，它通过 GridView s 的步骤`Rows`集合并为每一行获取特定产品信息使用 BLL 的`GetProductByProductID(productID)`方法。</span><span class="sxs-lookup"><span data-stu-id="e584c-295">It then steps through the GridView s `Rows` collection and for each row gets the particular product information using the BLL s `GetProductByProductID(productID)` method.</span></span> <span data-ttu-id="e584c-296">检索到`ProductsRow`实例具有相同的方式更新其属性`BatchUpdate`，但更新导入的行后`products``ProductsDataTable`通过 DataTable s [ `ImportRow(DataRow)`方法](https://msdn.microsoft.com/library/system.data.datatable.importrow(VS.80).aspx).</span><span class="sxs-lookup"><span data-stu-id="e584c-296">The retrieved `ProductsRow` instance has its properties updated in the same fashion as `BatchUpdate`, but after updating the row it is imported into the `products` `ProductsDataTable` via the DataTable s [`ImportRow(DataRow)` method](https://msdn.microsoft.com/library/system.data.datatable.importrow(VS.80).aspx).</span></span>

<span data-ttu-id="e584c-297">之后`For Each`循环完成后，`products`包含一个`ProductsRow`GridView 中的每一行的实例。</span><span class="sxs-lookup"><span data-stu-id="e584c-297">After the `For Each` loop completes, `products` contains one `ProductsRow` instance for each row in the GridView.</span></span> <span data-ttu-id="e584c-298">由于每个`ProductsRow`已添加到实例`products`（而不是已更新），如果我们会盲目地将其传递给`UpdateWithTransaction`方法`ProductsTableAdatper`将尝试将每个记录插入数据库。</span><span class="sxs-lookup"><span data-stu-id="e584c-298">Since each of the `ProductsRow` instances have been added to the `products` (instead of updated), if we blindly pass it to the `UpdateWithTransaction` method the `ProductsTableAdatper` will try to insert each of the records into the database.</span></span> <span data-ttu-id="e584c-299">相反，我们需要指定这些行的每个已修改了 （未添加）。</span><span class="sxs-lookup"><span data-stu-id="e584c-299">Instead, we need to specify that each of these rows has been modified (not added).</span></span>

<span data-ttu-id="e584c-300">这可以通过将新方法添加到名为 BLL `UpdateProductsWithTransaction`。</span><span class="sxs-lookup"><span data-stu-id="e584c-300">This can be accomplished by adding a new method to the BLL named `UpdateProductsWithTransaction`.</span></span> <span data-ttu-id="e584c-301">`UpdateProductsWithTransaction`如下所示的设置`RowState`的每个`ProductsRow`实例中`ProductsDataTable`到`Modified`，然后将传递`ProductsDataTable`为 DAL 的`UpdateWithTransaction`方法。</span><span class="sxs-lookup"><span data-stu-id="e584c-301">`UpdateProductsWithTransaction`, shown below, sets the `RowState` of each of the `ProductsRow` instances in the `ProductsDataTable` to `Modified` and then passes the `ProductsDataTable` to the DAL s `UpdateWithTransaction` method.</span></span>


[!code-vb[Main](batch-updating-vb/samples/sample8.vb)]

## <a name="summary"></a><span data-ttu-id="e584c-302">总结</span><span class="sxs-lookup"><span data-stu-id="e584c-302">Summary</span></span>

<span data-ttu-id="e584c-303">GridView 提供内置的每行的编辑功能，但不能创建完全可编辑的接口的支持。</span><span class="sxs-lookup"><span data-stu-id="e584c-303">The GridView provides built-in per-row editing capabilities, but lacks support for creating fully editable interfaces.</span></span> <span data-ttu-id="e584c-304">正如我们看到在本教程中，此类接口是可能的但需要的工作。</span><span class="sxs-lookup"><span data-stu-id="e584c-304">As we saw in this tutorial, such interfaces are possible, but require a bit of work.</span></span> <span data-ttu-id="e584c-305">若要创建的每个行是可编辑一个 GridView，我们需要将转换为 Templatefield GridView 的字段并定义中的编辑界面`ItemTemplate`s。</span><span class="sxs-lookup"><span data-stu-id="e584c-305">To create a GridView where every row is editable, we need to convert the GridView s fields into TemplateFields and define the editing interface within the `ItemTemplate` s.</span></span> <span data-ttu-id="e584c-306">此外，更新所有的按钮 Web 控件的类型必须被添加到页上，独立于 GridView。</span><span class="sxs-lookup"><span data-stu-id="e584c-306">Additionally, Update All -type Button Web controls must be added to the page, separate from the GridView.</span></span> <span data-ttu-id="e584c-307">这些按钮`Click`事件处理程序需要枚举 GridView s`Rows`集合中，存储中的更改`ProductsDataTable`，并将更新的信息传递到适当的 BLL 方法。</span><span class="sxs-lookup"><span data-stu-id="e584c-307">These Buttons `Click` event handlers need to enumerate the GridView s `Rows` collection, store the changes in a `ProductsDataTable`, and pass the updated information into the appropriate BLL method.</span></span>

<span data-ttu-id="e584c-308">在下一教程中我们将了解如何创建用于方式成批删除的接口。</span><span class="sxs-lookup"><span data-stu-id="e584c-308">In the next tutorial we'll see how to create an interface for batch deleting.</span></span> <span data-ttu-id="e584c-309">具体而言，每个 GridView 行将包含一个复选框和而不是更新所有的键入按钮，我们将删除所选行按钮。</span><span class="sxs-lookup"><span data-stu-id="e584c-309">In particular, each GridView row will include a checkbox and instead of Update All -type buttons, we'll have Delete Selected Rows buttons.</span></span>

<span data-ttu-id="e584c-310">快乐编程 ！</span><span class="sxs-lookup"><span data-stu-id="e584c-310">Happy Programming!</span></span>

## <a name="about-the-author"></a><span data-ttu-id="e584c-311">关于作者</span><span class="sxs-lookup"><span data-stu-id="e584c-311">About the Author</span></span>

<span data-ttu-id="e584c-312">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml)的七个部 asp/ASP.NET 书籍并创办了作者[4GuysFromRolla.com](http://www.4guysfromrolla.com)，自 1998 年以来一直致力于 Microsoft Web 技术。</span><span class="sxs-lookup"><span data-stu-id="e584c-312">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), author of seven ASP/ASP.NET books and founder of [4GuysFromRolla.com](http://www.4guysfromrolla.com), has been working with Microsoft Web technologies since 1998.</span></span> <span data-ttu-id="e584c-313">Scott 是独立的顾问、 培训师和编写器。</span><span class="sxs-lookup"><span data-stu-id="e584c-313">Scott works as an independent consultant, trainer, and writer.</span></span> <span data-ttu-id="e584c-314">他最新著作是[ *Sams Teach 自己 ASP.NET 2.0 24 小时内*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco)。</span><span class="sxs-lookup"><span data-stu-id="e584c-314">His latest book is [*Sams Teach Yourself ASP.NET 2.0 in 24 Hours*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span></span> <span data-ttu-id="e584c-315">他可以到达[ mitchell@4GuysFromRolla.com。](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="e584c-315">He can be reached at [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span></span> <span data-ttu-id="e584c-316">或通过他的博客，其中，请参阅[ http://ScottOnWriting.NET ](http://ScottOnWriting.NET)。</span><span class="sxs-lookup"><span data-stu-id="e584c-316">or via his blog, which can be found at [http://ScottOnWriting.NET](http://ScottOnWriting.NET).</span></span>

## <a name="special-thanks-to"></a><span data-ttu-id="e584c-317">特别感谢</span><span class="sxs-lookup"><span data-stu-id="e584c-317">Special Thanks To</span></span>

<span data-ttu-id="e584c-318">很多有用的审阅者已评审本系列教程。</span><span class="sxs-lookup"><span data-stu-id="e584c-318">This tutorial series was reviewed by many helpful reviewers.</span></span> <span data-ttu-id="e584c-319">本教程中的潜在顾客审阅者是 Teresa Murphy 和 David Suru。</span><span class="sxs-lookup"><span data-stu-id="e584c-319">Lead reviewers for this tutorial were Teresa Murphy and David Suru.</span></span> <span data-ttu-id="e584c-320">是否有兴趣查看我即将推出的 MSDN 文章？</span><span class="sxs-lookup"><span data-stu-id="e584c-320">Interested in reviewing my upcoming MSDN articles?</span></span> <span data-ttu-id="e584c-321">如果是这样，给我在行[ mitchell@4GuysFromRolla.com。](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="e584c-321">If so, drop me a line at [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="e584c-322">[上一页](wrapping-database-modifications-within-a-transaction-vb.md)
> [下一页](batch-deleting-vb.md)</span><span class="sxs-lookup"><span data-stu-id="e584c-322">[Previous](wrapping-database-modifications-within-a-transaction-vb.md)
[Next](batch-deleting-vb.md)</span></span>
