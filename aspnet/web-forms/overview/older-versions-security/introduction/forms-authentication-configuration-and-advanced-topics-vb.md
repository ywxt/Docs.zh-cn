---
uid: web-forms/overview/older-versions-security/introduction/forms-authentication-configuration-and-advanced-topics-vb
title: 窗体身份验证配置和高级的主题 (VB) |Microsoft Docs
author: rick-anderson
description: 在本教程中我们将检查各种窗体身份验证设置，并了解如何通过窗体元素对其进行修改。 这将需要一个详细...
ms.author: aspnetcontent
ms.date: 01/14/2008
ms.assetid: 829d2f56-5c48-445b-b826-3418a450c788
msc.legacyurl: /web-forms/overview/older-versions-security/introduction/forms-authentication-configuration-and-advanced-topics-vb
msc.type: authoredcontent
ms.openlocfilehash: d740694772b0fc5ad1c9c4eb5763004523586631
ms.sourcegitcommit: b28cd0313af316c051c2ff8549865bff67f2fbb4
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 07/05/2018
ms.locfileid: "37831137"
---
<a name="forms-authentication-configuration-and-advanced-topics-vb"></a><span data-ttu-id="cc488-104">窗体身份验证配置和高级的主题 (VB)</span><span class="sxs-lookup"><span data-stu-id="cc488-104">Forms Authentication Configuration and Advanced Topics (VB)</span></span>
====================
<span data-ttu-id="cc488-105">通过[Scott Mitchell](https://twitter.com/ScottOnWriting)</span><span class="sxs-lookup"><span data-stu-id="cc488-105">by [Scott Mitchell](https://twitter.com/ScottOnWriting)</span></span>

<span data-ttu-id="cc488-106">[下载代码](http://download.microsoft.com/download/2/F/7/2F705A34-F9DE-4112-BBDE-60098089645E/ASPNET_Security_Tutorial_03_VB.zip)或[下载 PDF](http://download.microsoft.com/download/2/F/7/2F705A34-F9DE-4112-BBDE-60098089645E/aspnet_tutorial03_AuthAdvanced_vb.pdf)</span><span class="sxs-lookup"><span data-stu-id="cc488-106">[Download Code](http://download.microsoft.com/download/2/F/7/2F705A34-F9DE-4112-BBDE-60098089645E/ASPNET_Security_Tutorial_03_VB.zip) or [Download PDF](http://download.microsoft.com/download/2/F/7/2F705A34-F9DE-4112-BBDE-60098089645E/aspnet_tutorial03_AuthAdvanced_vb.pdf)</span></span>

> <span data-ttu-id="cc488-107">在本教程中我们将检查各种窗体身份验证设置，并了解如何通过窗体元素对其进行修改。</span><span class="sxs-lookup"><span data-stu-id="cc488-107">In this tutorial we will examine the various forms authentication settings and see how to modify them through the forms element.</span></span> <span data-ttu-id="cc488-108">这将需要自定义窗体身份验证票证超时值，使用自定义 URL （例如 SignIn.aspx 而不是 login.aspx 的情况) 和无 cookie forms 身份验证票证的登录页的详细的信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-108">This will entail a detailed look at customizing the forms authentication ticket's timeout value, using a login page with a custom URL (like SignIn.aspx instead of Login.aspx), and cookieless forms authentication tickets.</span></span>


## <a name="introduction"></a><span data-ttu-id="cc488-109">介绍</span><span class="sxs-lookup"><span data-stu-id="cc488-109">Introduction</span></span>

<span data-ttu-id="cc488-110">在中[前一篇教程](an-overview-of-forms-authentication-vb.md)，我们介绍了在 ASP.NET 应用程序，从 Web.config 中指定配置设置要在页中显示不同创建日志中实现窗体身份验证所必需的步骤用户经过身份验证和匿名用户的内容。</span><span class="sxs-lookup"><span data-stu-id="cc488-110">In the [previous tutorial](an-overview-of-forms-authentication-vb.md) we looked at the steps necessary for implementing forms authentication in an ASP.NET application, from specifying configuration settings in Web.config to creating a log in page to displaying different content for authenticated and anonymous users.</span></span> <span data-ttu-id="cc488-111">回想一下，我们将此网站配置为使用窗体身份验证的模式属性设置&lt;身份验证&gt;向窗体元素。</span><span class="sxs-lookup"><span data-stu-id="cc488-111">Recall that we configured the website to use forms authentication by setting the mode attribute of the &lt;authentication&gt; element to Forms.</span></span> <span data-ttu-id="cc488-112">&lt;身份验证&gt;元素可以根据需要包含&lt;窗体&gt;子元素，可以通过它指定具有多种类型的窗体身份验证设置。</span><span class="sxs-lookup"><span data-stu-id="cc488-112">The &lt;authentication&gt; element may optionally include a &lt;forms&gt; child element, through which an assortment of forms authentication settings may be specified.</span></span>

<span data-ttu-id="cc488-113">在本教程中我们将检查各种窗体身份验证设置，并了解如何通过修改&lt;窗体&gt;元素。</span><span class="sxs-lookup"><span data-stu-id="cc488-113">In this tutorial we will examine the various forms authentication settings and see how to modify them through the &lt;forms&gt; element.</span></span> <span data-ttu-id="cc488-114">这将需要自定义窗体身份验证票证超时值，使用自定义 URL （例如 SignIn.aspx 而不是 login.aspx 的情况) 和无 cookie forms 身份验证票证的登录页的详细的信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-114">This will entail a detailed look at customizing the forms authentication ticket's timeout value, using a login page with a custom URL (like SignIn.aspx instead of Login.aspx), and cookieless forms authentication tickets.</span></span> <span data-ttu-id="cc488-115">我们将更加仔细地检查窗体身份验证票证的构成，并请参阅 ASP.NET 所需确保票证的数据安全的即使检查和篡改的预防措施。</span><span class="sxs-lookup"><span data-stu-id="cc488-115">We will also examine the makeup of the forms authentication ticket more closely and see the precautions ASP.NET takes to ensure that the ticket's data is secure from inspection and tampering.</span></span> <span data-ttu-id="cc488-116">最后，我们将考察如何将额外的用户数据存储在窗体身份验证票证以及如何通过自定义主体对象的此数据进行建模。</span><span class="sxs-lookup"><span data-stu-id="cc488-116">Finally, we will look at how to store extra user data in the forms authentication ticket and how to model this data through a custom principal object.</span></span>

## <a name="step-1-examining-the-ltformsgt-configuration-settings"></a><span data-ttu-id="cc488-117">第 1 步： 检查&lt;窗体&gt;配置设置</span><span class="sxs-lookup"><span data-stu-id="cc488-117">Step 1: Examining the &lt;forms&gt; Configuration Settings</span></span>

<span data-ttu-id="cc488-118">在 ASP.NET 中的窗体身份验证系统提供多种可自定义应用程序的应用程序基础的配置设置。</span><span class="sxs-lookup"><span data-stu-id="cc488-118">The forms authentication system in ASP.NET offers a number of configuration settings that can be customized on an application-by-application basis.</span></span> <span data-ttu-id="cc488-119">这包括设置，例如： 票证生存期的窗体身份验证;哪种保护应用于票证;在哪些条件无 cookie 身份验证票证都使用;登录页中; 的路径和其他信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-119">This includes settings like: the lifetime of the forms authentication ticket; what sort of protection is applied to the ticket; under what conditions cookieless authentication tickets are used; the path to the login page; and other information.</span></span> <span data-ttu-id="cc488-120">若要修改的默认值，添加[&lt;窗体&gt;元素](https://msdn.microsoft.com/library/1d3t3c61.aspx)的小孩[&lt;身份验证&gt;元素](https://msdn.microsoft.com/library/532aee0e.aspx)，指定这些属性想要自定义为 XML 属性的值如下所示：</span><span class="sxs-lookup"><span data-stu-id="cc488-120">To modify the default values, add a [&lt;forms&gt; element](https://msdn.microsoft.com/library/1d3t3c61.aspx) as a child of the [&lt;authentication&gt; element](https://msdn.microsoft.com/library/532aee0e.aspx), specifying those property values you want to customize as XML attributes like so:</span></span>

[!code-xml[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample1.xml)]

<span data-ttu-id="cc488-121">表 1 总结了可以通过自定义的属性&lt;窗体&gt;元素。</span><span class="sxs-lookup"><span data-stu-id="cc488-121">Table 1 summarizes the properties that can be customized through the &lt;forms&gt; element.</span></span> <span data-ttu-id="cc488-122">Web.config 是一个 XML 文件，因为左侧列中的属性名称是区分大小写。</span><span class="sxs-lookup"><span data-stu-id="cc488-122">Since Web.config is an XML file, the attribute names in the left column are case-sensitive.</span></span>


| <span data-ttu-id="cc488-123"><strong>特性</strong></span><span class="sxs-lookup"><span data-stu-id="cc488-123"><strong>Attribute</strong></span></span> |                                                                                                                                                                                                                                     <span data-ttu-id="cc488-124"><strong>说明</strong></span><span class="sxs-lookup"><span data-stu-id="cc488-124"><strong>Description</strong></span></span>                                                                                                                                                                                                                                      |
|----------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|         <span data-ttu-id="cc488-125">无 cookie</span><span class="sxs-lookup"><span data-stu-id="cc488-125">cookieless</span></span>         |                                                                                                                <span data-ttu-id="cc488-126">此属性指定在什么条件下的身份验证票证存储在与要在 URL 中嵌入的 cookie。</span><span class="sxs-lookup"><span data-stu-id="cc488-126">This attribute specifies under what conditions the authentication ticket is stored in a cookie versus being embedded in the URL.</span></span> <span data-ttu-id="cc488-127">允许的值为： UseCookies;UseUri;自动检测;和 UseDeviceProfile （默认值）。</span><span class="sxs-lookup"><span data-stu-id="cc488-127">Allowable values are: UseCookies; UseUri; AutoDetect; and UseDeviceProfile (the default).</span></span> <span data-ttu-id="cc488-128">步骤 2 检查更多详细信息中的此设置。</span><span class="sxs-lookup"><span data-stu-id="cc488-128">Step 2 examines this setting in more detail.</span></span>                                                                                                                |
|         <span data-ttu-id="cc488-129">defaultUrl</span><span class="sxs-lookup"><span data-stu-id="cc488-129">defaultUrl</span></span>         |                                                                                                                                                         <span data-ttu-id="cc488-130">指示用户将重定向到登录后从登录页如果不存在 RedirectUrl 值在查询字符串中指定的 URL。</span><span class="sxs-lookup"><span data-stu-id="cc488-130">Indicates the URL that users are redirected to after signing in from the login page if there is no RedirectUrl value specified in the querystring.</span></span> <span data-ttu-id="cc488-131">默认值为 default.aspx。</span><span class="sxs-lookup"><span data-stu-id="cc488-131">The default value is default.aspx.</span></span>                                                                                                                                                         |
|           <span data-ttu-id="cc488-132">域</span><span class="sxs-lookup"><span data-stu-id="cc488-132">domain</span></span>           | <span data-ttu-id="cc488-133">当使用基于 cookie 的身份验证票证，此设置指定 cookie s 域值。</span><span class="sxs-lookup"><span data-stu-id="cc488-133">When using cookie-based authentication tickets, this setting specifies the cookie s domain value.</span></span> <span data-ttu-id="cc488-134">默认值为空字符串，这会导致浏览器使用从该情况下，它已签发 （如 www.yourdomain.com) 的域。</span><span class="sxs-lookup"><span data-stu-id="cc488-134">The default value is an empty string, which causes the browser to use the domain from which it was issued (such as www.yourdomain.com).</span></span> <span data-ttu-id="cc488-135">在这种情况下，将 cookie<strong>不</strong>到子域，例如 admin.yourdomain.com 进行请求时发送。</span><span class="sxs-lookup"><span data-stu-id="cc488-135">In this case, the cookie will <strong>not</strong> be sent when making requests to subdomains, such as admin.yourdomain.com.</span></span> <span data-ttu-id="cc488-136">如果您想要传递给自定义将其设置为 yourdomain.com 域的属性所需的所有子域的 cookie。</span><span class="sxs-lookup"><span data-stu-id="cc488-136">If you want the cookie to be passed to all subdomains you need to customize the domain attribute setting it to yourdomain.com.</span></span> |
|  <span data-ttu-id="cc488-137">enableCrossAppRedirects</span><span class="sxs-lookup"><span data-stu-id="cc488-137">enableCrossAppRedirects</span></span>   |                                                                                                                                                                   <span data-ttu-id="cc488-138">一个布尔值，该值指示是否在同一台服务器上其他 web 应用程序重定向到 Url 时要记住身份验证的用户。</span><span class="sxs-lookup"><span data-stu-id="cc488-138">A Boolean value indicating whether authenticated users are remembered when redirected to URLs in other web applications on the same server.</span></span> <span data-ttu-id="cc488-139">默认值为 false。</span><span class="sxs-lookup"><span data-stu-id="cc488-139">The default is false.</span></span>                                                                                                                                                                   |
|          <span data-ttu-id="cc488-140">loginUrl</span><span class="sxs-lookup"><span data-stu-id="cc488-140">loginUrl</span></span>          |                                                                                                                                                                                                                      <span data-ttu-id="cc488-141">登录页的 URL。</span><span class="sxs-lookup"><span data-stu-id="cc488-141">The URL of the login page.</span></span> <span data-ttu-id="cc488-142">默认值为 login.aspx 的情况。</span><span class="sxs-lookup"><span data-stu-id="cc488-142">The default value is login.aspx.</span></span>                                                                                                                                                                                                                      |
|            <span data-ttu-id="cc488-143">name</span><span class="sxs-lookup"><span data-stu-id="cc488-143">name</span></span>            |                                                                                                                                                                                                   <span data-ttu-id="cc488-144">在使用基于 cookie 的身份验证票证的 cookie 的名称。</span><span class="sxs-lookup"><span data-stu-id="cc488-144">When using cookie-based authentication tickets, the name of the cookie.</span></span> <span data-ttu-id="cc488-145">默认值为。ASPXAUTH。</span><span class="sxs-lookup"><span data-stu-id="cc488-145">The default is .ASPXAUTH.</span></span>                                                                                                                                                                                                   |
|            <span data-ttu-id="cc488-146">path</span><span class="sxs-lookup"><span data-stu-id="cc488-146">path</span></span>            |                                                                             <span data-ttu-id="cc488-147">当使用基于 cookie 的身份验证票证，此设置指定 cookie s 路径属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-147">When using cookie-based authentication tickets, this setting specifies the cookie s path attribute.</span></span> <span data-ttu-id="cc488-148">路径属性使开发人员来限制到特定的目录层次结构的 cookie 的范围。</span><span class="sxs-lookup"><span data-stu-id="cc488-148">The path attribute enables a developer to limit the scope of a cookie to a particular directory hierarchy.</span></span> <span data-ttu-id="cc488-149">默认值是/，通知将发送到对域进行的任何请求的身份验证票证 cookie 的浏览器。</span><span class="sxs-lookup"><span data-stu-id="cc488-149">The default value is /, which informs the browser to send the authentication ticket cookie to any request made to the domain.</span></span>                                                                              |
|         <span data-ttu-id="cc488-150">保护</span><span class="sxs-lookup"><span data-stu-id="cc488-150">protection</span></span>         |                                                                                                                                            <span data-ttu-id="cc488-151">指示哪些技术用于保护窗体身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-151">Indicates what techniques are used to protect the forms authentication ticket.</span></span> <span data-ttu-id="cc488-152">允许的值为： 所有 （默认）;加密;None;和验证。</span><span class="sxs-lookup"><span data-stu-id="cc488-152">The allowable values are: All (the default); Encryption; None; and Validation.</span></span> <span data-ttu-id="cc488-153">在步骤 3 中的详细讨论了这些设置。</span><span class="sxs-lookup"><span data-stu-id="cc488-153">These settings are discussed in detail in Step 3.</span></span>                                                                                                                                            |
|         <span data-ttu-id="cc488-154">requireSSL</span><span class="sxs-lookup"><span data-stu-id="cc488-154">requireSSL</span></span>         |                                                                                                                                                                                <span data-ttu-id="cc488-155">一个布尔值，该值指示是否需要 SSL 连接来传输身份验证 cookie。</span><span class="sxs-lookup"><span data-stu-id="cc488-155">A Boolean value that indicates whether an SSL connection is required to transmit the authentication cookie.</span></span> <span data-ttu-id="cc488-156">默认值为 False。</span><span class="sxs-lookup"><span data-stu-id="cc488-156">The default value is false.</span></span>                                                                                                                                                                                |
|     <span data-ttu-id="cc488-157">slidingExpiration</span><span class="sxs-lookup"><span data-stu-id="cc488-157">slidingExpiration</span></span>      |                                                                                                 <span data-ttu-id="cc488-158">一个布尔值，该值指示用户是否每次重置的身份验证 cookie s 超时值在单个会话期间访问该网站。</span><span class="sxs-lookup"><span data-stu-id="cc488-158">A Boolean value that indicates whether the authentication cookie s timeout is reset each time the user visits the site during a single session.</span></span> <span data-ttu-id="cc488-159">默认值为 true。</span><span class="sxs-lookup"><span data-stu-id="cc488-159">The default value is true.</span></span> <span data-ttu-id="cc488-160">在指定的更详细地讨论身份验证票证超时策略票证的超时值部分。</span><span class="sxs-lookup"><span data-stu-id="cc488-160">The authentication ticket timeout policy is discussed in more detail in the Specifying the Ticket s Timeout Value section.</span></span>                                                                                                 |
|          <span data-ttu-id="cc488-161">超时</span><span class="sxs-lookup"><span data-stu-id="cc488-161">timeout</span></span>           |                                                                                                                               <span data-ttu-id="cc488-162">指定时间，以分钟为单位，在其后的身份验证票证 cookie 过期。</span><span class="sxs-lookup"><span data-stu-id="cc488-162">Specifies the time, in minutes, after which the authentication ticket cookie expires.</span></span> <span data-ttu-id="cc488-163">默认值为 30。</span><span class="sxs-lookup"><span data-stu-id="cc488-163">The default value is 30.</span></span> <span data-ttu-id="cc488-164">在指定的更详细地讨论身份验证票证超时策略票证的超时值部分。</span><span class="sxs-lookup"><span data-stu-id="cc488-164">The authentication ticket timeout policy is discussed in more detail in the Specifying the Ticket s Timeout Value section.</span></span>                                                                                                                               |

<span data-ttu-id="cc488-165">**表 1**: 摘要&lt;窗体&gt;元素的特性</span><span class="sxs-lookup"><span data-stu-id="cc488-165">**Table 1**: A Summary of the &lt;forms&gt; Element's Attributes</span></span>

<span data-ttu-id="cc488-166">在 ASP.NET 2.0 及更高版本，默认窗体身份验证值是硬编码在.NET Framework 中的 FormsAuthenticationConfiguration 类中。</span><span class="sxs-lookup"><span data-stu-id="cc488-166">In ASP.NET 2.0 and beyond, the default forms authentication values are hard-coded in the FormsAuthenticationConfiguration class in the .NET Framework.</span></span> <span data-ttu-id="cc488-167">必须在 Web.config 文件中应用程序的应用程序的基础上应用任何修改。</span><span class="sxs-lookup"><span data-stu-id="cc488-167">Any modifications must be applied on an application-by-application basis in the Web.config file.</span></span> <span data-ttu-id="cc488-168">这不同于 ASP.NET 1.x 中，其中默认窗体身份验证值在 machine.config 文件中存储 （并因此无法修改通过编辑 machine.config）。</span><span class="sxs-lookup"><span data-stu-id="cc488-168">This differs from ASP.NET 1.x, where the default forms authentication values were stored in the machine.config file (and could therefore be modified via editing machine.config).</span></span> <span data-ttu-id="cc488-169">While 主题有关的 ASP.NET 1.x 中，有必要提及窗体身份验证系统设置的数量在 ASP.NET 2.0 中具有不同的默认值及其他比在 ASP.NET 1.x。</span><span class="sxs-lookup"><span data-stu-id="cc488-169">While on the topic of ASP.NET 1.x, it is worthwhile to mention that a number of the forms authentication system settings have different default values in ASP.NET 2.0 and beyond than in ASP.NET 1.x.</span></span> <span data-ttu-id="cc488-170">如果要从 ASP.NET 1.x 环境迁移你的应用程序，它是必须要注意的这些差异。</span><span class="sxs-lookup"><span data-stu-id="cc488-170">If you are migrating your application from an ASP.NET 1.x environment, it is important to be aware of these differences.</span></span> <span data-ttu-id="cc488-171">请查阅[&lt;窗体&gt;元素技术文档](https://msdn.microsoft.com/library/1d3t3c61.aspx)有关差异的列表。</span><span class="sxs-lookup"><span data-stu-id="cc488-171">Consult [the &lt;forms&gt; element technical documentation](https://msdn.microsoft.com/library/1d3t3c61.aspx) for a list of the differences.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-172">多个窗体身份验证设置，如超时、 域和路径，指定生成的窗体身份验证票证 cookie 的详细信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-172">Several forms authentication settings, such as the timeout, domain, and path, specify details for the resulting forms authentication ticket cookie.</span></span> <span data-ttu-id="cc488-173">对 cookie、 工作原理和它们的各种属性的详细信息，请阅读[此 Cookie 教程](http://www.quirksmode.org/js/cookies.html)。</span><span class="sxs-lookup"><span data-stu-id="cc488-173">For more information on cookies, how they work, and their various properties, read [this Cookies tutorial](http://www.quirksmode.org/js/cookies.html).</span></span>


### <a name="specifying-the-tickets-timeout-value"></a><span data-ttu-id="cc488-174">指定的票证超时值</span><span class="sxs-lookup"><span data-stu-id="cc488-174">Specifying the Ticket's Timeout Value</span></span>

<span data-ttu-id="cc488-175">窗体身份验证票证是表示标识的令牌。</span><span class="sxs-lookup"><span data-stu-id="cc488-175">The forms authentication ticket is a token that represents an identity.</span></span> <span data-ttu-id="cc488-176">使用基于 cookie 的身份验证票证，此令牌中以 cookie 的形式保存并发送到 web 服务器上每个请求。</span><span class="sxs-lookup"><span data-stu-id="cc488-176">With cookie-based authentication tickets, this token is held in the form of a cookie and sent to the web server on each request.</span></span> <span data-ttu-id="cc488-177">拥有的令牌，从本质上讲，声明，我*用户名*，我已登录，并使用，以便可以跨页面访问记住用户的标识。</span><span class="sxs-lookup"><span data-stu-id="cc488-177">Possession of the token, in essence, declares, I am *username*, I have already logged in, and is used so that a user's identity can be remembered across page visits.</span></span>

<span data-ttu-id="cc488-178">窗体身份验证票证不仅包括用户的标识，而且还包含可帮助确保完整性和安全令牌的信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-178">The forms authentication ticket not only includes the user's identity, but also contains information to help ensure the integrity and security of the token.</span></span> <span data-ttu-id="cc488-179">毕竟，我们不希望恶意用户能够创建盗版的令牌，或以某种 underhanded 方式修改合法的令牌。</span><span class="sxs-lookup"><span data-stu-id="cc488-179">After all, we don't want a nefarious user to be able to create a counterfeit token, or to modify a legit token in some underhanded way.</span></span>

<span data-ttu-id="cc488-180">包含在票证中的一个此类位是*到期*，即的日期和时间票证将不再有效。</span><span class="sxs-lookup"><span data-stu-id="cc488-180">One such bit of information included in the ticket is an *expiry*, which is the date and time the ticket is no longer valid.</span></span> <span data-ttu-id="cc488-181">FormsAuthenticationModule 将检查身份验证票证，每次它确保不具有尚未传递票证的到期。</span><span class="sxs-lookup"><span data-stu-id="cc488-181">Each time the FormsAuthenticationModule inspects an authentication ticket, it ensures that the ticket's expiry has not yet passed.</span></span> <span data-ttu-id="cc488-182">如果是，它不考虑该票证，并标识为匿名用户。</span><span class="sxs-lookup"><span data-stu-id="cc488-182">If it has, it disregards the ticket and identifies the user as being anonymous.</span></span> <span data-ttu-id="cc488-183">这种保护措施，帮助防止重播攻击。</span><span class="sxs-lookup"><span data-stu-id="cc488-183">This safeguard helps protect against replay attacks.</span></span> <span data-ttu-id="cc488-184">而无需到期时间，如果黑客已能够通过物理访问其计算机并通过其 cookie 根可能是获取用户的有效的身份验证票证-她动手-他们可以将请求发送到具有此被盗的身份验证票证的服务器和获取条目。</span><span class="sxs-lookup"><span data-stu-id="cc488-184">Without an expiry, if a hacker was able to get her hands on a user's valid authentication ticket - perhaps by gaining physical access to their computer and rooting through their cookies - they could send a request to the server with this stolen authentication ticket and gain entry.</span></span> <span data-ttu-id="cc488-185">虽然到期时间不会阻止这种情况下，它却限制此类攻击可能会成功在此期间的窗口。</span><span class="sxs-lookup"><span data-stu-id="cc488-185">While the expiry doesn't prevent this scenario, it does limit the window during which such an attack can succeed.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-186">步骤 3 个窗体身份验证系统用来保护身份验证票证的详细信息更多技术。</span><span class="sxs-lookup"><span data-stu-id="cc488-186">Step 3 details additional techniques used by the forms authentication system to protect the authentication ticket.</span></span>


<span data-ttu-id="cc488-187">在创建时的身份验证票证，窗体身份验证系统通过咨询超时设置来确定其到期。</span><span class="sxs-lookup"><span data-stu-id="cc488-187">When creating the authentication ticket, the forms authentication system determines its expiry by consulting the timeout setting.</span></span> <span data-ttu-id="cc488-188">表 1 中，将默认值设置为 30 分钟的超时值中所述这意味着，在创建窗体身份验证票证时其到期设置为日期和时间在将来 30 分钟。</span><span class="sxs-lookup"><span data-stu-id="cc488-188">As noted in Table 1, the timeout setting defaults to 30 minutes, meaning that when the forms authentication ticket is created its expiry is set to a date and time 30 minutes in the future.</span></span>

<span data-ttu-id="cc488-189">到期时间定义一个绝对时间在将来的窗体身份验证票证过期时。</span><span class="sxs-lookup"><span data-stu-id="cc488-189">The expiry defines an absolute time in the future when the forms authentication ticket expires.</span></span> <span data-ttu-id="cc488-190">但开发人员通常要实现可调到期，一个每次用户将回顾站点重置。</span><span class="sxs-lookup"><span data-stu-id="cc488-190">But usually developers want to implement a sliding expiry, one that is reset every time the user revisits the site.</span></span> <span data-ttu-id="cc488-191">此行为由 slidingExpiration 设置确定。</span><span class="sxs-lookup"><span data-stu-id="cc488-191">This behavior is determined by the slidingExpiration settings.</span></span> <span data-ttu-id="cc488-192">如果设置为 true （默认值），每次 FormsAuthenticationModule 将用户进行身份验证的时，它将更新票证的到期。</span><span class="sxs-lookup"><span data-stu-id="cc488-192">If set to true (the default), each time the FormsAuthenticationModule authenticates a user, it updates the ticket's expiry.</span></span> <span data-ttu-id="cc488-193">如果设置为 false，过期日期未更新对每个请求，从而导致票证过期完全超时分钟数票证最初创建。</span><span class="sxs-lookup"><span data-stu-id="cc488-193">If set to false, the expiry is not updated on each request, thereby causing the ticket to expire exactly timeout number of minutes past when the ticket was first created.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-194">存储在身份验证票证的到期时间是绝对日期和时间值，如 2008 年 8 月 2 日上午 11:34。</span><span class="sxs-lookup"><span data-stu-id="cc488-194">The expiry stored in the authentication ticket is an absolute date and time value, like August 2, 2008 11:34 AM.</span></span> <span data-ttu-id="cc488-195">此外，日期和时间都是相对于 web 服务器的本地时间。</span><span class="sxs-lookup"><span data-stu-id="cc488-195">Moreover, the date and time are relative to the web server's local time.</span></span> <span data-ttu-id="cc488-196">这种设计决策可能会有一些有趣的副作用围绕夏时制 (DST)，这是在美国的时钟移动提前一小时 （假设 web 服务器所在的位置夏时制观察到的区域设置中） 时。</span><span class="sxs-lookup"><span data-stu-id="cc488-196">This design decision can have some interesting side effects around Daylight Saving Time (DST), which is when clocks in the United States are moved ahead one hour (assuming the web server is hosted in a locale where Daylight Saving Time is observed).</span></span> <span data-ttu-id="cc488-197">请考虑会发生什么情况为 ASP.NET 网站使用 DST 开始时间 30 分钟后到期 （这是凌晨 2:00）。</span><span class="sxs-lookup"><span data-stu-id="cc488-197">Consider what would happen for an ASP.NET website with a 30 minute expiry near the time that DST begins (which is at 2:00 AM).</span></span> <span data-ttu-id="cc488-198">假设访问者登录到站点在 2008 年 3 月 11 日凌晨 1:55。</span><span class="sxs-lookup"><span data-stu-id="cc488-198">Imagine a visitor signs on to the site on March 11, 2008 at 1:55 AM.</span></span> <span data-ttu-id="cc488-199">这会生成将在到期日期为 2008 年 3 月 11 日凌晨 2:25 （将来 30 分钟） 的窗体身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-199">This would generate a forms authentication ticket that expires at March 11, 2008 at 2:25 AM (30 minutes in the future).</span></span> <span data-ttu-id="cc488-200">但是，一旦围绕回滚凌晨 2:00，时钟跳转到凌晨 3:00 由于 DST。</span><span class="sxs-lookup"><span data-stu-id="cc488-200">However, once 2:00 AM rolls around, the clock jumps to 3:00 AM because of DST.</span></span> <span data-ttu-id="cc488-201">当用户加载新页面 （在上午 3:01） 在登录后的六分钟时，FormsAuthenticationModule 将说明在票证已过期，将用户重定向到登录页。</span><span class="sxs-lookup"><span data-stu-id="cc488-201">When the user loads a new page six minutes after signing in (at 3:01 AM), the FormsAuthenticationModule notes that the ticket has expired and redirects the user to the login page.</span></span> <span data-ttu-id="cc488-202">这和其他身份验证票证超时问题，以及解决方法有关的更全面讨论，选取一份 Stefan Schackow *Professional ASP.NET 2.0 安全、 成员资格和角色管理*(ISBN:978-0-7645-9698-8)。</span><span class="sxs-lookup"><span data-stu-id="cc488-202">For a more thorough discussion on this and other authentication ticket timeout oddities, as well as workarounds, pick up a copy of Stefan Schackow's *Professional ASP.NET 2.0 Security, Membership, and Role Management* (ISBN: 978-0-7645-9698-8).</span></span>


<span data-ttu-id="cc488-203">图 1 说明了在工作流时 slidingExpiration 设置为 false，超时设置为 30。</span><span class="sxs-lookup"><span data-stu-id="cc488-203">Figure 1 illustrates the workflow when slidingExpiration is set to false and timeout is set to 30.</span></span> <span data-ttu-id="cc488-204">请注意，在登录时生成的身份验证票证包含到期日期，并在后续请求中未更新此值。</span><span class="sxs-lookup"><span data-stu-id="cc488-204">Note that the authentication ticket generated at login contains the expiration date, and this value is not updated on subsequent requests.</span></span> <span data-ttu-id="cc488-205">如果 FormsAuthenticationModule 找到此票证已到期，它将其丢弃，并将请求视为匿名。</span><span class="sxs-lookup"><span data-stu-id="cc488-205">If the FormsAuthenticationModule finds that the ticket has expired, it discards it and treats the request as anonymous.</span></span>


<span data-ttu-id="cc488-206">[![以图形形式的窗体身份验证票证到期时 slidingExpiration 为 false](forms-authentication-configuration-and-advanced-topics-vb/_static/image2.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="cc488-206">[![A Graphical Representation of the Forms Authentication Ticket's Expiry When slidingExpiration is false](forms-authentication-configuration-and-advanced-topics-vb/_static/image2.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image1.png)</span></span>

<span data-ttu-id="cc488-207">**图 01**: 的窗体身份验证票证到期时 slidingExpiration 的图形表示形式为 false ([单击以查看实际尺寸的图像](forms-authentication-configuration-and-advanced-topics-vb/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="cc488-207">**Figure 01**: A Graphical Representation of the Forms Authentication Ticket's Expiry When slidingExpiration is false ([Click to view full-size image](forms-authentication-configuration-and-advanced-topics-vb/_static/image3.png))</span></span>


<span data-ttu-id="cc488-208">图 2 显示了工作流时 slidingExpiration 设置为 true，并且超时设置为 30。</span><span class="sxs-lookup"><span data-stu-id="cc488-208">Figure 2 shows the workflow when slidingExpiration is set to true and timeout is set to 30.</span></span> <span data-ttu-id="cc488-209">时 （使用非过期票证） 收到的已经过身份验证的请求其到期将更新为在将来分钟的超时数。</span><span class="sxs-lookup"><span data-stu-id="cc488-209">When an authenticated request is received (with a non-expired ticket) its expiry is updated to timeout number of minutes in the future.</span></span>


<span data-ttu-id="cc488-210">[![以图形形式的窗体身份验证票证 slidingExpiration 条件为 true 时](forms-authentication-configuration-and-advanced-topics-vb/_static/image5.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="cc488-210">[![A Graphical Representation of the Forms Authentication Ticket's When slidingExpiration is true](forms-authentication-configuration-and-advanced-topics-vb/_static/image5.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image4.png)</span></span>

<span data-ttu-id="cc488-211">**图 02**： 窗体身份验证票证的图形表示形式 slidingExpiration 条件为 true 时 ([单击以查看实际尺寸的图像](forms-authentication-configuration-and-advanced-topics-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="cc488-211">**Figure 02**: A Graphical Representation of the Forms Authentication Ticket's When slidingExpiration is true ([Click to view full-size image](forms-authentication-configuration-and-advanced-topics-vb/_static/image6.png))</span></span>


<span data-ttu-id="cc488-212">在使用基于 cookie 的身份验证票证 （默认值），这一讨论将变得有点令人困惑，因为 cookie 还可以指定自己满。</span><span class="sxs-lookup"><span data-stu-id="cc488-212">When using cookie-based authentication tickets (the default), this discussion becomes a little more confusing because cookies can also have their own expiries specified.</span></span> <span data-ttu-id="cc488-213">Cookie 的到期 （或缺少局域） 会指示浏览器 cookie 应销毁时。</span><span class="sxs-lookup"><span data-stu-id="cc488-213">A cookie's expiry (or lack thereof) instructs the browser when the cookie should be destroyed.</span></span> <span data-ttu-id="cc488-214">如果 cookie 缺少到期时间，销毁浏览器关闭时。</span><span class="sxs-lookup"><span data-stu-id="cc488-214">If the cookie lacks an expiry, it is destroyed when the browser shuts down.</span></span> <span data-ttu-id="cc488-215">如果过期时间存在，但是，cookie 的日期之前会一直存储在用户计算机上，指定在到期时间已通过。</span><span class="sxs-lookup"><span data-stu-id="cc488-215">If an expiry is present, however, the cookie remains stored on the user's computer until the date and time specified in the expiry has passed.</span></span> <span data-ttu-id="cc488-216">Cookie 时销毁由浏览器，它不能再发送到 web 服务器。</span><span class="sxs-lookup"><span data-stu-id="cc488-216">When a cookie is destroyed by the browser, it is no longer sent to the web server.</span></span> <span data-ttu-id="cc488-217">因此，进行销毁是 cookie 的类似于用户注销站点。</span><span class="sxs-lookup"><span data-stu-id="cc488-217">Therefore, the destruction of a cookie is analogous to the user logging out of the site.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-218">当然，用户可以主动删除存储在其计算机上的任何 cookie。</span><span class="sxs-lookup"><span data-stu-id="cc488-218">Of course, a user may proactively remove any cookies stored on their computer.</span></span> <span data-ttu-id="cc488-219">在 Internet Explorer 7 中，将转到工具，选项，并单击浏览历史记录部分中的删除按钮。</span><span class="sxs-lookup"><span data-stu-id="cc488-219">In Internet Explorer 7, you would go to Tools, Options, and click the Delete button in the Browsing history section.</span></span> <span data-ttu-id="cc488-220">在这里，单击删除 cookie 按钮。</span><span class="sxs-lookup"><span data-stu-id="cc488-220">From there, click the Delete cookies button.</span></span>


<span data-ttu-id="cc488-221">窗体身份验证系统将创建基于会话的或基于过期的 cookie，具体取决于传递给的值*persistCookie*参数。</span><span class="sxs-lookup"><span data-stu-id="cc488-221">The forms authentication system creates session-based or expiry-based cookies depending on the value passed in to the *persistCookie* parameter.</span></span> <span data-ttu-id="cc488-222">回想一下，FormsAuthentication 类 GetAuthCookie、 SetAuthCookie 和 RedirectFromLoginPage 方法采用两个输入参数：*用户名*并*persistCookie*。</span><span class="sxs-lookup"><span data-stu-id="cc488-222">Recall that the FormsAuthentication class's GetAuthCookie, SetAuthCookie, and RedirectFromLoginPage methods take in two input parameters: *username* and *persistCookie*.</span></span> <span data-ttu-id="cc488-223">我们在前面的教程中创建的登录页包含一个记住我的复选框，来确定是否已创建持久 cookie。</span><span class="sxs-lookup"><span data-stu-id="cc488-223">The login page we created in the preceding tutorial included a Remember me CheckBox, which determined whether a persistent cookie was created.</span></span> <span data-ttu-id="cc488-224">永久 cookie 是基于到期的;非持久性 cookie 是基于会话的。</span><span class="sxs-lookup"><span data-stu-id="cc488-224">Persistent cookies are expiry-based; non-persistent cookies are session-based.</span></span>

<span data-ttu-id="cc488-225">已讨论过的超时和 slidingExpiration 概念不适用于相同这两种基于会话和过期的 cookie。</span><span class="sxs-lookup"><span data-stu-id="cc488-225">The timeout and slidingExpiration concepts already discussed apply the same to both session- and expiry-based cookies.</span></span> <span data-ttu-id="cc488-226">没有在执行过程中只有一个细微的差别： 当使用基于过期的 cookie slidingTimeout 设置为 true 时，cookie 的到期只会更新当超过一半的指定时间已过。</span><span class="sxs-lookup"><span data-stu-id="cc488-226">There is only one minor difference in execution: when using expiry-based cookies with slidingTimeout set to true, the cookie's expiry is only updated when more than half of the specified time has elapsed.</span></span>

<span data-ttu-id="cc488-227">让我们更新我们网站的身份验证票证超时策略，使票证超时 （60 分钟），一小时后的使用可调到期。</span><span class="sxs-lookup"><span data-stu-id="cc488-227">Let's update our website's authentication ticket timeout policies so that tickets timeout after one hour (60 minutes), using a sliding expiration.</span></span> <span data-ttu-id="cc488-228">若要此更改生效，更新 Web.config 文件中，添加&lt;窗体&gt;元素&lt;身份验证&gt;元素使用以下标记：</span><span class="sxs-lookup"><span data-stu-id="cc488-228">To effect this change, update the Web.config file, adding a &lt;forms&gt; element to the &lt;authentication&gt; element with the following markup:</span></span>

[!code-xml[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample2.xml)]

### <a name="using-an-login-page-url-other-than-loginaspx"></a><span data-ttu-id="cc488-229">使用 login.aspx 的情况以外的登录页 URL</span><span class="sxs-lookup"><span data-stu-id="cc488-229">Using an Login Page URL Other than Login.aspx</span></span>

<span data-ttu-id="cc488-230">因为 FormsAuthenticationModule 自动重定向到登录页的未经授权的用户，必须知道登录页面的 URL。</span><span class="sxs-lookup"><span data-stu-id="cc488-230">Since the FormsAuthenticationModule automatically redirects unauthorized users to the login page, it needs to know the login page's URL.</span></span> <span data-ttu-id="cc488-231">中的 loginUrl 属性指定此 URL&lt;窗体&gt;元素，默认值为 login.aspx 的情况。</span><span class="sxs-lookup"><span data-stu-id="cc488-231">This URL is specified by the loginUrl attribute in the &lt;forms&gt; element and defaults to login.aspx.</span></span> <span data-ttu-id="cc488-232">如果要将迁移对现有网站，你可能已使用不同的 URL，其中一个已设置为书签并被搜索引擎索引登录页。</span><span class="sxs-lookup"><span data-stu-id="cc488-232">If you are porting over an existing website, you may already have a login page with a different URL, one that has already been bookmarked and indexed by search engines.</span></span> <span data-ttu-id="cc488-233">而不是正在对现有的登录页重命名为 login.aspx 的情况和重大链接和用户的书签，而是可以修改 loginUrl 属性以指向您的登录页。</span><span class="sxs-lookup"><span data-stu-id="cc488-233">Rather than renaming your existing login page to login.aspx and breaking links and users' bookmarks, you can instead modify the loginUrl attribute to point to your login page.</span></span>

<span data-ttu-id="cc488-234">例如，如果您的登录页名为 SignIn.aspx 并且位于用户目录中，您可以指向 ~/Users/SignIn.aspx loginUrl 配置设置如下所示：</span><span class="sxs-lookup"><span data-stu-id="cc488-234">For example, if your login page was named SignIn.aspx and was located in the Users directory, you could point the loginUrl configuration setting to ~/Users/SignIn.aspx like so:</span></span>

[!code-xml[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample3.xml)]

<span data-ttu-id="cc488-235">由于我们的当前应用程序已具有一个名为 login.aspx 的情况的登录页，则无需指定自定义值中的&lt;窗体&gt;元素。</span><span class="sxs-lookup"><span data-stu-id="cc488-235">Since our current application already has a login page named Login.aspx, there's no need to specify a custom value in the &lt;forms&gt; element.</span></span>

## <a name="step-2-using-cookieless-forms-authentication-tickets"></a><span data-ttu-id="cc488-236">步骤 2： 使用无 Cookie Forms 身份验证票证</span><span class="sxs-lookup"><span data-stu-id="cc488-236">Step 2: Using Cookieless Forms Authentication Tickets</span></span>

<span data-ttu-id="cc488-237">默认情况下窗体身份验证系统确定是否将其身份验证票证存储在 cookie 集合或将它们嵌入中基于用户代理访问网站的 URL。</span><span class="sxs-lookup"><span data-stu-id="cc488-237">By default the forms authentication system determines whether to store its authentication tickets in the cookies collection or embed them in the URL based on the user agent visiting the site.</span></span> <span data-ttu-id="cc488-238">所有主流桌面浏览器等 Internet Explorer、 Firefox、 Opera 和 Safari 支持 cookie，但并非所有移动设备执行操作。</span><span class="sxs-lookup"><span data-stu-id="cc488-238">All mainstream desktop browsers like Internet Explorer, Firefox, Opera, and Safari, support cookies, but not all mobile devices do.</span></span>

<span data-ttu-id="cc488-239">使用窗体身份验证系统的 cookie 策略取决于中的无 cookie 设置&lt;窗体&gt;元素，它可以分配四个值之一：</span><span class="sxs-lookup"><span data-stu-id="cc488-239">The cookie policy used by the forms authentication system depends on the cookieless setting in the &lt;forms&gt; element, which can be assigned one of four values:</span></span>

- <span data-ttu-id="cc488-240">UseCookies-指定将始终使用基于 cookie 的身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-240">UseCookies - specifies that cookie-based authentication tickets will always be used.</span></span>
- <span data-ttu-id="cc488-241">UseUri-指示将永远不使用基于 cookie 的身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-241">UseUri - indicates that cookie-based authentication tickets will never be used.</span></span>
- <span data-ttu-id="cc488-242">不使用自动检测功能-如果设备配置文件不支持 cookie，基于 cookie 的身份验证票证;如果设备配置文件支持 cookie，探测机制用于确定是否已启用 cookie。</span><span class="sxs-lookup"><span data-stu-id="cc488-242">AutoDetect - if the device profile does not support cookies, cookie-based authentication tickets are not used; if the device profile supports cookies, a probing mechanism is used to determine if cookies are enabled.</span></span>
- <span data-ttu-id="cc488-243">UseDeviceProfile-默认设置;仅当设备配置文件支持 cookie，请使用基于 cookie 的身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-243">UseDeviceProfile - the default; uses cookie-based authentication tickets only if the device profile supports cookies.</span></span> <span data-ttu-id="cc488-244">不使用任何探测机制。</span><span class="sxs-lookup"><span data-stu-id="cc488-244">No probing mechanism is used.</span></span>

<span data-ttu-id="cc488-245">自动检测和 UseDeviceProfile 设置依赖*设备配置文件*中认定是否使用基于 cookie 的或无 cookie 身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-245">The AutoDetect and UseDeviceProfile settings rely on a *device profile* in ascertaining whether to use cookie-based or cookieless authentication tickets.</span></span> <span data-ttu-id="cc488-246">ASP.NET 维护的各种设备和其功能，例如是否支持 cookie，它们支持的 JavaScript 和等等的哪些版本的数据库。</span><span class="sxs-lookup"><span data-stu-id="cc488-246">ASP.NET maintains a database of various devices and their capabilities, such as whether they support cookies, what version of JavaScript they support, and so on.</span></span> <span data-ttu-id="cc488-247">每次设备请求 web 页从 web 服务器发送沿*用户代理*标识设备类型的 HTTP 标头。</span><span class="sxs-lookup"><span data-stu-id="cc488-247">Each time a device requests a web page from a web server it sends along a *user-agent* HTTP header that identifies the device type.</span></span> <span data-ttu-id="cc488-248">ASP.NET 会自动与指定在其数据库中的相应配置文件匹配提供的用户代理字符串。</span><span class="sxs-lookup"><span data-stu-id="cc488-248">ASP.NET automatically matches the supplied user-agent string with the corresponding profile specified in its database.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-249">设备功能的此数据库存储在多个 XML 文件符合[浏览器定义文件架构](https://msdn.microsoft.com/library/ms228122.aspx)。</span><span class="sxs-lookup"><span data-stu-id="cc488-249">This database of device capabilities is stored in a number of XML files that adhere to the [Browser Definition File schema](https://msdn.microsoft.com/library/ms228122.aspx).</span></span> <span data-ttu-id="cc488-250">默认设备配置文件位于 %windir%\microsoft.net\framework\v2.0.50727\config\browsers。</span><span class="sxs-lookup"><span data-stu-id="cc488-250">The default device profile files are located in %WINDIR%\Microsoft.Net\Framework\v2.0.50727\CONFIG\Browsers.</span></span> <span data-ttu-id="cc488-251">此外可以将自定义文件添加到应用程序的应用\_浏览器文件夹。</span><span class="sxs-lookup"><span data-stu-id="cc488-251">You can also add custom files to your application's App\_Browsers folder.</span></span> <span data-ttu-id="cc488-252">有关详细信息，请参阅[如何： 检测浏览器类型在 ASP.NET Web Pages](https://msdn.microsoft.com/library/3yekbd5b.aspx)。</span><span class="sxs-lookup"><span data-stu-id="cc488-252">For more information, see [How To: Detect Browser Types in ASP.NET Web Pages](https://msdn.microsoft.com/library/3yekbd5b.aspx).</span></span>


<span data-ttu-id="cc488-253">因为默认设置为 UseDeviceProfile，将通过其配置文件报告它不支持 cookie 的设备访问该站点时使用无 cookie forms 身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-253">Because the default setting is UseDeviceProfile, cookieless forms authentication tickets will be used when the site is visited by a device whose profile reports that it does not support cookies.</span></span>

### <a name="encoding-the-authentication-ticket-in-the-url"></a><span data-ttu-id="cc488-254">编码的 URL 中的身份验证票证</span><span class="sxs-lookup"><span data-stu-id="cc488-254">Encoding the Authentication Ticket in the URL</span></span>

<span data-ttu-id="cc488-255">Cookie 是一个自然媒体是默认窗体身份验证设置使用 cookie，如果正在访问的设备支持它们的原因将特定网站的每个请求中包含从浏览器的信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-255">Cookies are a natural medium for including information from the browser in each request to a particular website, which is why the default forms authentication settings use cookies if the visiting device supports them.</span></span> <span data-ttu-id="cc488-256">如果不支持 cookie，必须使用用于将从客户端身份验证票证传递到服务器的替代方法。</span><span class="sxs-lookup"><span data-stu-id="cc488-256">If cookies are not supported, an alternate means for passing the authentication ticket from the client to the server must be employed.</span></span> <span data-ttu-id="cc488-257">用于无 cookie 的环境中常见的解决方法是在 URL 中的 cookie 数据进行编码。</span><span class="sxs-lookup"><span data-stu-id="cc488-257">A common workaround used in cookieless environments is to encode the cookie data in the URL.</span></span>

<span data-ttu-id="cc488-258">请参阅如何在 URL 中嵌入此类信息的最佳方式是强制站点以使用无 cookie 身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-258">The best way to see how such information can be embedded within the URL is to force the site to use cookieless authentication tickets.</span></span> <span data-ttu-id="cc488-259">这可以通过将无 cookie 的配置设置设置为 UseUri 来实现：</span><span class="sxs-lookup"><span data-stu-id="cc488-259">This can be accomplished by setting the cookieless configuration setting to UseUri:</span></span>

[!code-xml[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample4.xml)]

<span data-ttu-id="cc488-260">您一次进行此更改，请访问通过浏览器站点。</span><span class="sxs-lookup"><span data-stu-id="cc488-260">Once you have made this change, visit the site through a browser.</span></span> <span data-ttu-id="cc488-261">如果以匿名用户访问，Url 会查找完全像以前一样。</span><span class="sxs-lookup"><span data-stu-id="cc488-261">When visiting as an anonymous user, the URLs will look exactly like they did before.</span></span> <span data-ttu-id="cc488-262">例如，访问 Default.aspx 页时我的浏览器地址栏显示了以下 URL:</span><span class="sxs-lookup"><span data-stu-id="cc488-262">For example, when visiting Default.aspx page my browser's address bar shows the following URL:</span></span>

`http://localhost:2448/ASPNET\_Security\_Tutorial\_03\_CS/default.aspx`

<span data-ttu-id="cc488-263">但是，在记录，窗体身份验证票证被嵌入到 URL。</span><span class="sxs-lookup"><span data-stu-id="cc488-263">However, upon logging in, the forms authentication ticket is embedded into the URL.</span></span> <span data-ttu-id="cc488-264">例如，访问登录页和 Sam 的身份登录之后, 我就会返回到 Default.aspx 页但 URL 这一次是：</span><span class="sxs-lookup"><span data-stu-id="cc488-264">For example, after visiting the login page and logging in as Sam, I am returned to the Default.aspx page, but the URL this time is:</span></span>

`http://localhost:2448/ASPNET\_Security\_Tutorial\_03\_CS/(F(jaIOIDTJxIr12xYS-VVgkqKCVAuIoW30Bu0diWi6flQC-FyMaLXJfow\_Vd9GZkB2Cv-rfezq0gKadKX0YPZCkA2))/default.aspx`

<span data-ttu-id="cc488-265">窗体身份验证票证已嵌入到 URL 中。</span><span class="sxs-lookup"><span data-stu-id="cc488-265">The forms authentication ticket has been embedded within the URL.</span></span> <span data-ttu-id="cc488-266">字符串 (F (jaIOIDTJxIr12xYS VVgkqKCVAuIoW30Bu0diWi6flQC FyMaLXJfow\_Vd9GZkB2Cv rfezq0gKadKX0YPZCkA2) 表示十六进制编码的身份验证票证信息，并且是相同的数据通常存储在一个 cookie。</span><span class="sxs-lookup"><span data-stu-id="cc488-266">The string (F(jaIOIDTJxIr12xYS-VVgkqKCVAuIoW30Bu0diWi6flQC-FyMaLXJfow\_Vd9GZkB2Cv-rfezq0gKadKX0YPZCkA2) represents the hex-encoded authentication ticket information, and is the same data that is usually stored within a cookie.</span></span>

<span data-ttu-id="cc488-267">为了使无 cookie 身份验证票证，若要运行，系统必须对所有 Url 的页上，以包含身份验证票证数据进行都编码，否则身份验证票证时将会丢失用户单击的链接。</span><span class="sxs-lookup"><span data-stu-id="cc488-267">In order for cookieless authentication tickets to work, the system must encode all URLs on the page to include the authentication ticket data, otherwise the authentication ticket will be lost when the user clicks on a link.</span></span> <span data-ttu-id="cc488-268">幸运的是，自动执行此嵌入的逻辑。</span><span class="sxs-lookup"><span data-stu-id="cc488-268">Thankfully, this embedding logic is performed automatically.</span></span> <span data-ttu-id="cc488-269">若要演示此功能，打开 Default.aspx 页，并添加超链接控件，分别将其文本和 NavigateUrl 属性设置为测试链接和 SomePage.aspx。</span><span class="sxs-lookup"><span data-stu-id="cc488-269">To demonstrate this functionality, open the Default.aspx page and add a HyperLink control, setting its Text and NavigateUrl properties to Test Link and SomePage.aspx, respectively.</span></span> <span data-ttu-id="cc488-270">它并不重要，真的没有一个页面中名为 SomePage.aspx 我们的项目。</span><span class="sxs-lookup"><span data-stu-id="cc488-270">It doesn't matter that there really isn't a page in our project named SomePage.aspx.</span></span>

<span data-ttu-id="cc488-271">将所做的更改保存到 Default.aspx，然后通过浏览器访问它。</span><span class="sxs-lookup"><span data-stu-id="cc488-271">Save the changes to Default.aspx and then visit it through a browser.</span></span> <span data-ttu-id="cc488-272">在登录到站点，以便在 URL 中嵌入窗体身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-272">Log on to the site so that the forms authentication ticket is embedded in the URL.</span></span> <span data-ttu-id="cc488-273">接下来，从 Default.aspx，单击测试链接链接。</span><span class="sxs-lookup"><span data-stu-id="cc488-273">Next, from Default.aspx, click the Test Link link.</span></span> <span data-ttu-id="cc488-274">这是怎么回事？</span><span class="sxs-lookup"><span data-stu-id="cc488-274">What happened?</span></span> <span data-ttu-id="cc488-275">如果不存在名为 SomePage.aspx 任何页，则表示出现 404 错误，但这是不值得在此处。</span><span class="sxs-lookup"><span data-stu-id="cc488-275">If no page named SomePage.aspx exists, then a 404 error occurred, but that's not what's important here.</span></span> <span data-ttu-id="cc488-276">相反，重点介绍在您的浏览器的地址栏。</span><span class="sxs-lookup"><span data-stu-id="cc488-276">Instead, focus on the Address bar in your browser.</span></span> <span data-ttu-id="cc488-277">请注意，它包含窗体身份验证票证在 URL 中 ！</span><span class="sxs-lookup"><span data-stu-id="cc488-277">Note that it includes the forms authentication ticket in the URL!</span></span>

`http://localhost:2448/ASPNET\_Security\_Tutorial\_03\_CS/(F(jaIOIDTJxIr12xYS-VVgkqKCVAuIoW30Bu0diWi6flQC-FyMaLXJfow\_Vd9GZkB2Cv-rfezq0gKadKX0YPZCkA2))/SomePage.aspx`

<span data-ttu-id="cc488-278">链接中 URL SomePage.aspx 已自动转换为一个 URL，包括身份验证票证-我们不需要编写一点代码 ！</span><span class="sxs-lookup"><span data-stu-id="cc488-278">The URL SomePage.aspx in the link was automatically converted into a URL that included the authentication ticket - we didn't have to write a lick of code!</span></span> <span data-ttu-id="cc488-279">窗体身份验证票证将自动嵌入在 URL 中的任何不是以 http:// 开头的超链接或 /。</span><span class="sxs-lookup"><span data-stu-id="cc488-279">The form authentication ticket will automatically be embedded in the URL for any hyperlinks that do not start with http:// or /.</span></span> <span data-ttu-id="cc488-280">如果在调用 Response.Redirect、 超链接控件，或定位 HTML 元素中出现的超链接并不重要 (即&lt;href ="..."&gt;...&lt;/a&gt;)。</span><span class="sxs-lookup"><span data-stu-id="cc488-280">It doesn't matter if the hyperlink appears in a call to Response.Redirect, in a HyperLink control, or in an anchor HTML element (i.e., &lt;a href="..."&gt;...&lt;/a&gt;).</span></span> <span data-ttu-id="cc488-281">只要 URL 不是类似于 http://www.someserver.com/SomePage.aspx 或 /SomePage.aspx，身份验证票证将嵌入为我们的窗体。</span><span class="sxs-lookup"><span data-stu-id="cc488-281">As long as the URL isn't something like http://www.someserver.com/SomePage.aspx or /SomePage.aspx, the forms authentication ticket will be embedded for us.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-282">无 cookie forms 身份验证票证遵循相同的超时策略作为基于 cookie 的身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-282">Cookieless forms authentication tickets adhere to the same timeout policies as cookie-based authentication tickets.</span></span> <span data-ttu-id="cc488-283">但是，无 cookie 身份验证票证的更容易出现重播攻击，因为在 URL 中直接嵌入身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-283">However, cookieless authentication tickets are more prone to replay attacks since the authentication ticket is embedded directly in the URL.</span></span> <span data-ttu-id="cc488-284">假设访问网站并记录在中，然后再将 URL 粘贴到同事的电子邮件中的用户。</span><span class="sxs-lookup"><span data-stu-id="cc488-284">Imagine a user who visits a website, logs in, and then pastes the URL in an email to a colleague.</span></span> <span data-ttu-id="cc488-285">如果该同事达到过期日期之前单击该链接，它们将作为其发送电子邮件的用户记录 ！</span><span class="sxs-lookup"><span data-stu-id="cc488-285">If the colleague clicks on that link before the expiry is reached, they will be logged in as the user who sent the email!</span></span>


## <a name="step-3-securing-the-authentication-ticket"></a><span data-ttu-id="cc488-286">步骤 3： 保护身份验证票证</span><span class="sxs-lookup"><span data-stu-id="cc488-286">Step 3: Securing the Authentication Ticket</span></span>

<span data-ttu-id="cc488-287">通过网络传输的窗体身份验证票证 cookie 中或直接在 URL 中嵌入。</span><span class="sxs-lookup"><span data-stu-id="cc488-287">The forms authentication ticket is transmitted over the wire either in a cookie or embedded directly within the URL.</span></span> <span data-ttu-id="cc488-288">标识信息的补充身份验证票证还可以包含用户数据，（正如我们将在步骤 4 中看到的）。</span><span class="sxs-lookup"><span data-stu-id="cc488-288">In addition to identity information, the authentication ticket can also include user data (as we will see in Step 4).</span></span> <span data-ttu-id="cc488-289">因此，很重要的已加密的票证数据防止被 （更重要的是） 的窗体身份验证系统可以保证票证被未篡改。</span><span class="sxs-lookup"><span data-stu-id="cc488-289">Consequently, it is important that the ticket's data is encrypted from prying eyes and (even more importantly) that the forms authentication system can guarantee that the ticket was not tampered with.</span></span>

<span data-ttu-id="cc488-290">若要确保票证的数据的隐私，窗体身份验证系统可以加密票证数据。</span><span class="sxs-lookup"><span data-stu-id="cc488-290">To ensure the privacy of the ticket's data, the forms authentication system can encrypt the ticket data.</span></span> <span data-ttu-id="cc488-291">无法加密票证数据通过网络传输纯文本发送可能敏感的信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-291">Failure to encrypt the ticket data sends potentially sensitive information over the wire in plain-text.</span></span>

<span data-ttu-id="cc488-292">若要确保票证的真实性，窗体身份验证系统必须*验证*票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-292">To guarantee a ticket's authenticity, the forms authentication system must *validate* the ticket.</span></span> <span data-ttu-id="cc488-293">验证是确保数据的特定部分未进行修改，并通过完成的 act *[消息身份验证代码 (MAC)](http://en.wikipedia.org/wiki/Message_authentication_code)*。</span><span class="sxs-lookup"><span data-stu-id="cc488-293">Validation is the act of ensuring that a particular piece of data has not been modified, and is accomplished via a *[message authentication code (MAC)](http://en.wikipedia.org/wiki/Message_authentication_code)*.</span></span> <span data-ttu-id="cc488-294">简单地说，MAC 是信息的标识需要验证 （在此情况下，该票证） 的数据的一小部分。</span><span class="sxs-lookup"><span data-stu-id="cc488-294">In a nutshell, the MAC is a small piece of information that identifies the data that needs to be validated (in this case, the ticket).</span></span> <span data-ttu-id="cc488-295">如果修改了 MAC 所表示的数据，然后在 MAC 和数据将不匹配。</span><span class="sxs-lookup"><span data-stu-id="cc488-295">If the data represented by the MAC is modified, then the MAC and the data will not match up.</span></span> <span data-ttu-id="cc488-296">此外，它是计算硬黑客同时修改数据并生成自己的 MAC 以与已修改的数据一致。</span><span class="sxs-lookup"><span data-stu-id="cc488-296">Moreover, it is computationally hard for a hacker to both modify the data and generate his own MAC to correspond with the modified data.</span></span>

<span data-ttu-id="cc488-297">创建 （或修改） 时的票证，窗体身份验证系统创建 MAC，并将其附加到的票证数据。</span><span class="sxs-lookup"><span data-stu-id="cc488-297">When creating (or modifying) a ticket, the forms authentication system creates a MAC and attaches it to the ticket's data.</span></span> <span data-ttu-id="cc488-298">当后续请求到达时，窗体身份验证系统进行比较来验证票证数据的真实性的 MAC 和票证数据。</span><span class="sxs-lookup"><span data-stu-id="cc488-298">When a subsequent request arrives, the forms authentication system compares the MAC and ticket data to validate the authenticity of the ticket data.</span></span> <span data-ttu-id="cc488-299">图 3 以图形方式显示此工作流。</span><span class="sxs-lookup"><span data-stu-id="cc488-299">Figure 3 illustrates this workflow graphically.</span></span>


<span data-ttu-id="cc488-300">[![票证的真实性确保通过 MAC](forms-authentication-configuration-and-advanced-topics-vb/_static/image8.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image7.png)</span><span class="sxs-lookup"><span data-stu-id="cc488-300">[![The Ticket's Authenticity is Ensured through a MAC](forms-authentication-configuration-and-advanced-topics-vb/_static/image8.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image7.png)</span></span>

<span data-ttu-id="cc488-301">**图 03**： 通过 MAC 确保票证的真实性 ([单击以查看实际尺寸的图像](forms-authentication-configuration-and-advanced-topics-vb/_static/image9.png))</span><span class="sxs-lookup"><span data-stu-id="cc488-301">**Figure 03**: The Ticket's Authenticity is Ensured through a MAC ([Click to view full-size image](forms-authentication-configuration-and-advanced-topics-vb/_static/image9.png))</span></span>


<span data-ttu-id="cc488-302">什么是安全度量值应用于身份验证票证中的保护设置取决于&lt;窗体&gt;元素。</span><span class="sxs-lookup"><span data-stu-id="cc488-302">What security measures are applied to the authentication ticket depends on the protection setting in the &lt;forms&gt; element.</span></span> <span data-ttu-id="cc488-303">保护设置可能会分配给以下三个值之一：</span><span class="sxs-lookup"><span data-stu-id="cc488-303">The protection setting may be assigned to one of the following three values:</span></span>

- <span data-ttu-id="cc488-304">所有的票证已加密，并进行数字签名 （默认值）。</span><span class="sxs-lookup"><span data-stu-id="cc488-304">All - the ticket is both encrypted and digitally signed (the default).</span></span>
- <span data-ttu-id="cc488-305">加密-仅加密已应用-无 MAC 生成。</span><span class="sxs-lookup"><span data-stu-id="cc488-305">Encryption - only encryption is applied - no MAC is generated.</span></span>
- <span data-ttu-id="cc488-306">无-票证是加密和数字签名。</span><span class="sxs-lookup"><span data-stu-id="cc488-306">None - the ticket is neither encrypted nor digitally signed.</span></span>
- <span data-ttu-id="cc488-307">生成验证-MAC，但通过网络传输纯文本发送票证数据。</span><span class="sxs-lookup"><span data-stu-id="cc488-307">Validation - a MAC is generated, but the ticket data is sent over the wire in plain-text.</span></span>

<span data-ttu-id="cc488-308">Microsoft 强烈建议使用的所有设置。</span><span class="sxs-lookup"><span data-stu-id="cc488-308">Microsoft strongly recommends using the All setting.</span></span>

### <a name="setting-the-validation-and-decryption-keys"></a><span data-ttu-id="cc488-309">设置验证和解密密钥</span><span class="sxs-lookup"><span data-stu-id="cc488-309">Setting the Validation and Decryption Keys</span></span>

<span data-ttu-id="cc488-310">加密和哈希算法由窗体身份验证系统中用于加密和验证身份验证票证是通过可自定义[ &lt;machineKey&gt;元素](https://msdn.microsoft.com/library/w8h3skw9.aspx)在 Web.config 中。表 2 轮廓&lt;machineKey&gt;元素的特性和其可能的值。</span><span class="sxs-lookup"><span data-stu-id="cc488-310">The encryption and hashing algorithms used by the forms authentication system to encrypt and validate the authentication ticket are customizable through the [&lt;machineKey&gt; element](https://msdn.microsoft.com/library/w8h3skw9.aspx) in Web.config. Table 2 outlines the &lt;machineKey&gt; element's attributes and their possible values.</span></span>

| <span data-ttu-id="cc488-311">**特性**</span><span class="sxs-lookup"><span data-stu-id="cc488-311">**Attribute**</span></span> | <span data-ttu-id="cc488-312">**说明**</span><span class="sxs-lookup"><span data-stu-id="cc488-312">**Description**</span></span> |
| --- | --- |
| <span data-ttu-id="cc488-313">解密</span><span class="sxs-lookup"><span data-stu-id="cc488-313">decryption</span></span> | <span data-ttu-id="cc488-314">指示用于加密的算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-314">Indicates the algorithm used for encryption.</span></span> <span data-ttu-id="cc488-315">此属性可以具有以下四个值之一:-自动-默认设置;确定基于 decryptionKey 属性的长度的算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-315">This attribute can have one of the following four values: - Auto - the default; determines the algorithm based on the length of the decryptionKey attribute.</span></span> <span data-ttu-id="cc488-316">使用 AES-[高级加密标准 (AES)](http://en.wikipedia.org/wiki/Advanced_Encryption_Standard)算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-316">- AES - uses the [Advanced Encryption Standard (AES)](http://en.wikipedia.org/wiki/Advanced_Encryption_Standard) algorithm.</span></span> <span data-ttu-id="cc488-317">DES-使用[数据加密标准 (DES)](http://en.wikipedia.org/wiki/Data_Encryption_Standard)此算法被视为计算弱，因此不应。</span><span class="sxs-lookup"><span data-stu-id="cc488-317">- DES - uses the [Data Encryption Standard (DES)](http://en.wikipedia.org/wiki/Data_Encryption_Standard) This algorithm is considered computationally weak and should not be used.</span></span> <span data-ttu-id="cc488-318">-3DES-使用[三重 DES](http://en.wikipedia.org/wiki/Triple_DES)算法工作方式是应用三次的 DES 算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-318">- 3DES - uses the [Triple DES](http://en.wikipedia.org/wiki/Triple_DES) algorithm, which works by applying the DES algorithm three times.</span></span> |
| <span data-ttu-id="cc488-319">decryptionKey</span><span class="sxs-lookup"><span data-stu-id="cc488-319">decryptionKey</span></span> | <span data-ttu-id="cc488-320">使用的加密算法密钥。</span><span class="sxs-lookup"><span data-stu-id="cc488-320">The secret key used by the encryption algorithm.</span></span> <span data-ttu-id="cc488-321">此值必须是适当的长度 （基于中解密值）、 自动生成或附加，其中任何一个值的十六进制字符串 IsolateApps。</span><span class="sxs-lookup"><span data-stu-id="cc488-321">This value must either be a hexadecimal string of the appropriate length (based on the value in decryption), AutoGenerate, or either value appended with,IsolateApps.</span></span> <span data-ttu-id="cc488-322">添加 IsolateApps 指示 ASP.NET 为每个应用程序使用的唯一值。</span><span class="sxs-lookup"><span data-stu-id="cc488-322">Adding IsolateApps instructs ASP.NET to use a unique value for each application.</span></span> <span data-ttu-id="cc488-323">默认值为 AutoGenerate，IsolateApps。</span><span class="sxs-lookup"><span data-stu-id="cc488-323">The default is AutoGenerate,IsolateApps.</span></span> |
| <span data-ttu-id="cc488-324">验证</span><span class="sxs-lookup"><span data-stu-id="cc488-324">validation</span></span> | <span data-ttu-id="cc488-325">指示用于验证的算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-325">Indicates the algorithm used for validation.</span></span> <span data-ttu-id="cc488-326">此属性可以具有以下四个值之一:-AES-使用高级加密标准 (AES) 算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-326">This attribute can have one of the following four values: - AES - uses the Advanced Encryption Standard (AES) algorithm.</span></span> <span data-ttu-id="cc488-327">-MD5-使用[消息摘要 5 (MD5)](http://en.wikipedia.org/wiki/MD5)算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-327">- MD5 - uses the [Message-Digest 5 (MD5)](http://en.wikipedia.org/wiki/MD5) algorithm.</span></span> <span data-ttu-id="cc488-328">-SHA1-使用[SHA1](http://en.wikipedia.org/wiki/Sha1)算法 （默认值）。</span><span class="sxs-lookup"><span data-stu-id="cc488-328">- SHA1 - uses the [SHA1](http://en.wikipedia.org/wiki/Sha1) algorithm (the default).</span></span> <span data-ttu-id="cc488-329">-3DES-使用三重 DES 算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-329">- 3DES - uses the Triple DES algorithm.</span></span> |
| <span data-ttu-id="cc488-330">validationKey</span><span class="sxs-lookup"><span data-stu-id="cc488-330">validationKey</span></span> | <span data-ttu-id="cc488-331">由验证算法密钥。</span><span class="sxs-lookup"><span data-stu-id="cc488-331">The secret key used by the validation algorithm.</span></span> <span data-ttu-id="cc488-332">此值必须是适当的长度 （基于在验证中的值）、 自动生成或附加，其中任何一个值的十六进制字符串 IsolateApps。</span><span class="sxs-lookup"><span data-stu-id="cc488-332">This value must either be a hexadecimal string of the appropriate length (based on the value in validation), AutoGenerate, or either value appended with,IsolateApps.</span></span> <span data-ttu-id="cc488-333">添加 IsolateApps 指示 ASP.NET 为每个应用程序使用的唯一值。</span><span class="sxs-lookup"><span data-stu-id="cc488-333">Adding IsolateApps instructs ASP.NET to use a unique value for each application.</span></span> <span data-ttu-id="cc488-334">默认值为 AutoGenerate，IsolateApps。</span><span class="sxs-lookup"><span data-stu-id="cc488-334">The default is AutoGenerate,IsolateApps.</span></span> |

<span data-ttu-id="cc488-335">**表 2**: &lt;machineKey&gt;元素特性</span><span class="sxs-lookup"><span data-stu-id="cc488-335">**Table 2**: The &lt;machineKey&gt; Element Attributes</span></span>

<span data-ttu-id="cc488-336">这些加密和验证的选项，和专业人员的全面讨论和缺点的各种算法，不在本教程的范围。</span><span class="sxs-lookup"><span data-stu-id="cc488-336">A thorough discussion of these encryption and validation options, and the pros and cons of the various algorithms, is beyond the scope of this tutorial.</span></span> <span data-ttu-id="cc488-337">为深入了解一下这些问题，包括要使用，哪些加密和验证算法的指南哪些密钥长度，若要使用，以及如何才能生成这些密钥，请参阅*Professional ASP.NET 2.0 安全、 成员资格和角色管理*.</span><span class="sxs-lookup"><span data-stu-id="cc488-337">For an in-depth look at these issues, including guidance on what encryption and validation algorithms to use, what key lengths to use, and how best to generate these keys, refer to *Professional ASP.NET 2.0 Security, Membership, and Role Management*.</span></span>

<span data-ttu-id="cc488-338">默认情况下，对于每个应用程序，自动生成用于加密和验证的密钥，这些键存储在本地安全机构 (LSA)。</span><span class="sxs-lookup"><span data-stu-id="cc488-338">By default, the keys used for encryption and validation are generated automatically for each application, and these keys are stored in the Local Security Authority (LSA).</span></span> <span data-ttu-id="cc488-339">简单地说，默认设置保证 web 服务器的 web 服务器和应用程序的应用程序的基础上的唯一键。</span><span class="sxs-lookup"><span data-stu-id="cc488-339">In short, the default settings guarantee unique keys on a web server-by-web server and application-by-application basis.</span></span> <span data-ttu-id="cc488-340">因此，此默认行为将不能用于以下两种情况：</span><span class="sxs-lookup"><span data-stu-id="cc488-340">Consequently, this default behavior will not work for the two following scenarios:</span></span>

- <span data-ttu-id="cc488-341">**Web 场**-在[web 场](http://en.wikipedia.org/wiki/Web_farm)方案中，单个 web 应用程序托管在可伸缩性和冗余性的目的的多个 web 服务器上。</span><span class="sxs-lookup"><span data-stu-id="cc488-341">**Web Farms** - in a [web farm](http://en.wikipedia.org/wiki/Web_farm) scenario, a single web application is hosted on multiple web servers for purposes of scalability and redundancy.</span></span> <span data-ttu-id="cc488-342">每个传入请求调度到的服务器场，这意味着，在用户的会话的生存期，不同的服务器可用于处理其各种请求中。</span><span class="sxs-lookup"><span data-stu-id="cc488-342">Each incoming request is dispatched to a server in the farm, meaning that over the lifetime of a user's session, different servers may be used to handle his various requests.</span></span> <span data-ttu-id="cc488-343">因此，每个服务器必须使用相同的加密和验证密钥，以便创建窗体身份验证票证、 加密和验证在上一台服务器可以解密，并在该场中的不同服务器上进行验证。</span><span class="sxs-lookup"><span data-stu-id="cc488-343">Consequently, each server must use the same encryption and validation keys so that the forms authentication ticket created, encrypted, and validated on one server can be decrypted and validated on a different server in the farm.</span></span>
- <span data-ttu-id="cc488-344">**跨应用程序票证共享**-单个 web 服务器可以托管多个 ASP.NET 应用程序。</span><span class="sxs-lookup"><span data-stu-id="cc488-344">**Cross Application Ticket Sharing** - a single web server may host multiple ASP.NET applications.</span></span> <span data-ttu-id="cc488-345">如果你需要有关这些不同应用程序共享单个窗体身份验证票证，它是命令性，他们加密和验证的密钥匹配。</span><span class="sxs-lookup"><span data-stu-id="cc488-345">If you need for these different applications to share a single forms authentication ticket, it is imperative that their encryption and validation keys match up.</span></span>

<span data-ttu-id="cc488-346">在处理在 web 场中设置或在同一台服务器上的应用程序之间共享身份验证票证时，你将需要配置&lt;machineKey&gt;在受影响的应用程序中的元素，以便其 decryptionKey 和validationKey 值匹配。</span><span class="sxs-lookup"><span data-stu-id="cc488-346">When working in a web farm setting or sharing authentication tickets across applications on the same server, you will need to configure the &lt;machineKey&gt; element in the affected applications so that their decryptionKey and validationKey values match up.</span></span>

<span data-ttu-id="cc488-347">尽管上述方案都不适用于我们的示例应用程序，我们仍可以指定显式 decryptionKey 和 validationKey 值并定义要使用的算法。</span><span class="sxs-lookup"><span data-stu-id="cc488-347">While neither of the above scenarios applies to our sample application, we can still specify explicit decryptionKey and validationKey values and define the algorithms to be used.</span></span> <span data-ttu-id="cc488-348">添加&lt;machineKey&gt;将设置为 Web.config 文件：</span><span class="sxs-lookup"><span data-stu-id="cc488-348">Add a &lt;machineKey&gt; setting to the Web.config file:</span></span>

[!code-xml[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample5.xml)]

<span data-ttu-id="cc488-349">有关详细信息，请查看[如何： 在 ASP.NET 2.0 配置 MachineKey](https://msdn.microsoft.com/library/ms998288.aspx)。</span><span class="sxs-lookup"><span data-stu-id="cc488-349">For more information check out [How To: Configure MachineKey in ASP.NET 2.0](https://msdn.microsoft.com/library/ms998288.aspx).</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-350">DecryptionKey 和 validationKey 值截取自[Steve Gibson](http://www.grc.com/stevegibson.htm)的[完美密码网页](https://www.grc.com/passwords.htm)，其中每个页，请访问上生成 64 随机的十六进制字符。</span><span class="sxs-lookup"><span data-stu-id="cc488-350">The decryptionKey and validationKey values were taken from [Steve Gibson](http://www.grc.com/stevegibson.htm)'s [Perfect Passwords web page](https://www.grc.com/passwords.htm), which generates 64 random hexadecimal characters on each page visit.</span></span> <span data-ttu-id="cc488-351">若要降低对生产应用程序正在影响这些密钥的可能性，建议您的更高版本的密钥替换为从完美密码页随机生成的。</span><span class="sxs-lookup"><span data-stu-id="cc488-351">To lessen the likelihood of these keys making their way into your production applications, you are encouraged to replace the above keys with randomly generated ones from the Perfect Passwords page.</span></span>


## <a name="step-4-storing-additional-user-data-in-the-ticket"></a><span data-ttu-id="cc488-352">步骤 4： 存储在票证中的其他用户数据</span><span class="sxs-lookup"><span data-stu-id="cc488-352">Step 4: Storing Additional User Data in the Ticket</span></span>

<span data-ttu-id="cc488-353">许多 web 应用程序显示有关的信息，或使基于当前登录用户的页面的显示。</span><span class="sxs-lookup"><span data-stu-id="cc488-353">Many web applications display information about or base the page's display on the currently logged on user.</span></span> <span data-ttu-id="cc488-354">例如，web 页可能会显示用户的名称和她上次登录的每个页面右上角的日期。</span><span class="sxs-lookup"><span data-stu-id="cc488-354">For example, a web page might show the user's name and the date she last logged on in the upper corner of every page.</span></span> <span data-ttu-id="cc488-355">窗体身份验证票证存储当前登录的用户的用户名，但页面需要的任何其他信息时，必须转到要查找不会存储在身份验证票证的信息的用户存储区-通常数据库。</span><span class="sxs-lookup"><span data-stu-id="cc488-355">The forms authentication ticket stores the currently logged on user's username, but when any other information is needed, the page must go to the user store - typically a database - to lookup the information not stored in the authentication ticket.</span></span>

<span data-ttu-id="cc488-356">很少的代码与我们可以存储在窗体身份验证票证的其他用户信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-356">With a little bit of code we can store additional user information in the forms authentication ticket.</span></span> <span data-ttu-id="cc488-357">可以通过表示此类数据[FormsAuthenticationTicket 类](https://msdn.microsoft.com/library/system.web.security.formsauthenticationticket.aspx)的[UserData 属性](https://msdn.microsoft.com/library/system.web.security.formsauthenticationticket.userdata.aspx)。</span><span class="sxs-lookup"><span data-stu-id="cc488-357">Such data can be expressed through the [FormsAuthenticationTicket class](https://msdn.microsoft.com/library/system.web.security.formsauthenticationticket.aspx)'s [UserData property](https://msdn.microsoft.com/library/system.web.security.formsauthenticationticket.userdata.aspx).</span></span> <span data-ttu-id="cc488-358">这是信息的一个有用地方安置的少量通常所需的用户有关。</span><span class="sxs-lookup"><span data-stu-id="cc488-358">This is a useful place to put small amounts of information about the user that is commonly needed.</span></span> <span data-ttu-id="cc488-359">UserData 属性包含在身份验证票证 cookie，并像其他票证字段、 是加密和验证中指定的值基于窗体身份验证系统的配置。</span><span class="sxs-lookup"><span data-stu-id="cc488-359">The value specified in the UserData property is included as part of the authentication ticket cookie and, like the other ticket fields, is encrypted and validated based on the forms authentication system's configuration.</span></span> <span data-ttu-id="cc488-360">默认情况下，UserData 为空字符串。</span><span class="sxs-lookup"><span data-stu-id="cc488-360">By default, UserData is an empty string.</span></span>

<span data-ttu-id="cc488-361">为了在身份验证票证中存储用户数据，我们需要在登录页上获取特定于用户的信息并将其存储在票证中编写一些代码。</span><span class="sxs-lookup"><span data-stu-id="cc488-361">In order to store user data in the authentication ticket, we need to write a bit of code in the login page that grabs the user-specific information and stores it in the ticket.</span></span> <span data-ttu-id="cc488-362">由于用户数据是字符串类型的属性，在其中存储的数据必须正确地序列化为字符串。</span><span class="sxs-lookup"><span data-stu-id="cc488-362">Since UserData is a property of type String, the data stored in it must be properly serialized as a string.</span></span> <span data-ttu-id="cc488-363">例如，假设，我们的用户存储包含每个用户的出生日期和雇主，名称中，我们想要将这些两个属性值存储在身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-363">For example, imagine that our user store included each user's date of birth and the name of their employer, and we wanted to store these two property values in the authentication ticket.</span></span> <span data-ttu-id="cc488-364">我们可能这些值序列化到字符串通过串联雇主名称后跟出生的字符串的竖线 (|) 的用户的日期。</span><span class="sxs-lookup"><span data-stu-id="cc488-364">We could serialize these values into a string by concatenating the user's date of birth's string with a pipe ( | ), followed by the employer name.</span></span> <span data-ttu-id="cc488-365">用户在 1974 年 8 月 15，云中生成适用于 Northwind Traders，我们将 UserData 属性将字符串分配： 1974年-08-15 |Northwind Traders。</span><span class="sxs-lookup"><span data-stu-id="cc488-365">For a user born on August 15, 1974 that works for Northwind Traders, we would assign the UserData property the string: 1974-08-15|Northwind Traders .</span></span>

<span data-ttu-id="cc488-366">我们需要访问存储在票证中的数据，只要我们能做到获取当前请求的 FormsAuthenticationTicket 和反序列化的 UserData 属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-366">Whenever we need to access the data stored in the ticket, we can do so by grabbing the current request's FormsAuthenticationTicket and deserializing the UserData property.</span></span> <span data-ttu-id="cc488-367">出生和雇主名称示例的日期，在我们会将 UserData 字符串拆分为两个基于分隔符 (|) 的子字符串。</span><span class="sxs-lookup"><span data-stu-id="cc488-367">In the case of the date of birth and employer name example, we would split the UserData string into two substrings based on the delimiter ( | ).</span></span>


<span data-ttu-id="cc488-368">[![其他用户信息可以存储在身份验证票证](forms-authentication-configuration-and-advanced-topics-vb/_static/image11.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image10.png)</span><span class="sxs-lookup"><span data-stu-id="cc488-368">[![Additional User Information Can Be Stored in the Authentication Ticket](forms-authentication-configuration-and-advanced-topics-vb/_static/image11.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image10.png)</span></span>

<span data-ttu-id="cc488-369">**图 04**： 其他用户的信息可以存储在身份验证票证 ([单击以查看实际尺寸的图像](forms-authentication-configuration-and-advanced-topics-vb/_static/image12.png))</span><span class="sxs-lookup"><span data-stu-id="cc488-369">**Figure 04**: Additional User Information Can Be Stored in the Authentication Ticket ([Click to view full-size image](forms-authentication-configuration-and-advanced-topics-vb/_static/image12.png))</span></span>


### <a name="writing-information-to-userdata"></a><span data-ttu-id="cc488-370">UserData 将信息写入</span><span class="sxs-lookup"><span data-stu-id="cc488-370">Writing Information to UserData</span></span>

<span data-ttu-id="cc488-371">遗憾的是，将特定于用户的信息添加到窗体身份验证票证不像这样简单如您所料。</span><span class="sxs-lookup"><span data-stu-id="cc488-371">Unfortunately, adding user-specific information to a forms authentication ticket is not as straightforward as one might expect.</span></span> <span data-ttu-id="cc488-372">FormsAuthenticationTicket 类的 UserData 属性是只读的并且只能通过 FormsAuthenticationTicket 类构造函数指定。</span><span class="sxs-lookup"><span data-stu-id="cc488-372">The UserData property of the FormsAuthenticationTicket class is read-only and can only be specified through the FormsAuthenticationTicket class constructor.</span></span> <span data-ttu-id="cc488-373">指定时 UserData 属性构造函数中，我们还需要提供票证的其他值： 用户名、 发布日期、 过期和等等。</span><span class="sxs-lookup"><span data-stu-id="cc488-373">When specifying the UserData property in the constructor, we also need to provide the ticket's other values: the username, the issue date, the expiration, and so on.</span></span> <span data-ttu-id="cc488-374">我们在前面的教程中创建的登录页，这是所有由为我们 FormsAuthentication 类。</span><span class="sxs-lookup"><span data-stu-id="cc488-374">When we created the login page in the preceding tutorial, this was all handled for us by the FormsAuthentication class.</span></span> <span data-ttu-id="cc488-375">向添加时 UserData FormsAuthenticationTicket，我们将需要编写代码来复制很多 FormsAuthentication 类已提供的功能。</span><span class="sxs-lookup"><span data-stu-id="cc488-375">When adding UserData to the FormsAuthenticationTicket, we will need to write code to replicate much of the functionality already provided by the FormsAuthentication class.</span></span>

<span data-ttu-id="cc488-376">我们来探讨使用 UserData 通过更新 Login.aspx 页面，以记录有关用户身份验证票证的其他信息所需的代码。</span><span class="sxs-lookup"><span data-stu-id="cc488-376">Let's explore the necessary code for working with UserData by updating the Login.aspx page to record additional information about the user to the authentication ticket.</span></span> <span data-ttu-id="cc488-377">假设我们的用户存储包含有关适用于用户的公司和其标题的信息，我们想要的身份验证票证中获取此信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-377">Pretend that our user store contains information about the company the user works for and their title, and that we want to capture this information in the authentication ticket.</span></span> <span data-ttu-id="cc488-378">更新 Login.aspx 页面 LoginButton Click 事件处理程序，使代码看起来如下所示：</span><span class="sxs-lookup"><span data-stu-id="cc488-378">Update the Login.aspx page's LoginButton Click event handler so that the code looks like the following:</span></span>

[!code-vb[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample6.vb)]

<span data-ttu-id="cc488-379">让我们逐步完成此一行代码一次。</span><span class="sxs-lookup"><span data-stu-id="cc488-379">Let's step through this code one line at a time.</span></span> <span data-ttu-id="cc488-380">该方法开始时通过定义四个字符串数组： 用户、 密码、 companyName 和 titleAtCompany。</span><span class="sxs-lookup"><span data-stu-id="cc488-380">The method starts by defining four string arrays: users, passwords, companyName, and titleAtCompany.</span></span> <span data-ttu-id="cc488-381">这些列阵储存用户名、 密码、 公司名称和标题的用户帐户在系统中的其中有三个： Scott、 Jisun 和 Sam。</span><span class="sxs-lookup"><span data-stu-id="cc488-381">These arrays hold the usernames, passwords, company names, and titles for the user accounts in the system, of which there are three: Scott, Jisun, and Sam.</span></span> <span data-ttu-id="cc488-382">在实际的应用程序，将从用户存储区，并非硬编码的页面的源代码中查询这些值。</span><span class="sxs-lookup"><span data-stu-id="cc488-382">In a real application, these values would be queried from the user store, not hard-coded in the page's source code.</span></span>

<span data-ttu-id="cc488-383">在上一教程中，如果提供的凭据不有效我们只需调用 FormsAuthentication.RedirectFromLoginPage （UserName.Text，RememberMe.Checked），执行以下步骤：</span><span class="sxs-lookup"><span data-stu-id="cc488-383">In the previous tutorial, if the supplied credentials were valid we simply called FormsAuthentication.RedirectFromLoginPage(UserName.Text, RememberMe.Checked), which performed the following steps:</span></span>

1. <span data-ttu-id="cc488-384">创建窗体身份验证票证</span><span class="sxs-lookup"><span data-stu-id="cc488-384">Created the forms authentication ticket</span></span>
2. <span data-ttu-id="cc488-385">票证写入在适当的存储。</span><span class="sxs-lookup"><span data-stu-id="cc488-385">Wrote the ticket to the appropriate store.</span></span> <span data-ttu-id="cc488-386">对于基于 cookie 的身份验证票证，所使用的浏览器的 cookie 集合;为无 cookie 身份验证票证的票证数据序列化到 URL</span><span class="sxs-lookup"><span data-stu-id="cc488-386">For cookies-based authentication tickets, the browser's cookies collection is used; for cookieless authentication tickets, the ticket data is serialized into the URL</span></span>
3. <span data-ttu-id="cc488-387">被该用户重定向到相应的页</span><span class="sxs-lookup"><span data-stu-id="cc488-387">Redirected the user to the appropriate page</span></span>

<span data-ttu-id="cc488-388">在上面的代码中复制这些步骤。</span><span class="sxs-lookup"><span data-stu-id="cc488-388">These steps are replicated in the code above.</span></span> <span data-ttu-id="cc488-389">首先，我们最终将 UserData 属性中存储的字符串是通过合并的公司名称和标题，并分隔两个值的竖线字符 (|) 形成的。</span><span class="sxs-lookup"><span data-stu-id="cc488-389">First, the string we will eventually store in the UserData property is formed by combining the company name and the title, delimiting the two values with a pipe character ( | ).</span></span>

<span data-ttu-id="cc488-390">Dim userDataString As String = String.Concat(companyName(i), "|", titleAtCompany(i))</span><span class="sxs-lookup"><span data-stu-id="cc488-390">Dim userDataString As String = String.Concat(companyName(i), "|", titleAtCompany(i))</span></span>

<span data-ttu-id="cc488-391">接下来，调用方法时，这会创建身份验证票证，FormsAuthentication.GetAuthCookie 加密和验证根据配置设置，并将其置于 HttpCookie 对象。</span><span class="sxs-lookup"><span data-stu-id="cc488-391">Next, the FormsAuthentication.GetAuthCookie method is invoked, which creates the authentication ticket, encrypts and validates it according to the configuration settings, and places it in an HttpCookie object.</span></span>

<span data-ttu-id="cc488-392">Dim authCookie 作为 HttpCookie = FormsAuthentication.GetAuthCookie （UserName.Text，RememberMe.Checked）</span><span class="sxs-lookup"><span data-stu-id="cc488-392">Dim authCookie As HttpCookie = FormsAuthentication.GetAuthCookie(UserName.Text, RememberMe.Checked)</span></span>

<span data-ttu-id="cc488-393">若要使用的 cookie 中嵌入 FormAuthenticationTicket，我们需要调用 FormAuthentication 类[解密方法](https://msdn.microsoft.com/library/system.web.security.formsauthentication.decrypt.aspx)、 传入的 cookie 值。</span><span class="sxs-lookup"><span data-stu-id="cc488-393">In order to work with the FormAuthenticationTicket embedded within the cookie, we need to call the FormAuthentication class's [Decrypt method](https://msdn.microsoft.com/library/system.web.security.formsauthentication.decrypt.aspx), passing in the cookie value.</span></span>

<span data-ttu-id="cc488-394">Dim ticket As FormsAuthenticationTicket = FormsAuthentication.Decrypt(authCookie.Value)</span><span class="sxs-lookup"><span data-stu-id="cc488-394">Dim ticket As FormsAuthenticationTicket = FormsAuthentication.Decrypt(authCookie.Value)</span></span>

<span data-ttu-id="cc488-395">然后，创建*新*FormsAuthenticationTicket 实例根据现有 FormsAuthenticationTicket 值。</span><span class="sxs-lookup"><span data-stu-id="cc488-395">We then create a *new* FormsAuthenticationTicket instance based on the existing FormsAuthenticationTicket's values.</span></span> <span data-ttu-id="cc488-396">但是，此新票证包含特定于用户的信息 (userDataString)。</span><span class="sxs-lookup"><span data-stu-id="cc488-396">However, this new ticket includes the user-specific information (userDataString).</span></span>

<span data-ttu-id="cc488-397">Dim newTicket 作为 FormsAuthenticationTicket = 新 FormsAuthenticationTicket(ticket.版本中，票证。名称，票证。IssueDate、 票证。过期时间，票证。IsPersistent，userDataString)</span><span class="sxs-lookup"><span data-stu-id="cc488-397">Dim newTicket As FormsAuthenticationTicket = New FormsAuthenticationTicket(ticket.Version, ticket.Name, ticket.IssueDate, ticket.Expiration, ticket.IsPersistent, userDataString)</span></span>

<span data-ttu-id="cc488-398">我们然后加密 （和验证） 新 FormsAuthenticationTicket 实例通过调用[加密方法](https://msdn.microsoft.com/library/system.web.security.formsauthentication.encrypt.aspx)，并将其置于 authCookie 回此加密 （和验证） 的数据。</span><span class="sxs-lookup"><span data-stu-id="cc488-398">We then encrypt (and validate) the new FormsAuthenticationTicket instance by calling the [Encrypt method](https://msdn.microsoft.com/library/system.web.security.formsauthentication.encrypt.aspx), and put this encrypted (and validated) data back into authCookie.</span></span>

<span data-ttu-id="cc488-399">authCookie.Value = FormsAuthentication.Encrypt(newTicket)</span><span class="sxs-lookup"><span data-stu-id="cc488-399">authCookie.Value = FormsAuthentication.Encrypt(newTicket)</span></span>

<span data-ttu-id="cc488-400">最后，authCookie 添加到 Response.Cookies 集合并调用 GetRedirectUrl 方法来确定向用户发送的相应页。</span><span class="sxs-lookup"><span data-stu-id="cc488-400">Finally, authCookie is added to the Response.Cookies collection and the GetRedirectUrl method is called to determine the appropriate page to send the user.</span></span>

[!code-vb[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample7.vb)]

<span data-ttu-id="cc488-401">需要所有这些代码，因为 UserData 属性是只读的它 FormsAuthentication 类不提供其 GetAuthCookie、 SetAuthCookie 或 RedirectFromLoginPage 方法中指定用户数据信息的任何方法。</span><span class="sxs-lookup"><span data-stu-id="cc488-401">All of this code is needed because the UserData property is read-only and the FormsAuthentication class does not provide any methods for specifying UserData information in its GetAuthCookie, SetAuthCookie, or RedirectFromLoginPage methods.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-402">我们刚检查的代码将特定于用户的信息存储在基于 cookie 的身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-402">The code we just examined stores user-specific information in a cookies-based authentication ticket.</span></span> <span data-ttu-id="cc488-403">负责序列化到的 URL 的窗体身份验证票证的类是.NET Framework 的内部。</span><span class="sxs-lookup"><span data-stu-id="cc488-403">The classes responsible for serializing the forms authentication ticket to the URL are internal to the .NET Framework.</span></span> <span data-ttu-id="cc488-404">长话短说，不能在无 cookie forms 身份验证票证中存储用户数据。</span><span class="sxs-lookup"><span data-stu-id="cc488-404">Long story short, you cannot store user data in a cookieless forms authentication ticket.</span></span>


### <a name="accessing-the-userdata-information"></a><span data-ttu-id="cc488-405">访问用户数据信息</span><span class="sxs-lookup"><span data-stu-id="cc488-405">Accessing the UserData Information</span></span>

<span data-ttu-id="cc488-406">现在每个用户的公司名称和标题存储在窗体身份验证票证的 UserData 属性在登录时。</span><span class="sxs-lookup"><span data-stu-id="cc488-406">At this point each user's company name and title is stored in the forms authentication ticket's UserData property when they log in.</span></span> <span data-ttu-id="cc488-407">可以从任何页面上的身份验证票证访问此信息，而无需用户存储到的行程。</span><span class="sxs-lookup"><span data-stu-id="cc488-407">This information can be accessed from the authentication ticket on any page without requiring a trip to the user store.</span></span> <span data-ttu-id="cc488-408">为了说明如何从 UserData 属性检索此信息，让我们更新 Default.aspx，以便其欢迎消息包括不仅用户的名称，但还它们适用于公司和职位。</span><span class="sxs-lookup"><span data-stu-id="cc488-408">To illustrate how this information can be retrieved from the UserData property, let's update Default.aspx so that its welcome message includes not only the user's name, but also the company they work for and their title.</span></span>

<span data-ttu-id="cc488-409">目前，Default.aspx 将 AuthenticatedMessagePanel 面板包含一个名为 WelcomeBackMessage 的标签控件。</span><span class="sxs-lookup"><span data-stu-id="cc488-409">Currently, Default.aspx contains an AuthenticatedMessagePanel Panel with a Label control named WelcomeBackMessage.</span></span> <span data-ttu-id="cc488-410">向经过身份验证的用户仅显示此面板。</span><span class="sxs-lookup"><span data-stu-id="cc488-410">This Panel is only displayed to authenticated users.</span></span> <span data-ttu-id="cc488-411">更新 Default.aspx 的页中的代码\_加载事件处理程序，使其如下所示：</span><span class="sxs-lookup"><span data-stu-id="cc488-411">Update the code in Default.aspx's Page\_Load event handler so that it looks like the following:</span></span>

[!code-vb[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample8.vb)]

<span data-ttu-id="cc488-412">如果 Request.IsAuthenticated 为 True，则首先将 WelcomeBackMessage 的文本属性设置为欢迎使用后，*用户名*。</span><span class="sxs-lookup"><span data-stu-id="cc488-412">If Request.IsAuthenticated is True, then the WelcomeBackMessage's Text property is first set to Welcome back, *username*.</span></span> <span data-ttu-id="cc488-413">然后，User.Identity 属性是强制转换为 FormsIdentity 对象，以便我们可以访问基础 FormsAuthenticationTicket。</span><span class="sxs-lookup"><span data-stu-id="cc488-413">Then, the User.Identity property is cast to a FormsIdentity object so that we can access the underlying FormsAuthenticationTicket.</span></span> <span data-ttu-id="cc488-414">FormsAuthenticationTicket 后，我们反 UserData 属性序列化为的公司名称和标题。</span><span class="sxs-lookup"><span data-stu-id="cc488-414">Once we have the FormsAuthenticationTicket, we deserialize the UserData property into the company name and title.</span></span> <span data-ttu-id="cc488-415">通过将拆分管道字符中的字符串来完成此操作。</span><span class="sxs-lookup"><span data-stu-id="cc488-415">This is accomplished by splitting the string on the pipe character.</span></span> <span data-ttu-id="cc488-416">WelcomeBackMessage 标签中然后会显示公司名称和标题。</span><span class="sxs-lookup"><span data-stu-id="cc488-416">The company name and title are then displayed in the WelcomeBackMessage Label.</span></span>

<span data-ttu-id="cc488-417">图 5 显示了操作中的此显示的屏幕截图。</span><span class="sxs-lookup"><span data-stu-id="cc488-417">Figure 5 shows a screenshot of this display in action.</span></span> <span data-ttu-id="cc488-418">日志记录为 Scott 显示包含 Scott 的公司和标题的欢迎使用后的消息。</span><span class="sxs-lookup"><span data-stu-id="cc488-418">Logging in as Scott displays a welcome back message that includes Scott's company and title.</span></span>


<span data-ttu-id="cc488-419">[![会显示当前登录的用户的公司和标题](forms-authentication-configuration-and-advanced-topics-vb/_static/image14.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image13.png)</span><span class="sxs-lookup"><span data-stu-id="cc488-419">[![The Currently Logged On User's Company and Title are Displayed](forms-authentication-configuration-and-advanced-topics-vb/_static/image14.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image13.png)</span></span>

<span data-ttu-id="cc488-420">**图 05**： 显示当前登录的用户的公司和标题 ([单击以查看实际尺寸的图像](forms-authentication-configuration-and-advanced-topics-vb/_static/image15.png))</span><span class="sxs-lookup"><span data-stu-id="cc488-420">**Figure 05**: The Currently Logged On User's Company and Title are Displayed ([Click to view full-size image](forms-authentication-configuration-and-advanced-topics-vb/_static/image15.png))</span></span>


> [!NOTE]
> <span data-ttu-id="cc488-421">身份验证票证的 UserData 属性将充当用户存储的缓存。</span><span class="sxs-lookup"><span data-stu-id="cc488-421">The authentication ticket's UserData property serves as a cache for the user store.</span></span> <span data-ttu-id="cc488-422">与任何缓存一样，它需要时修改基础数据进行更新。</span><span class="sxs-lookup"><span data-stu-id="cc488-422">Like any cache, it needs to be updated when the underlying data is modified.</span></span> <span data-ttu-id="cc488-423">例如，如果用户可以从中更新其配置文件的网页，必须刷新缓存 UserData 属性中的字段以反映用户所做的更改。</span><span class="sxs-lookup"><span data-stu-id="cc488-423">For example, if there is a web page from which users can update their profile, the fields cached in the UserData property must be refreshed to reflect the changes made by the user.</span></span>


## <a name="step-5-using-a-custom-principal"></a><span data-ttu-id="cc488-424">步骤 5： 使用自定义主体</span><span class="sxs-lookup"><span data-stu-id="cc488-424">Step 5: Using a Custom Principal</span></span>

<span data-ttu-id="cc488-425">在每个传入请求 FormsAuthenticationModule 尝试对用户进行身份验证。</span><span class="sxs-lookup"><span data-stu-id="cc488-425">On each incoming request the FormsAuthenticationModule attempts to authenticate the user.</span></span> <span data-ttu-id="cc488-426">如果存在未过期的身份验证票证，则 FormsAuthenticationModule 将 HttpContext.User 属性分配给一个新的 GenericPrincipal 对象。</span><span class="sxs-lookup"><span data-stu-id="cc488-426">If a non-expired authentication ticket is present, the FormsAuthenticationModule assigns the HttpContext.User property to a new GenericPrincipal object.</span></span> <span data-ttu-id="cc488-427">此 GenericPrincipal 对象具有类型 FormsIdentity，其中包括对窗体身份验证票证的引用的标识。</span><span class="sxs-lookup"><span data-stu-id="cc488-427">This GenericPrincipal object has an Identity of type FormsIdentity, which includes a reference to the forms authentication ticket.</span></span> <span data-ttu-id="cc488-428">GenericPrincipal 类包含实现 IPrincipal 的类所需的裸机最小功能-它只是具有 Identity 属性和 IsInRole 方法。</span><span class="sxs-lookup"><span data-stu-id="cc488-428">The GenericPrincipal class contains the bare minimum functionality needed by a class that implements IPrincipal - it just has an Identity property and an IsInRole method.</span></span>

<span data-ttu-id="cc488-429">主体对象具有两项职责： 若要指示为该用户所属的角色并提供标识信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-429">The principal object has two responsibilities: to indicate what roles the user belongs to and to provide identity information.</span></span> <span data-ttu-id="cc488-430">这通过 IPrincipal 接口 IsInRole (*roleName*) 方法和标识属性，分别。</span><span class="sxs-lookup"><span data-stu-id="cc488-430">This is accomplished through the IPrincipal interface's IsInRole(*roleName*) method and Identity property, respectively.</span></span> <span data-ttu-id="cc488-431">GenericPrincipal 类允许角色名称的字符串数组指定通过其构造函数;其 IsInRole (*roleName*) 方法只是检查以查看是否已传递的中*roleName*存在于字符串数组。</span><span class="sxs-lookup"><span data-stu-id="cc488-431">The GenericPrincipal class allows for a string array of role names to be specified via its constructor; its IsInRole(*roleName*) method merely checks to see if the passed in *roleName* exists within the string array.</span></span> <span data-ttu-id="cc488-432">当 FormsAuthenticationModule 创建 GenericPrincipal 时，会传递一个空字符串数组中给 GenericPrincipal 的构造函数。</span><span class="sxs-lookup"><span data-stu-id="cc488-432">When the FormsAuthenticationModule creates the GenericPrincipal, it passes in an empty string array to the GenericPrincipal's constructor.</span></span> <span data-ttu-id="cc488-433">因此，对 IsInRole 的任何调用将始终返回 False。</span><span class="sxs-lookup"><span data-stu-id="cc488-433">Consequently, any call to IsInRole will always return False.</span></span>

<span data-ttu-id="cc488-434">GenericPrincipal 类能满足大多数窗体基于的身份验证方案不使用角色的需求。</span><span class="sxs-lookup"><span data-stu-id="cc488-434">The GenericPrincipal class meets the needs for most forms based authentication scenarios where roles are not used.</span></span> <span data-ttu-id="cc488-435">对于这些情况下，默认角色处理时不足或者当需要将自定义的 IIdentity 对象与用户相关联，可以在身份验证工作流过程中创建自定义 IPrincipal 对象并将其分配给 HttpContext.User 属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-435">For those situations where the default role handling is insufficient or when you need to associate a custom IIdentity object with the user, you can create a custom IPrincipal object during the authentication workflow and assign it to the HttpContext.User property.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-436">我们会在将来的教程中，当 ASP。启用 NET 的角色框架创建类型的自定义主体对象[RolePrincipal](https://msdn.microsoft.com/library/system.web.security.roleprincipal.aspx)并覆盖窗体身份验证创建 GenericPrincipal 对象。</span><span class="sxs-lookup"><span data-stu-id="cc488-436">As we will see in future tutorials, when ASP.NET's Roles framework is enabled it creates a custom principal object of type [RolePrincipal](https://msdn.microsoft.com/library/system.web.security.roleprincipal.aspx) and overwrites the forms authentication-created GenericPrincipal object.</span></span> <span data-ttu-id="cc488-437">若要自定义角色框架的 API 与主体的 IsInRole 方法执行此操作。</span><span class="sxs-lookup"><span data-stu-id="cc488-437">It does this in order to customize the principal's IsInRole method to interface with the Roles framework's API.</span></span>


<span data-ttu-id="cc488-438">由于我们具有不关心自己角色尚未，我们必须用于此时，必须查明创建自定义主体的唯一原因是将自定义的 IIdentity 对象向主体相关联。</span><span class="sxs-lookup"><span data-stu-id="cc488-438">Since we have not concerned ourselves with roles yet, the only reason we would have for creating a custom principal at this juncture would be to associate a custom IIdentity object to the principal.</span></span> <span data-ttu-id="cc488-439">在步骤 4 中还介绍了用户的公司名称和其标题中特定身份验证票证的 UserData 属性中存储其他用户信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-439">In Step 4 we looked at storing additional user information in the authentication ticket's UserData property, in particular the user's company name and their title.</span></span> <span data-ttu-id="cc488-440">但是，UserData 信息只是将可通过身份验证票证访问，然后仅为序列化字符串，这意味着，只要我们想要查看用户信息存储在票证中我们需要分析 UserData 属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-440">However, the UserData information is only accessible through the authentication ticket and only then as a serialized string, meaning that anytime we want to view the user information stored in the ticket we need to parse the UserData property.</span></span>

<span data-ttu-id="cc488-441">我们可以通过创建实现 IIdentity 和包括 CompanyName 和 Title 属性的类来提高开发人员体验。</span><span class="sxs-lookup"><span data-stu-id="cc488-441">We can improve the developer experience by creating a class that implements IIdentity and includes CompanyName and Title properties.</span></span> <span data-ttu-id="cc488-442">这样一来，开发人员可以访问当前登录的用户的公司名称和标题直接通过公司名称和标题属性，而不需要了解如何分析 UserData 属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-442">That way, a developer can access the currently logged on user's company name and title directly through the CompanyName and Title properties without needed to know how to parse the UserData property.</span></span>

### <a name="creating-the-custom-identity-and-principal-classes"></a><span data-ttu-id="cc488-443">创建自定义标识和主体类</span><span class="sxs-lookup"><span data-stu-id="cc488-443">Creating the Custom Identity and Principal Classes</span></span>

<span data-ttu-id="cc488-444">对于本教程，让我们将创建在应用程序中的自定义主体和标识对象\_代码文件夹。</span><span class="sxs-lookup"><span data-stu-id="cc488-444">For this tutorial, let's create the custom principal and identity objects in the App\_Code folder.</span></span> <span data-ttu-id="cc488-445">首先，通过添加应用\_代码到你的项目的文件夹-右键单击解决方案资源管理器中的项目名称，选择添加 ASP.NET 文件夹选项，并选择应用\_代码。</span><span class="sxs-lookup"><span data-stu-id="cc488-445">Start by adding an App\_Code folder to your project - right-click on the project name in Solution Explorer, select the Add ASP.NET Folder option, and choose App\_Code.</span></span> <span data-ttu-id="cc488-446">应用\_代码文件夹是一个特殊的 ASP.NET 文件夹包含类文件特定于该网站。</span><span class="sxs-lookup"><span data-stu-id="cc488-446">The App\_Code folder is a special ASP.NET folder that holds class files specific to the website.</span></span>

> [!NOTE]
> <span data-ttu-id="cc488-447">应用\_管理通过网站项目模型项目时，应仅使用代码文件夹。</span><span class="sxs-lookup"><span data-stu-id="cc488-447">The App\_Code folder should only be used when managing your project through the Website Project Model.</span></span> <span data-ttu-id="cc488-448">如果使用的[Web 应用程序项目模型](https://msdn.microsoft.com/asp.net/Aa336618.aspx)、 创建标准文件夹并将类添加到的。</span><span class="sxs-lookup"><span data-stu-id="cc488-448">If you are using the [Web Application Project Model](https://msdn.microsoft.com/asp.net/Aa336618.aspx), create a standard folder and add the classes to that.</span></span> <span data-ttu-id="cc488-449">例如，无法添加名为类的新文件夹，并那里将你的代码。</span><span class="sxs-lookup"><span data-stu-id="cc488-449">For example, you could add a new folder named Classes, and place your code there.</span></span>


<span data-ttu-id="cc488-450">接下来，将两个新类文件添加到应用\_代码文件夹，一个命名的 CustomIdentity.vb，另一个名为 CustomPrincipal.vb。</span><span class="sxs-lookup"><span data-stu-id="cc488-450">Next, add two new class files to the App\_Code folder, one named CustomIdentity.vb and one named CustomPrincipal.vb.</span></span>


<span data-ttu-id="cc488-451">[![将 CustomIdentity 和 CustomPrincipal 类添加到你的项目](forms-authentication-configuration-and-advanced-topics-vb/_static/image17.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image16.png)</span><span class="sxs-lookup"><span data-stu-id="cc488-451">[![Add the CustomIdentity and CustomPrincipal Classes to Your Project](forms-authentication-configuration-and-advanced-topics-vb/_static/image17.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image16.png)</span></span>

<span data-ttu-id="cc488-452">**图 06**： 将 CustomIdentity 和 CustomPrincipal 类添加到你的项目 ([单击以查看实际尺寸的图像](forms-authentication-configuration-and-advanced-topics-vb/_static/image18.png))</span><span class="sxs-lookup"><span data-stu-id="cc488-452">**Figure 06**: Add the CustomIdentity and CustomPrincipal Classes to Your Project ([Click to view full-size image](forms-authentication-configuration-and-advanced-topics-vb/_static/image18.png))</span></span>


<span data-ttu-id="cc488-453">CustomIdentity 类负责实现 IIdentity 接口，定义 AuthenticationType、 IsAuthenticated 和名称属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-453">The CustomIdentity class is responsible for implementing the IIdentity interface, which defines the AuthenticationType, IsAuthenticated, and Name properties.</span></span> <span data-ttu-id="cc488-454">除了这些必需的属性，我们感兴趣公开基础窗体身份验证票证，以及用户的公司名称和标题的属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-454">In addition to those required properties, we are interested in exposing the underlying forms authentication ticket as well as properties for the user's company name and title.</span></span> <span data-ttu-id="cc488-455">下面的代码进入 CustomIdentity 类。</span><span class="sxs-lookup"><span data-stu-id="cc488-455">Enter the following code into the CustomIdentity class.</span></span>

[!code-vb[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample9.vb)]

<span data-ttu-id="cc488-456">请注意，此类包含一个 FormsAuthenticationTicket 成员变量 (\_票证) 和通过构造函数时必须提供此票证的信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-456">Note that the class includes a FormsAuthenticationTicket member variable (\_ticket) and that this ticket information must be supplied through the constructor.</span></span> <span data-ttu-id="cc488-457">在返回的标识名称，则使用此票证数据其 UserData 属性进行分析，以返回公司名称和标题属性的值。</span><span class="sxs-lookup"><span data-stu-id="cc488-457">This ticket data is used in returning the identity's Name; its UserData property is parsed to return the values for the CompanyName and Title properties.</span></span>

<span data-ttu-id="cc488-458">接下来，创建 CustomPrincipal 类。</span><span class="sxs-lookup"><span data-stu-id="cc488-458">Next, create the CustomPrincipal class.</span></span> <span data-ttu-id="cc488-459">因为我们不关心角色此时，必须查明，CustomPrincipal 类的构造函数接受仅 CustomIdentity 对象;它的 IsInRole 方法始终返回 False。</span><span class="sxs-lookup"><span data-stu-id="cc488-459">Since we are not concerned with roles at this juncture, the CustomPrincipal class's constructor accepts only a CustomIdentity object; its IsInRole method always returns False.</span></span>

[!code-vb[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample10.vb)]

### <a name="assigning-a-customprincipal-object-to-the-incoming-requests-security-context"></a><span data-ttu-id="cc488-460">CustomPrincipal 对象分配给传入请求的安全上下文</span><span class="sxs-lookup"><span data-stu-id="cc488-460">Assigning a CustomPrincipal Object to the Incoming Request's Security Context</span></span>

<span data-ttu-id="cc488-461">现在，我们扩展了默认 IIdentity 规范，包括公司名称和标题属性的类，以及使用自定义标识的自定义主体类。</span><span class="sxs-lookup"><span data-stu-id="cc488-461">We now have a class that extends the default IIdentity specification to include CompanyName and Title properties, as well as a custom principal class that uses the custom identity.</span></span> <span data-ttu-id="cc488-462">我们可以单步执行 ASP.NET 管道，并将我们的自定义主体对象分配给传入请求的安全上下文。</span><span class="sxs-lookup"><span data-stu-id="cc488-462">We are ready to step into the ASP.NET pipeline and assign our custom principal object to the incoming request's security context.</span></span>

<span data-ttu-id="cc488-463">在 ASP.NET 管道接收传入请求并处理它通过一系列步骤。</span><span class="sxs-lookup"><span data-stu-id="cc488-463">The ASP.NET pipeline takes an incoming request and processes it through a number of steps.</span></span> <span data-ttu-id="cc488-464">每个步骤中，在引发特定事件以使开发人员可以利用 ASP.NET 管道并修改其生命周期中的特定点处的请求。</span><span class="sxs-lookup"><span data-stu-id="cc488-464">At each step, a particular event is raised, making it possible for developers to tap into the ASP.NET pipeline and modify the request at certain points in its lifecycle.</span></span> <span data-ttu-id="cc488-465">FormsAuthenticationModule，例如，等待 ASP.NET 引发[AuthenticateRequest 事件](https://msdn.microsoft.com/library/system.web.httpapplication.authenticaterequest.aspx)，此时它会检查身份验证票证的传入请求。</span><span class="sxs-lookup"><span data-stu-id="cc488-465">The FormsAuthenticationModule, for example, waits for ASP.NET to raise the [AuthenticateRequest event](https://msdn.microsoft.com/library/system.web.httpapplication.authenticaterequest.aspx), at which point it inspects the incoming request for an authentication ticket.</span></span> <span data-ttu-id="cc488-466">如果找到一个身份验证票证，则是创建一个 GenericPrincipal 对象并将其分配给 HttpContext.User 属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-466">If an authentication ticket is found, a GenericPrincipal object is created and assigned to the HttpContext.User property.</span></span>

<span data-ttu-id="cc488-467">AuthenticateRequest 事件之后，ASP.NET 管道引发[PostAuthenticateRequest 事件](https://msdn.microsoft.com/library/system.web.httpapplication.postauthenticaterequest.aspx)，这是我们可以替换为创建的实例与 FormsAuthenticationModule 的 GenericPrincipal 对象我们CustomPrincipal 对象。</span><span class="sxs-lookup"><span data-stu-id="cc488-467">After the AuthenticateRequest event, the ASP.NET pipeline raises the [PostAuthenticateRequest event](https://msdn.microsoft.com/library/system.web.httpapplication.postauthenticaterequest.aspx), which is where we can replace the GenericPrincipal object created by the FormsAuthenticationModule with an instance of our CustomPrincipal object.</span></span> <span data-ttu-id="cc488-468">图 7 显示了此工作流。</span><span class="sxs-lookup"><span data-stu-id="cc488-468">Figure 7 depicts this workflow.</span></span>


<span data-ttu-id="cc488-469">[![GenericPrincipal CustomPrincipal PostAuthenticationRequest 事件中被替换为](forms-authentication-configuration-and-advanced-topics-vb/_static/image20.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image19.png)</span><span class="sxs-lookup"><span data-stu-id="cc488-469">[![The GenericPrincipal is Replaced by a CustomPrincipal in the PostAuthenticationRequest Event](forms-authentication-configuration-and-advanced-topics-vb/_static/image20.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image19.png)</span></span>

<span data-ttu-id="cc488-470">**图 07**: GenericPrincipal CustomPrincipal PostAuthenticationRequest 事件中被替换为 ([单击以查看实际尺寸的图像](forms-authentication-configuration-and-advanced-topics-vb/_static/image21.png))</span><span class="sxs-lookup"><span data-stu-id="cc488-470">**Figure 07**: The GenericPrincipal is Replaced by a CustomPrincipal in the PostAuthenticationRequest Event ([Click to view full-size image](forms-authentication-configuration-and-advanced-topics-vb/_static/image21.png))</span></span>


<span data-ttu-id="cc488-471">若要执行的代码以响应 ASP.NET 管道事件，我们可以在 Global.asax 中创建相应的事件处理程序，或创建我们自己的 HTTP 模块。</span><span class="sxs-lookup"><span data-stu-id="cc488-471">In order to execute code in response to an ASP.NET pipeline event, we can either create the appropriate event handler in Global.asax or create our own HTTP Module.</span></span> <span data-ttu-id="cc488-472">对于本教程让我们在 Global.asax 中创建的事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="cc488-472">For this tutorial let's create the event handler in Global.asax.</span></span> <span data-ttu-id="cc488-473">首先将 Global.asax 添加到你的网站。</span><span class="sxs-lookup"><span data-stu-id="cc488-473">Start by adding Global.asax to your website.</span></span> <span data-ttu-id="cc488-474">右键单击解决方案资源管理器中的项目名称并添加名为 Global.asax 的全局应用程序类类型的项。</span><span class="sxs-lookup"><span data-stu-id="cc488-474">Right-click on the project name in Solution Explorer and add an item of type Global Application Class named Global.asax.</span></span>


<span data-ttu-id="cc488-475">[![Global.asax 文件添加到你的网站](forms-authentication-configuration-and-advanced-topics-vb/_static/image23.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image22.png)</span><span class="sxs-lookup"><span data-stu-id="cc488-475">[![Add a Global.asax File to Your Website](forms-authentication-configuration-and-advanced-topics-vb/_static/image23.png)](forms-authentication-configuration-and-advanced-topics-vb/_static/image22.png)</span></span>

<span data-ttu-id="cc488-476">**图 08**： 将 Global.asax 文件添加到您的网站 ([单击以查看实际尺寸的图像](forms-authentication-configuration-and-advanced-topics-vb/_static/image24.png))</span><span class="sxs-lookup"><span data-stu-id="cc488-476">**Figure 08**: Add a Global.asax File to Your Website ([Click to view full-size image](forms-authentication-configuration-and-advanced-topics-vb/_static/image24.png))</span></span>


<span data-ttu-id="cc488-477">默认 Global.asax 模板包括多个 ASP.NET 管道事件，包括开始时，最终的事件处理程序和[错误事件](https://msdn.microsoft.com/library/system.web.httpapplication.error.aspx)，等等。</span><span class="sxs-lookup"><span data-stu-id="cc488-477">The default Global.asax template includes event handlers for a number of the ASP.NET pipeline events, including the Start, End and [Error event](https://msdn.microsoft.com/library/system.web.httpapplication.error.aspx), among others.</span></span> <span data-ttu-id="cc488-478">可以删除这些事件处理程序，因为我们不需要这些服务为此应用程序。</span><span class="sxs-lookup"><span data-stu-id="cc488-478">Feel free to remove these event handlers, as we do not need them for this application.</span></span> <span data-ttu-id="cc488-479">我们感兴趣的事件是 PostAuthenticateRequest。</span><span class="sxs-lookup"><span data-stu-id="cc488-479">The event we are interested in is PostAuthenticateRequest.</span></span> <span data-ttu-id="cc488-480">更新在 Global.asax 文件，使其标记看起来类似于以下：</span><span class="sxs-lookup"><span data-stu-id="cc488-480">Update your Global.asax file so its markup looks similar to the following:</span></span>

[!code-aspx[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample11.aspx)]

<span data-ttu-id="cc488-481">应用程序\_OnPostAuthenticateRequest 方法执行每次 ASP.NET 运行时将引发 PostAuthenticateRequest 事件，这种情况发生在每个传入的页请求上一次。</span><span class="sxs-lookup"><span data-stu-id="cc488-481">The Application\_OnPostAuthenticateRequest method executes each time the ASP.NET runtime raises the PostAuthenticateRequest event, which happens once on each incoming page request.</span></span> <span data-ttu-id="cc488-482">事件处理程序首先会检查以查看用户进行身份验证，并且通过窗体身份验证进行身份验证。</span><span class="sxs-lookup"><span data-stu-id="cc488-482">The event handler starts by checking to see if the user is authenticated and was authenticated via forms authentication.</span></span> <span data-ttu-id="cc488-483">如果是这样，新的 CustomIdentity 对象创建，并在其构造函数中传递当前请求的身份验证票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-483">If so, a new CustomIdentity object is created and passed the current request's authentication ticket in its constructor.</span></span> <span data-ttu-id="cc488-484">接下来，CustomPrincipal 对象是创建并在其构造函数中传递的刚创建 CustomIdentity 对象。</span><span class="sxs-lookup"><span data-stu-id="cc488-484">Following that, a CustomPrincipal object is created and passed the just-created CustomIdentity object in its constructor.</span></span> <span data-ttu-id="cc488-485">最后，将当前请求的安全上下文被分配给新创建的 CustomPrincipal 对象。</span><span class="sxs-lookup"><span data-stu-id="cc488-485">Finally, the current request's security context is assigned to the newly created CustomPrincipal object.</span></span>

<span data-ttu-id="cc488-486">请注意-将 CustomPrincipal 对象与请求的安全上下文关联的最后一步将主体分配到两个属性： HttpContext.User 和 Thread.CurrentPrincipal。</span><span class="sxs-lookup"><span data-stu-id="cc488-486">Note that the last step - associating the CustomPrincipal object with the request's security context - assigns the principal to two properties: HttpContext.User and Thread.CurrentPrincipal.</span></span> <span data-ttu-id="cc488-487">由于在 ASP.NET 中处理的安全上下文的方式，这些两个分配是必需的。</span><span class="sxs-lookup"><span data-stu-id="cc488-487">These two assignments are necessary because of the way security contexts are handled in ASP.NET.</span></span> <span data-ttu-id="cc488-488">.NET Framework 将与每个正在运行的线程; 相关联的安全上下文此信息是可通过 IPrincipal 对象用作[线程对象](https://msdn.microsoft.com/library/system.threading.thread.aspx)的[CurrentPrincipal 属性](https://msdn.microsoft.com/library/system.threading.thread.currentcontext.aspx)。</span><span class="sxs-lookup"><span data-stu-id="cc488-488">The .NET Framework associates a security context with each running thread; this information is available as an IPrincipal object through the [Thread object](https://msdn.microsoft.com/library/system.threading.thread.aspx)'s [CurrentPrincipal property](https://msdn.microsoft.com/library/system.threading.thread.currentcontext.aspx).</span></span> <span data-ttu-id="cc488-489">让人困惑的是 ASP.NET 有其自己的安全上下文信息 (HttpContext.User)。</span><span class="sxs-lookup"><span data-stu-id="cc488-489">What is a confusing is that ASP.NET has its own security context information (HttpContext.User).</span></span>

<span data-ttu-id="cc488-490">在某些情况下，确定的安全上下文; 时检查 Thread.CurrentPrincipal 属性在其他情况下，使用 HttpContext.User。</span><span class="sxs-lookup"><span data-stu-id="cc488-490">In certain scenarios, the Thread.CurrentPrincipal property is examined when determining the security context; in other scenarios, HttpContext.User is used.</span></span> <span data-ttu-id="cc488-491">例如，有在.NET 中的安全功能，可让开发人员以声明方式状态的哪些用户或角色可以实例化类或调用特定方法 (请参阅[添加到业务和数据层使用的授权规则PrincipalPermissionAttributes](https://weblogs.asp.net/scottgu/archive/2006/10/04/Tip_2F00_Trick_3A00_-Adding-Authorization-Rules-to-Business-and-Data-Layers-using-PrincipalPermissionAttributes.aspx))。</span><span class="sxs-lookup"><span data-stu-id="cc488-491">For example, there are security features in .NET that allow developers to declaratively state what users or roles can instantiate a class or invoke specific methods (see [Adding Authorization Rules to Business and Data Layers Using PrincipalPermissionAttributes](https://weblogs.asp.net/scottgu/archive/2006/10/04/Tip_2F00_Trick_3A00_-Adding-Authorization-Rules-to-Business-and-Data-Layers-using-PrincipalPermissionAttributes.aspx)).</span></span> <span data-ttu-id="cc488-492">实际上，这些声明性方法确定通过 Thread.CurrentPrincipal 属性的安全上下文。</span><span class="sxs-lookup"><span data-stu-id="cc488-492">Underneath the covers, these declarative techniques determine the security context via the Thread.CurrentPrincipal property.</span></span>

<span data-ttu-id="cc488-493">在其他情况下，使用 HttpContext.User 属性。</span><span class="sxs-lookup"><span data-stu-id="cc488-493">In other scenarios, the HttpContext.User property is used.</span></span> <span data-ttu-id="cc488-494">例如上, 一教程中我们使用此属性以显示当前登录的用户的用户名。</span><span class="sxs-lookup"><span data-stu-id="cc488-494">For example, in the previous tutorial we used this property to display the currently logged on user's username.</span></span> <span data-ttu-id="cc488-495">很明显，然后，它是命令性，匹配的 Thread.CurrentPrincipal 和 HttpContext.User 属性中的安全上下文信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-495">Clearly, then, it is imperative that the security context information in the Thread.CurrentPrincipal and HttpContext.User properties match up.</span></span>

<span data-ttu-id="cc488-496">ASP.NET 运行时自动同步为我们的这些属性值。</span><span class="sxs-lookup"><span data-stu-id="cc488-496">The ASP.NET runtime automatically syncs these property values for us.</span></span> <span data-ttu-id="cc488-497">但是，AuthenticateRequest 事件之后，会发生这种同步，但*之前*PostAuthenticateRequest 事件。</span><span class="sxs-lookup"><span data-stu-id="cc488-497">However, this synchronization occurs after the AuthenticateRequest event, but *before* the PostAuthenticateRequest event.</span></span> <span data-ttu-id="cc488-498">因此，我们 PostAuthenticateRequest 事件中添加自定义主体时需要进行某些手动分配 Thread.CurrentPrincipal，否则 Thread.CurrentPrincipal，HttpContext.User 将不同步。请参阅[Context.User vs。Thread.CurrentPrincipal](http://leastprivilege.com/2005/11/23/context-user-vs-thread-currentprincipal/)有关此问题的更多详细讨论。</span><span class="sxs-lookup"><span data-stu-id="cc488-498">Consequently, when adding a custom principal in the PostAuthenticateRequest event we need to be certain to manually assign the Thread.CurrentPrincipal or else Thread.CurrentPrincipal and HttpContext.User will be out of sync. See [Context.User vs. Thread.CurrentPrincipal](http://leastprivilege.com/2005/11/23/context-user-vs-thread-currentprincipal/) for a more detailed discussion on this issue.</span></span>

### <a name="accessing-the-companyname-and-title-properties"></a><span data-ttu-id="cc488-499">访问公司名称和标题属性</span><span class="sxs-lookup"><span data-stu-id="cc488-499">Accessing the CompanyName and Title Properties</span></span>

<span data-ttu-id="cc488-500">每当请求到达并分派给 ASP.NET 引擎，该应用程序\_OnPostAuthenticateRequest Global.asax 中的事件处理程序将激发。</span><span class="sxs-lookup"><span data-stu-id="cc488-500">Whenever a request arrives and is dispatched to the ASP.NET engine, the Application\_OnPostAuthenticateRequest event handler in Global.asax will fire.</span></span> <span data-ttu-id="cc488-501">如果请求已成功经过 formsauthenticationmodule，事件处理程序将使用基于窗体身份验证票证的 CustomIdentity 对象创建一个新的 CustomPrincipal 对象。</span><span class="sxs-lookup"><span data-stu-id="cc488-501">If the request has been successfully authenticated by the FormsAuthenticationModule, the event handler will create a new CustomPrincipal object with a CustomIdentity object based on the forms authentication ticket.</span></span> <span data-ttu-id="cc488-502">在位置对此逻辑，使用访问有关当前已登录的用户的公司名称和标题信息是非常简单。</span><span class="sxs-lookup"><span data-stu-id="cc488-502">With this logic in place, accessing information about the currently logged on user's company name and title is incredibly straightforward.</span></span>

<span data-ttu-id="cc488-503">返回到页\_在 Default.aspx 中，其中在步骤 4 中，我们编写了代码，以检索窗体身份验证票证和分析 UserData 属性以显示用户的公司名称和标题的 Load 事件处理程序。</span><span class="sxs-lookup"><span data-stu-id="cc488-503">Return to the Page\_Load event handler in Default.aspx, where in Step 4 we wrote code to retrieve the form authentication ticket and parse the UserData property in order to display the user's company name and title.</span></span> <span data-ttu-id="cc488-504">与使用现在的 CustomPrincipal 和 CustomIdentity 对象，就不必分析出票证的 UserData 属性的值。</span><span class="sxs-lookup"><span data-stu-id="cc488-504">With the CustomPrincipal and CustomIdentity objects in use now, there's no need to parse the values out of the ticket's UserData property.</span></span> <span data-ttu-id="cc488-505">相反，只需获取对 CustomIdentity 对象的引用，并使用其公司名称和标题属性：</span><span class="sxs-lookup"><span data-stu-id="cc488-505">Instead, simply get a reference to the CustomIdentity object and use its CompanyName and Title properties:</span></span>

[!code-vb[Main](forms-authentication-configuration-and-advanced-topics-vb/samples/sample12.vb)]

## <a name="summary"></a><span data-ttu-id="cc488-506">总结</span><span class="sxs-lookup"><span data-stu-id="cc488-506">Summary</span></span>

<span data-ttu-id="cc488-507">在本教程中介绍了如何自定义通过 Web.config 的窗体身份验证系统的设置。我们了解了如何处理的身份验证票证到期日期以及如何使用加密和验证安全措施来防止检查和修改的票证。</span><span class="sxs-lookup"><span data-stu-id="cc488-507">In this tutorial we examined how to customize the forms authentication system's settings via Web.config. We looked at how the authentication ticket's expiration is handled and how the encryption and validation safeguards are used to protect the ticket from inspection and modification.</span></span> <span data-ttu-id="cc488-508">最后，我们讨论了使用身份验证票证的 UserData 属性存储在票证本身中的其他用户信息以及如何使用自定义主体和标识对象来以更加便于开发人员的方式公开此信息。</span><span class="sxs-lookup"><span data-stu-id="cc488-508">Finally, we discussed using the authentication ticket's UserData property to store additional user information in the ticket itself, and how to use custom principal and identity objects to expose this information in a more developer-friendly manner.</span></span>

<span data-ttu-id="cc488-509">本教程最后，我们对 ASP.NET 中的窗体身份验证执行的检查。</span><span class="sxs-lookup"><span data-stu-id="cc488-509">This tutorial concludes our examination of forms authentication in ASP.NET.</span></span> <span data-ttu-id="cc488-510">下一教程将启动到成员资格框架之旅。</span><span class="sxs-lookup"><span data-stu-id="cc488-510">The next tutorial starts our journey into the Membership framework.</span></span>

<span data-ttu-id="cc488-511">快乐编程 ！</span><span class="sxs-lookup"><span data-stu-id="cc488-511">Happy Programming!</span></span>

### <a name="further-reading"></a><span data-ttu-id="cc488-512">其他阅读材料</span><span class="sxs-lookup"><span data-stu-id="cc488-512">Further Reading</span></span>

<span data-ttu-id="cc488-513">在本教程中讨论的主题的详细信息，请参阅以下资源：</span><span class="sxs-lookup"><span data-stu-id="cc488-513">For more information on the topics discussed in this tutorial, refer to the following resources:</span></span>

- [<span data-ttu-id="cc488-514">仔细分析窗体身份验证</span><span class="sxs-lookup"><span data-stu-id="cc488-514">Dissecting Forms Authentication</span></span>](http://aspnet.4guysfromrolla.com/articles/072005-1.aspx)
- [<span data-ttu-id="cc488-515">ASP.NET 2.0 中所述： 窗体身份验证</span><span class="sxs-lookup"><span data-stu-id="cc488-515">Explained: Forms Authentication in ASP.NET 2.0</span></span>](https://msdn.microsoft.com/library/aa480476.aspx)
- [<span data-ttu-id="cc488-516">如何： 保护 ASP.NET 2.0 中的窗体身份验证</span><span class="sxs-lookup"><span data-stu-id="cc488-516">How To: Protect Forms Authentication in ASP.NET 2.0</span></span>](https://msdn.microsoft.com/library/ms998310.aspx)
- <span data-ttu-id="cc488-517">[专业 ASP.NET 2.0 安全、 成员资格和角色管理](http://www.wrox.com/WileyCDA/WroxTitle/productCd-0764596985.html)(ISBN: 978-0-7645-9698-8)</span><span class="sxs-lookup"><span data-stu-id="cc488-517">[Professional ASP.NET 2.0 Security, Membership, and Role Management](http://www.wrox.com/WileyCDA/WroxTitle/productCd-0764596985.html) (ISBN: 978-0-7645-9698-8)</span></span>
- [<span data-ttu-id="cc488-518">保护登录控件</span><span class="sxs-lookup"><span data-stu-id="cc488-518">Securing Login Controls</span></span>](https://msdn.microsoft.com/library/ms178346.aspx)
- [<span data-ttu-id="cc488-519">&lt;身份验证&gt;元素</span><span class="sxs-lookup"><span data-stu-id="cc488-519">The &lt;authentication&gt; Element</span></span>](https://msdn.microsoft.com/library/532aee0e.aspx)
- [<span data-ttu-id="cc488-520">&lt;窗体&gt;元素&lt;身份验证&gt;</span><span class="sxs-lookup"><span data-stu-id="cc488-520">The &lt;forms&gt; Element for &lt;authentication&gt;</span></span>](https://msdn.microsoft.com/library/1d3t3c61.aspx)
- [<span data-ttu-id="cc488-521">&lt;MachineKey&gt;元素</span><span class="sxs-lookup"><span data-stu-id="cc488-521">The &lt;machineKey&gt; Element</span></span>](https://msdn.microsoft.com/library/w8h3skw9.aspx)
- [<span data-ttu-id="cc488-522">了解窗体身份验证票证和 Cookie</span><span class="sxs-lookup"><span data-stu-id="cc488-522">Understanding the Forms Authentication Ticket and Cookie</span></span>](https://support.microsoft.com/kb/910443)

### <a name="video-training-on-topics-contained-in-this-tutorial"></a><span data-ttu-id="cc488-523">在本教程中包含的主题的视频培训</span><span class="sxs-lookup"><span data-stu-id="cc488-523">Video Training on Topics Contained in this Tutorial</span></span>

- [<span data-ttu-id="cc488-524">如何更改窗体身份验证属性</span><span class="sxs-lookup"><span data-stu-id="cc488-524">How to Change the Forms Authentication Properties</span></span>](../../../videos/authentication/how-to-change-the-forms-authentication-properties.md)
- [<span data-ttu-id="cc488-525">如何在 ASP.NET 应用程序中的设置和使用无 Cookie 身份验证</span><span class="sxs-lookup"><span data-stu-id="cc488-525">How to Setup and Use Cookie-less Authentication in an ASP.NET Application</span></span>](../../../videos/authentication/how-to-setup-and-use-cookie-less-authentication-in-an-aspnet-application.md)
- [<span data-ttu-id="cc488-526">ASP 窗体登录重定位</span><span class="sxs-lookup"><span data-stu-id="cc488-526">ASP Forms Login Relocation</span></span>](../../../videos/authentication/asp-forms-login-relocation.md)
- [<span data-ttu-id="cc488-527">窗体登录自定义密钥配置</span><span class="sxs-lookup"><span data-stu-id="cc488-527">Forms Login Custom Key Configuration</span></span>](../../../videos/authentication/forms-login-custom-key-configuration.md)
- [<span data-ttu-id="cc488-528">向身份验证方法添加自定义数据</span><span class="sxs-lookup"><span data-stu-id="cc488-528">Add Custom Data to the Authentication Method</span></span>](../../../videos/authentication/add-custom-data-to-the-authentication-method.md)
- [<span data-ttu-id="cc488-529">使用自定义主体对象</span><span class="sxs-lookup"><span data-stu-id="cc488-529">Use Custom Principal Objects</span></span>](../../../videos/authentication/use-custom-principal-objects.md)

### <a name="about-the-author"></a><span data-ttu-id="cc488-530">关于作者</span><span class="sxs-lookup"><span data-stu-id="cc488-530">About the Author</span></span>

<span data-ttu-id="cc488-531">自 1998 年以来，Scott Mitchell，多部 asp/ASP.NET 书籍的作者及 4GuysFromRolla.com 的已从事 Microsoft Web 技术工作。</span><span class="sxs-lookup"><span data-stu-id="cc488-531">Scott Mitchell, author of multiple ASP/ASP.NET books and founder of 4GuysFromRolla.com, has been working with Microsoft Web technologies since 1998.</span></span> <span data-ttu-id="cc488-532">Scott 是独立的顾问、 培训师和编写器。</span><span class="sxs-lookup"><span data-stu-id="cc488-532">Scott works as an independent consultant, trainer, and writer.</span></span> <span data-ttu-id="cc488-533">他最新著作是 *[Sams Teach 自己 ASP.NET 2.0 24 小时内](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco)*。</span><span class="sxs-lookup"><span data-stu-id="cc488-533">His latest book is *[Sams Teach Yourself ASP.NET 2.0 in 24 Hours](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco)*.</span></span> <span data-ttu-id="cc488-534">可以在达到 Scott [ mitchell@4guysfromrolla.com ](mailto:mitchell@4guysfromrolla.com)或通过他的博客[ http://ScottOnWriting.NET ](http://scottonwriting.net/)。</span><span class="sxs-lookup"><span data-stu-id="cc488-534">Scott can be reached at [mitchell@4guysfromrolla.com](mailto:mitchell@4guysfromrolla.com) or via his blog at [http://ScottOnWriting.NET](http://scottonwriting.net/).</span></span>

### <a name="special-thanks-to"></a><span data-ttu-id="cc488-535">特别感谢</span><span class="sxs-lookup"><span data-stu-id="cc488-535">Special Thanks To</span></span>

<span data-ttu-id="cc488-536">很多有用的审阅者已评审本系列教程。</span><span class="sxs-lookup"><span data-stu-id="cc488-536">This tutorial series was reviewed by many helpful reviewers.</span></span> <span data-ttu-id="cc488-537">本教程中的潜在顾客审阅者已 Alicja Maziarz。</span><span class="sxs-lookup"><span data-stu-id="cc488-537">Lead reviewer for this tutorial was Alicja Maziarz.</span></span> <span data-ttu-id="cc488-538">是否有兴趣查看我即将推出的 MSDN 文章？</span><span class="sxs-lookup"><span data-stu-id="cc488-538">Interested in reviewing my upcoming MSDN articles?</span></span> <span data-ttu-id="cc488-539">如果是这样，给我在行[ mitchell@4GuysFromRolla.com ](mailto:mitchell@4guysfromrolla.com)。</span><span class="sxs-lookup"><span data-stu-id="cc488-539">If so, drop me a line at [mitchell@4GuysFromRolla.com](mailto:mitchell@4guysfromrolla.com).</span></span>

> [!div class="step-by-step"]
> [<span data-ttu-id="cc488-540">上一篇</span><span class="sxs-lookup"><span data-stu-id="cc488-540">Previous</span></span>](an-overview-of-forms-authentication-vb.md)
