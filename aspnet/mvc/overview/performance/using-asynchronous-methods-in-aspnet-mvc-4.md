---
uid: mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
title: 在 ASP.NET MVC 4 中使用异步方法 |Microsoft Docs
author: Rick-Anderson
description: 本教程将讲述构建异步 ASP.NET MVC Web 应用程序使用 Visual Studio Express 2012 for Web，这是免费 ve 的基础知识...
ms.author: aspnetcontent
manager: wpickett
ms.date: 06/06/2012
ms.topic: article
ms.assetid: a56572ba-81c3-47af-826d-941e9c4775ec
ms.technology: dotnet-mvc
msc.legacyurl: /mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
msc.type: authoredcontent
ms.openlocfilehash: 6e9d23e4bf0ebbe3c7c8b52c550e0dfa1e6dbb52
ms.sourcegitcommit: 953ff9ea4369f154d6fd0239599279ddd3280009
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 07/03/2018
ms.locfileid: "37364344"
---
<a name="using-asynchronous-methods-in-aspnet-mvc-4"></a><span data-ttu-id="eacee-103">在 ASP.NET MVC 4 中使用异步方法</span><span class="sxs-lookup"><span data-stu-id="eacee-103">Using Asynchronous Methods in ASP.NET MVC 4</span></span>
====================
<span data-ttu-id="eacee-104">通过[Rick Anderson](https://github.com/Rick-Anderson)</span><span class="sxs-lookup"><span data-stu-id="eacee-104">by [Rick Anderson](https://github.com/Rick-Anderson)</span></span>

> <span data-ttu-id="eacee-105">本教程将教您构建异步 ASP.NET MVC Web 应用程序中使用的基础知识[Visual Studio Express 2012 for Web](https://www.microsoft.com/visualstudio/11)，这是 Microsoft Visual Studio 的免费版本。</span><span class="sxs-lookup"><span data-stu-id="eacee-105">This tutorial will teach you the basics of building an asynchronous ASP.NET MVC Web application using [Visual Studio Express 2012 for Web](https://www.microsoft.com/visualstudio/11), which is a free version of Microsoft Visual Studio.</span></span> <span data-ttu-id="eacee-106">此外可以使用[Visual Studio 2012](https://www.microsoft.com/visualstudio/11)。</span><span class="sxs-lookup"><span data-stu-id="eacee-106">You can also use [Visual Studio 2012](https://www.microsoft.com/visualstudio/11).</span></span>
> 
> <span data-ttu-id="eacee-107">本教程中在 github 上提供的完整示例  [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span><span class="sxs-lookup"><span data-stu-id="eacee-107">A complete sample is provided for this tutorial on github  [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span></span>


<span data-ttu-id="eacee-108">ASP.NET MVC 4[控制器](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx)结合使用的类[.NET 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx)使你能够编写的异步操作方法的返回类型的对象[任务&lt;ActionResult&gt;](https://msdn.microsoft.com/library/dd321424(VS.110).aspx).</span><span class="sxs-lookup"><span data-stu-id="eacee-108">The ASP.NET MVC 4 [Controller](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx) class in combination [.NET 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx) enables you to write asynchronous action methods that return an object of type [Task&lt;ActionResult&gt;](https://msdn.microsoft.com/library/dd321424(VS.110).aspx).</span></span> <span data-ttu-id="eacee-109">.NET Framework 4 引入了异步编程概念，称为[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)和 ASP.NET MVC 4 支持[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)。</span><span class="sxs-lookup"><span data-stu-id="eacee-109">The .NET Framework 4 introduced an asynchronous programming concept referred to as a [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) and ASP.NET MVC 4 supports [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx).</span></span> <span data-ttu-id="eacee-110">任务由**任务**类型和中的相关的类型[System.Threading.Tasks](https://msdn.microsoft.com/library/system.threading.tasks.aspx)命名空间。</span><span class="sxs-lookup"><span data-stu-id="eacee-110">Tasks are represented by the **Task** type and related types in the [System.Threading.Tasks](https://msdn.microsoft.com/library/system.threading.tasks.aspx) namespace.</span></span> <span data-ttu-id="eacee-111">与此异步支持生成.NET Framework 4.5 [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)和[异步](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)使用的关键字[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)比以前少得多复杂的对象异步方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-111">The .NET Framework 4.5 builds on this asynchronous support with the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords that make working with [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) objects much less complex than previous asynchronous approaches.</span></span> <span data-ttu-id="eacee-112">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)关键字是速记语法用于指示在某些其他代码段以异步方式等待一段代码。</span><span class="sxs-lookup"><span data-stu-id="eacee-112">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword is syntactical shorthand for indicating that a piece of code should asynchronously wait on some other piece of code.</span></span> <span data-ttu-id="eacee-113">[异步](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字都表示一个提示，可用于将方法标记为基于任务的异步方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-113">The [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword represents a hint that you can use to mark methods as task-based asynchronous methods.</span></span> <span data-ttu-id="eacee-114">组合**await**，**异步**，并**任务**对象可大大简化了.NET 4.5 中编写异步代码。</span><span class="sxs-lookup"><span data-stu-id="eacee-114">The combination of **await**, **async**, and the **Task** object makes it much easier for you to write asynchronous code in .NET 4.5.</span></span> <span data-ttu-id="eacee-115">调用异步方法的新模型*基于任务的异步模式*(**点击**)。</span><span class="sxs-lookup"><span data-stu-id="eacee-115">The new model for asynchronous methods is called the *Task-based Asynchronous Pattern* (**TAP**).</span></span> <span data-ttu-id="eacee-116">本教程假定你已具备一定的使用异步编程[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)并[异步](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字并[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)命名空间。</span><span class="sxs-lookup"><span data-stu-id="eacee-116">This tutorial assumes you have some familiarity with asynchronous programing using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace.</span></span>

<span data-ttu-id="eacee-117">有关使用的详细信息[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)并[异步](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字和[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)命名空间，请参阅以下参考资料。</span><span class="sxs-lookup"><span data-stu-id="eacee-117">For more information on the using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the following references.</span></span>

- [<span data-ttu-id="eacee-118">在.NET 中的白皮书： 实现异步</span><span class="sxs-lookup"><span data-stu-id="eacee-118">Whitepaper: Asynchrony in .NET</span></span>](https://go.microsoft.com/fwlink/?LinkId=204844)
- [<span data-ttu-id="eacee-119">Async/Await FAQ</span><span class="sxs-lookup"><span data-stu-id="eacee-119">Async/Await FAQ</span></span>](https://blogs.msdn.com/b/pfxteam/archive/2012/04/12/10293335.aspx)
- [<span data-ttu-id="eacee-120">Visual Studio 异步编程</span><span class="sxs-lookup"><span data-stu-id="eacee-120">Visual Studio Asynchronous Programming</span></span>](https://msdn.microsoft.com/vstudio/gg316360)

## <a id="HowRequestsProcessedByTP"></a>  <span data-ttu-id="eacee-121">线程池处理请求的方式</span><span class="sxs-lookup"><span data-stu-id="eacee-121">How Requests Are Processed by the Thread Pool</span></span>

<span data-ttu-id="eacee-122">在 web 服务器上，.NET Framework 维护一个用于服务 ASP.NET 请求的线程池。</span><span class="sxs-lookup"><span data-stu-id="eacee-122">On the web server, the .NET Framework maintains a pool of threads that are used to service ASP.NET requests.</span></span> <span data-ttu-id="eacee-123">当请求到达时，将调度池中的线程以处理该请求。</span><span class="sxs-lookup"><span data-stu-id="eacee-123">When a request arrives, a thread from the pool is dispatched to process that request.</span></span> <span data-ttu-id="eacee-124">如果以同步方式处理该请求，处理请求的线程是繁忙时请求正在处理，，线程不能另一个请求服务。</span><span class="sxs-lookup"><span data-stu-id="eacee-124">If the request is processed synchronously, the thread that processes the request is busy while the request is being processed, and that thread cannot service another request.</span></span>   
  
<span data-ttu-id="eacee-125">这可能不是问题，因为线程池可以设置得足够大，以容纳多个忙碌线程数。</span><span class="sxs-lookup"><span data-stu-id="eacee-125">This might not be a problem, because the thread pool can be made large enough to accommodate many busy threads.</span></span> <span data-ttu-id="eacee-126">但是，在线程池中的线程数会受到限制 （默认为.NET 4.5 最大值为 5,000）。</span><span class="sxs-lookup"><span data-stu-id="eacee-126">However, the number of threads in the thread pool is limited (the default maximum for .NET 4.5 is 5,000).</span></span> <span data-ttu-id="eacee-127">在大型应用程序使用高并发的长时间运行的请求，所有可用线程可能正忙。</span><span class="sxs-lookup"><span data-stu-id="eacee-127">In large applications with high concurrency of long-running requests, all available threads might be busy.</span></span> <span data-ttu-id="eacee-128">这种情况称为线程资源不足。</span><span class="sxs-lookup"><span data-stu-id="eacee-128">This condition is known as thread starvation.</span></span> <span data-ttu-id="eacee-129">当达到这种情况时，web 服务器请求进行排队。</span><span class="sxs-lookup"><span data-stu-id="eacee-129">When this condition is reached, the web server queues requests.</span></span> <span data-ttu-id="eacee-130">如果在请求队列已满，web 服务器会拒绝请求并处于 HTTP 503 状态 （服务器太忙）。</span><span class="sxs-lookup"><span data-stu-id="eacee-130">If the request queue becomes full, the web server rejects requests with an HTTP 503 status (Server Too Busy).</span></span> <span data-ttu-id="eacee-131">CLR 线程池将限制对新线程注入。</span><span class="sxs-lookup"><span data-stu-id="eacee-131">The CLR thread pool has limitations on new thread injections.</span></span> <span data-ttu-id="eacee-132">如果并发性突发 （也就是说，您的网站突然可以获取大量的请求） 和所有可用请求线程处于忙碌状态后端调用由于具有高延迟、 有限的线程注入速率可以使应用程序响应效果非常差。</span><span class="sxs-lookup"><span data-stu-id="eacee-132">If concurrency is bursty (that is, your web site can suddenly get a large number of requests) and all available request threads are busy because of backend calls with high latency, the limited thread injection rate can make your application respond very poorly.</span></span> <span data-ttu-id="eacee-133">此外，添加到线程池中每个新线程开销 （例如 1 MB 的堆栈内存)。</span><span class="sxs-lookup"><span data-stu-id="eacee-133">Additionally, each new thread added to the thread pool has overhead (such as 1 MB of stack memory).</span></span> <span data-ttu-id="eacee-134">使用到服务高延迟调用同步方法的线程池增长到.NET 4.5 默认最多 5 个 web 应用程序，000 线程将占用大约为 5 GB 较多内存的应用程序可以比服务相同请求使用异步方法，并仅有 50 个线程。</span><span class="sxs-lookup"><span data-stu-id="eacee-134">A web application using synchronous methods to service high latency calls where the thread pool grows to the .NET 4.5 default maximum of 5, 000 threads would consume approximately 5 GB more memory than an application able the service the same requests using asynchronous methods and only 50 threads.</span></span> <span data-ttu-id="eacee-135">当执行异步工作时，你不始终使用线程。</span><span class="sxs-lookup"><span data-stu-id="eacee-135">When you're doing asynchronous work, you're not always using a thread.</span></span> <span data-ttu-id="eacee-136">例如，当你进行的异步 web 服务请求，ASP.NET 将不使用任何线程之间**异步**方法调用和**await**。</span><span class="sxs-lookup"><span data-stu-id="eacee-136">For example, when you make an asynchronous web service request, ASP.NET will not be using any threads between the **async** method call and the **await**.</span></span> <span data-ttu-id="eacee-137">使用为请求提供服务的线程池具有高延迟可能会导致内存占用量大和的服务器硬件的利用率不佳。</span><span class="sxs-lookup"><span data-stu-id="eacee-137">Using the thread pool to service requests with high latency can lead to a large memory footprint and poor utilization of the server hardware.</span></span>

## <a name="processing-asynchronous-requests"></a><span data-ttu-id="eacee-138">处理异步请求</span><span class="sxs-lookup"><span data-stu-id="eacee-138">Processing Asynchronous Requests</span></span>

<span data-ttu-id="eacee-139">在 web 应用中，会看到大量的并发请求数在启动时或具有突发负载 （其中会增加并发情况突然），使 web 服务调用异步会增加应用的响应能力。</span><span class="sxs-lookup"><span data-stu-id="eacee-139">In a web app that sees a large number of concurrent requests at start-up or has a bursty load (where concurrency increases suddenly), making web service calls asynchronous increases the responsiveness of the app.</span></span> <span data-ttu-id="eacee-140">异步请求采用相同量的时间来处理与同步请求。</span><span class="sxs-lookup"><span data-stu-id="eacee-140">An asynchronous request takes the same amount of time to process as a synchronous request.</span></span> <span data-ttu-id="eacee-141">如果某个请求生成 web 服务调用，需要两秒钟来完成，请求需要两个秒是否执行同步或异步。</span><span class="sxs-lookup"><span data-stu-id="eacee-141">If a request makes a web service call that requires two seconds to complete, the request takes two seconds whether it's performed synchronously or asynchronously.</span></span> <span data-ttu-id="eacee-142">但是在异步调用，线程不是从响应其他请求等待第一个请求完成时阻塞。</span><span class="sxs-lookup"><span data-stu-id="eacee-142">However during an asynchronous call, a thread isn't blocked from responding to other requests while it waits for the first request to complete.</span></span> <span data-ttu-id="eacee-143">因此，异步请求可以防止出现请求排队和线程池增长时有许多并发请求调用长时间运行的操作。</span><span class="sxs-lookup"><span data-stu-id="eacee-143">Therefore, asynchronous requests prevent request queuing and thread pool growth when there are many concurrent requests that invoke long-running operations.</span></span>

## <a id="ChoosingSyncVasync"></a>  <span data-ttu-id="eacee-144">选择同步或异步操作方法</span><span class="sxs-lookup"><span data-stu-id="eacee-144">Choosing Synchronous or Asynchronous Action Methods</span></span>

<span data-ttu-id="eacee-145">本部分列出了有关何时使用同步或异步操作方法的指导原则。</span><span class="sxs-lookup"><span data-stu-id="eacee-145">This section lists guidelines for when to use synchronous or asynchronous action methods.</span></span> <span data-ttu-id="eacee-146">这些是只是一些准则;检查每个应用程序分别以确定是否异步方法能帮助提高性能。</span><span class="sxs-lookup"><span data-stu-id="eacee-146">These are just guidelines; examine each application individually to determine whether asynchronous methods help with performance.</span></span>

<span data-ttu-id="eacee-147">一般情况下，使用同步方法满足以下条件：</span><span class="sxs-lookup"><span data-stu-id="eacee-147">In general, use synchronous methods for the following conditions:</span></span>

- <span data-ttu-id="eacee-148">操作很简单或短时间运行。</span><span class="sxs-lookup"><span data-stu-id="eacee-148">The operations are simple or short-running.</span></span>
- <span data-ttu-id="eacee-149">简单性比效率更重要。</span><span class="sxs-lookup"><span data-stu-id="eacee-149">Simplicity is more important than efficiency.</span></span>
- <span data-ttu-id="eacee-150">操作是主要是 CPU 操作而不是涉及大量的磁盘或网络开销的操作。</span><span class="sxs-lookup"><span data-stu-id="eacee-150">The operations are primarily CPU operations instead of operations that involve extensive disk or network overhead.</span></span> <span data-ttu-id="eacee-151">对 CPU 密集型操作使用异步操作方法未提供任何好处，并导致更多的开销。</span><span class="sxs-lookup"><span data-stu-id="eacee-151">Using asynchronous action methods on CPU-bound operations provides no benefits and results in more overhead.</span></span>

  <span data-ttu-id="eacee-152">一般情况下，使用异步方法在以下条件：</span><span class="sxs-lookup"><span data-stu-id="eacee-152">In general, use asynchronous methods for the following conditions:</span></span>

- <span data-ttu-id="eacee-153">您要调用服务，可通过异步方法，并使用.NET 4.5 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="eacee-153">You're calling services that can be consumed through asynchronous methods, and you're using .NET 4.5 or higher.</span></span>
- <span data-ttu-id="eacee-154">操作是网络绑定的或绑定 I/O 而不是受 CPU 限制。</span><span class="sxs-lookup"><span data-stu-id="eacee-154">The operations are network-bound or I/O-bound instead of CPU-bound.</span></span>
- <span data-ttu-id="eacee-155">并行度不比代码的简单性更重要。</span><span class="sxs-lookup"><span data-stu-id="eacee-155">Parallelism is more important than simplicity of code.</span></span>
- <span data-ttu-id="eacee-156">你想要提供一种机制，允许用户取消长时间运行的请求。</span><span class="sxs-lookup"><span data-stu-id="eacee-156">You want to provide a mechanism that lets users cancel a long-running request.</span></span>
- <span data-ttu-id="eacee-157">当切换线程的好处远远超过上下文切换的成本。</span><span class="sxs-lookup"><span data-stu-id="eacee-157">When the benefit of switching threads outweighs the cost of the context switch.</span></span> <span data-ttu-id="eacee-158">一般情况下，您应使方法异步不执行任何工作时对 ASP.NET 请求线程等待同步方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-158">In general, you should make a method asynchronous if the synchronous method waits on the ASP.NET request thread while doing no work.</span></span> <span data-ttu-id="eacee-159">可以执行异步调用，ASP.NET 请求线程不被停止等待完成的 web 服务请求时任何工作。</span><span class="sxs-lookup"><span data-stu-id="eacee-159">By making the call asynchronous, the ASP.NET request thread is not stalled doing no work while it waits for the web service request to complete.</span></span>
- <span data-ttu-id="eacee-160">测试显示阻塞操作是在站点的性能瓶颈和 IIS 可以通过使用异步方法对这些阻塞调用服务更多的请求。</span><span class="sxs-lookup"><span data-stu-id="eacee-160">Testing shows that the blocking operations are a bottleneck in site performance and that IIS can service more requests by using asynchronous methods for these blocking calls.</span></span>

  <span data-ttu-id="eacee-161">可下载的示例演示如何有效地使用异步操作方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-161">The downloadable sample shows how to use asynchronous action methods effectively.</span></span> <span data-ttu-id="eacee-162">提供的示例旨在提供在 ASP.NET MVC 4 中使用.NET 4.5 的异步编程的简单演示。</span><span class="sxs-lookup"><span data-stu-id="eacee-162">The sample provided was designed to provide a simple demonstration of asynchronous programming in ASP.NET MVC 4 using .NET 4.5.</span></span> <span data-ttu-id="eacee-163">该示例不是要在 ASP.NET MVC 中异步编程的参考体系结构。</span><span class="sxs-lookup"><span data-stu-id="eacee-163">The sample is not intended to be a reference architecture for asynchronous programming in ASP.NET MVC.</span></span> <span data-ttu-id="eacee-164">在示例程序调用[ASP.NET Web API](../../../web-api/index.md)方法，用于反过来调用[Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx)以模拟长时间运行 web 服务调用。</span><span class="sxs-lookup"><span data-stu-id="eacee-164">The sample program calls [ASP.NET Web API](../../../web-api/index.md) methods which in turn call [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate long-running web service calls.</span></span> <span data-ttu-id="eacee-165">大多数生产应用程序不会显示这种明显的优势，到使用异步操作方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-165">Most production applications will not show such obvious benefits to using asynchronous action methods.</span></span>   
  
<span data-ttu-id="eacee-166">几个应用程序需要是异步的所有操作方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-166">Few applications require all action methods to be asynchronous.</span></span> <span data-ttu-id="eacee-167">通常情况下，将几个同步操作方法转换为异步方法提供了显著增加所需的工作量。</span><span class="sxs-lookup"><span data-stu-id="eacee-167">Often, converting a few synchronous action methods to asynchronous methods provides the best efficiency increase for the amount of work required.</span></span>

## <a id="SampleApp"></a>  <span data-ttu-id="eacee-168">示例应用程序</span><span class="sxs-lookup"><span data-stu-id="eacee-168">The Sample Application</span></span>

<span data-ttu-id="eacee-169">您可以下载的示例应用程序[ https://github.com/RickAndMSFT/Async-ASP.NET/ ](https://github.com/RickAndMSFT/Async-ASP.NET)上[GitHub](https://github.com/)站点。</span><span class="sxs-lookup"><span data-stu-id="eacee-169">You can download the sample application from [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET) on the [GitHub](https://github.com/) site.</span></span> <span data-ttu-id="eacee-170">存储库包含三个项目：</span><span class="sxs-lookup"><span data-stu-id="eacee-170">The repository consists of three projects:</span></span>

- <span data-ttu-id="eacee-171">*Mvc4Async*: ASP.NET MVC 4 项目包含本教程中使用的代码。</span><span class="sxs-lookup"><span data-stu-id="eacee-171">*Mvc4Async*: The ASP.NET MVC 4 project that contains the code used in this tutorial.</span></span> <span data-ttu-id="eacee-172">Web API 调用**WebAPIpgw**服务。</span><span class="sxs-lookup"><span data-stu-id="eacee-172">It makes Web API calls to the **WebAPIpgw** service.</span></span>
- <span data-ttu-id="eacee-173">*WebAPIpgw*： 用于实现 ASP.NET MVC 4 Web API 项目`Products, Gizmos and Widgets`控制器。</span><span class="sxs-lookup"><span data-stu-id="eacee-173">*WebAPIpgw*: The ASP.NET MVC 4 Web API project that implements the `Products, Gizmos and Widgets` controllers.</span></span> <span data-ttu-id="eacee-174">它提供的数据*WebAppAsync*项目并*Mvc4Async*项目。</span><span class="sxs-lookup"><span data-stu-id="eacee-174">It provides the data for the *WebAppAsync* project and the *Mvc4Async* project.</span></span>
- <span data-ttu-id="eacee-175">*WebAppAsync*： 另一个教程中使用的 ASP.NET Web 窗体项目。</span><span class="sxs-lookup"><span data-stu-id="eacee-175">*WebAppAsync*: The ASP.NET Web Forms project used in another tutorial.</span></span>

## <a id="GizmosSynch"></a>  <span data-ttu-id="eacee-176">Gizmos，请同步操作方法</span><span class="sxs-lookup"><span data-stu-id="eacee-176">The Gizmos Synchronous Action Method</span></span>

 <span data-ttu-id="eacee-177">下面的代码演示`Gizmos`同步操作方法，用于显示 gizmos，请的列表。</span><span class="sxs-lookup"><span data-stu-id="eacee-177">The following code shows the `Gizmos` synchronous action method that is used to display a list of gizmos.</span></span> <span data-ttu-id="eacee-178">（对于本文，零件是虚构的机械设备。）</span><span class="sxs-lookup"><span data-stu-id="eacee-178">(For this article, a gizmo is a fictional mechanical device.)</span></span> 

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample1.cs)]

<span data-ttu-id="eacee-179">下面的代码演示`GetGizmos`零件服务的方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-179">The following code shows the `GetGizmos` method of the gizmo service.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample2.cs)]

<span data-ttu-id="eacee-180">`GizmoService GetGizmos`方法将 URI 传递到 ASP.NET Web API HTTP 服务返回的 gizmos，请数据列表。</span><span class="sxs-lookup"><span data-stu-id="eacee-180">The `GizmoService GetGizmos` method passes a URI to an ASP.NET Web API HTTP service which returns a list of gizmos data.</span></span> <span data-ttu-id="eacee-181">*WebAPIpgw*项目中包含的 Web API 实现`gizmos, widget`和`product`控制器。</span><span class="sxs-lookup"><span data-stu-id="eacee-181">The *WebAPIpgw* project contains the implementation of the Web API `gizmos, widget` and `product` controllers.</span></span>  
<span data-ttu-id="eacee-182">下图显示了示例项目中的 gizmos，请视图。</span><span class="sxs-lookup"><span data-stu-id="eacee-182">The following image shows the gizmos view from the sample project.</span></span>

![Gizmos，请](using-asynchronous-methods-in-aspnet-mvc-4/_static/image1.png)

## <a id="CreatingAsynchGizmos"></a>  <span data-ttu-id="eacee-184">创建异步 gizmos，请操作方法</span><span class="sxs-lookup"><span data-stu-id="eacee-184">Creating an Asynchronous Gizmos Action Method</span></span>

<span data-ttu-id="eacee-185">该示例使用新[异步](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)并[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)关键字 （在.NET 4.5 和 Visual Studio 2012 中提供） 让编译器负责维护复杂的转换的必要条件异步编程。</span><span class="sxs-lookup"><span data-stu-id="eacee-185">The sample uses the new [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) and [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keywords (available in .NET 4.5 and Visual Studio 2012) to let the compiler be responsible for maintaining the complicated transformations necessary for asynchronous programming.</span></span> <span data-ttu-id="eacee-186">编译器可用于编写代码使用 C# 的同步的控制流构造，编译器会自动应用需使用回调，以避免阻塞线程的转换。</span><span class="sxs-lookup"><span data-stu-id="eacee-186">The compiler lets you write code using the C#'s synchronous control flow constructs and the compiler automatically applies the transformations necessary to use callbacks in order to avoid blocking threads.</span></span>

<span data-ttu-id="eacee-187">下面的代码演示`Gizmos`同步方法和`GizmosAsync`异步方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-187">The following code shows the `Gizmos` synchronous method and the `GizmosAsync` asynchronous method.</span></span> <span data-ttu-id="eacee-188">如果你的浏览器支持[HTML 5`<mark>`元素](http://www.w3.org/wiki/HTML/Elements/mark)，您会看到在更改`GizmosAsync`黄色突出显示框中。</span><span class="sxs-lookup"><span data-stu-id="eacee-188">If your browser supports the [HTML 5 `<mark>` element](http://www.w3.org/wiki/HTML/Elements/mark), you'll see the changes in `GizmosAsync` in yellow highlight.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample3.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample4.cs?highlight=1,3,5)]

 <span data-ttu-id="eacee-189">应用了以下更改，以允许`GizmosAsync`为异步。</span><span class="sxs-lookup"><span data-stu-id="eacee-189">The following changes were applied to allow the `GizmosAsync` to be asynchronous.</span></span>

- <span data-ttu-id="eacee-190">方法将标有[异步](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字，它会告知编译器将生成的正文部分的回调并自动创建`Task<ActionResult>`返回。</span><span class="sxs-lookup"><span data-stu-id="eacee-190">The method is marked with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, which tells the compiler to generate callbacks for parts of the body and to automatically create a `Task<ActionResult>` that is returned.</span></span>
- <span data-ttu-id="eacee-191">&quot;异步&quot;追加到方法名称。</span><span class="sxs-lookup"><span data-stu-id="eacee-191">&quot;Async&quot; was appended to the method name.</span></span> <span data-ttu-id="eacee-192">追加"Async"不是必需的但编写异步方法时，是的约定。</span><span class="sxs-lookup"><span data-stu-id="eacee-192">Appending "Async" is not required but is the convention when writing asynchronous methods.</span></span>
- <span data-ttu-id="eacee-193">返回类型已从`ActionResult`到`Task<ActionResult>`。</span><span class="sxs-lookup"><span data-stu-id="eacee-193">The return type was changed from `ActionResult` to `Task<ActionResult>`.</span></span> <span data-ttu-id="eacee-194">返回类型`Task<ActionResult>`表示正在进行的工作，并提供方法的调用方与用来等待异步操作完成的句柄。</span><span class="sxs-lookup"><span data-stu-id="eacee-194">The return type of `Task<ActionResult>` represents ongoing work and provides callers of the method with a handle through which to wait for the asynchronous operation's completion.</span></span> <span data-ttu-id="eacee-195">在这种情况下，调用方是 web 服务。</span><span class="sxs-lookup"><span data-stu-id="eacee-195">In this case, the caller is the web service.</span></span> <span data-ttu-id="eacee-196">`Task<ActionResult>` 表示正在进行处理的结果 `ActionResult.`</span><span class="sxs-lookup"><span data-stu-id="eacee-196">`Task<ActionResult>` represents ongoing work with a result of `ActionResult.`</span></span>
- <span data-ttu-id="eacee-197">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)关键字已应用于 web 服务调用。</span><span class="sxs-lookup"><span data-stu-id="eacee-197">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the web service call.</span></span>
- <span data-ttu-id="eacee-198">调用了异步 web 服务 API (`GetGizmosAsync`)。</span><span class="sxs-lookup"><span data-stu-id="eacee-198">The asynchronous web service API was called (`GetGizmosAsync`).</span></span>

<span data-ttu-id="eacee-199">内部`GetGizmosAsync`方法正文另一种异步方法，`GetGizmosAsync`调用。</span><span class="sxs-lookup"><span data-stu-id="eacee-199">Inside of the `GetGizmosAsync` method body another asynchronous method, `GetGizmosAsync` is called.</span></span> <span data-ttu-id="eacee-200">`GetGizmosAsync` 立即返回`Task<List<Gizmo>>`，最终会完成时有可用的数据。</span><span class="sxs-lookup"><span data-stu-id="eacee-200">`GetGizmosAsync` immediately returns a `Task<List<Gizmo>>` that will eventually complete when the data is available.</span></span> <span data-ttu-id="eacee-201">因为您不想要将零件数据之前执行任何其他操作，该代码等待任务 (使用**await**关键字)。</span><span class="sxs-lookup"><span data-stu-id="eacee-201">Because you don't want to do anything else until you have the gizmo data, the code awaits the task (using the **await** keyword).</span></span> <span data-ttu-id="eacee-202">可以使用**await**关键字仅在方法中使用批注**异步**关键字。</span><span class="sxs-lookup"><span data-stu-id="eacee-202">You can use the **await** keyword only in methods annotated with the **async** keyword.</span></span>

<span data-ttu-id="eacee-203">**Await**关键字不会阻止线程，直到任务完成。</span><span class="sxs-lookup"><span data-stu-id="eacee-203">The **await** keyword does not block the thread until the task is complete.</span></span> <span data-ttu-id="eacee-204">它以在任务中，回调注册方法的剩余部分，并立即返回。</span><span class="sxs-lookup"><span data-stu-id="eacee-204">It signs up the rest of the method as a callback on the task, and immediately returns.</span></span> <span data-ttu-id="eacee-205">等待的任务最终完成时，它将调用该回调，并因此恢复中断的位置的方法权限执行。</span><span class="sxs-lookup"><span data-stu-id="eacee-205">When the awaited task eventually completes, it will invoke that callback and thus resume the execution of the method right where it left off.</span></span> <span data-ttu-id="eacee-206">有关使用的详细信息[await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)并[异步](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字并[任务](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx)命名空间，请参阅[异步引用](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async)。</span><span class="sxs-lookup"><span data-stu-id="eacee-206">For more information on using the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the [async references](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async).</span></span>

<span data-ttu-id="eacee-207">下面的代码演示`GetGizmos`和`GetGizmosAsync`方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-207">The following code shows the `GetGizmos` and `GetGizmosAsync` methods.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample5.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample6.cs?highlight=1,4-8)]

 <span data-ttu-id="eacee-208">异步更改是对所做的内容相似**GizmosAsync**上面。</span><span class="sxs-lookup"><span data-stu-id="eacee-208">The asynchronous changes are similar to those made to the **GizmosAsync** above.</span></span> 

- <span data-ttu-id="eacee-209">方法签名使用批注[异步](https://msdn.microsoft.com/library/hh156513(VS.110).aspx)关键字，返回类型已更改为`Task<List<Gizmo>>`，和*异步*追加到方法名称。</span><span class="sxs-lookup"><span data-stu-id="eacee-209">The method signature was annotated with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, the return type was changed to `Task<List<Gizmo>>`, and *Async* was appended to the method name.</span></span>
- <span data-ttu-id="eacee-210">异步[HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx)而不是使用类[WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx)类。</span><span class="sxs-lookup"><span data-stu-id="eacee-210">The asynchronous [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) class is used instead of the [WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx) class.</span></span>
- <span data-ttu-id="eacee-211">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx)关键字已应用于[HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx)异步方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-211">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) asynchronous methods.</span></span>

<span data-ttu-id="eacee-212">下图显示了异步零件视图。</span><span class="sxs-lookup"><span data-stu-id="eacee-212">The following image shows the asynchronous gizmo view.</span></span>

![async](using-asynchronous-methods-in-aspnet-mvc-4/_static/image2.png)

<span data-ttu-id="eacee-214">浏览器表示法的 gizmos，请数据是相同的同步调用创建的视图。</span><span class="sxs-lookup"><span data-stu-id="eacee-214">The browsers presentation of the gizmos data is identical to the view created by the synchronous call.</span></span> <span data-ttu-id="eacee-215">唯一的区别是异步版本可能是在重负载下的性能更高。</span><span class="sxs-lookup"><span data-stu-id="eacee-215">The only difference is the asynchronous version may be more performant under heavy loads.</span></span>

## <a id="Parallel"></a>  <span data-ttu-id="eacee-216">并行执行多个操作</span><span class="sxs-lookup"><span data-stu-id="eacee-216">Performing Multiple Operations in Parallel</span></span>

<span data-ttu-id="eacee-217">异步操作方法后操作必须执行几个独立的操作，将通过同步方法的一个明显的优势。</span><span class="sxs-lookup"><span data-stu-id="eacee-217">Asynchronous action methods have a significant advantage over synchronous methods when an action must perform several independent operations.</span></span> <span data-ttu-id="eacee-218">在示例中提供，同步方法`PWG`（适用于产品、 小组件和 gizmos，请） 会显示三个 web 服务调用，以获取的产品、 小组件和 gizmos，请列表的结果。</span><span class="sxs-lookup"><span data-stu-id="eacee-218">In the sample provided, the synchronous method `PWG`(for Products, Widgets and Gizmos) displays the results of three web service calls to get a list of products, widgets, and gizmos.</span></span> <span data-ttu-id="eacee-219">[ASP.NET Web API](../../../web-api/index.md)项目提供了这些服务使用[Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx)模拟延迟或慢速网络调用。</span><span class="sxs-lookup"><span data-stu-id="eacee-219">The [ASP.NET Web API](../../../web-api/index.md) project that provides these services uses [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate latency or slow network calls.</span></span> <span data-ttu-id="eacee-220">当延迟设置为 500 毫秒，异步`PWGasync`方法采用稍有超过 500 毫秒完成时同步`PWG`版本高于 1500 毫秒。</span><span class="sxs-lookup"><span data-stu-id="eacee-220">When the delay is set to 500 milliseconds, the asynchronous `PWGasync` method takes a little over 500 milliseconds to complete while the synchronous `PWG` version takes over 1,500 milliseconds.</span></span> <span data-ttu-id="eacee-221">同步`PWG`方法显示在下面的代码。</span><span class="sxs-lookup"><span data-stu-id="eacee-221">The synchronous `PWG` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample7.cs)]

<span data-ttu-id="eacee-222">异步`PWGasync`方法显示在下面的代码。</span><span class="sxs-lookup"><span data-stu-id="eacee-222">The asynchronous `PWGasync` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample8.cs?highlight=1,3,12)]

<span data-ttu-id="eacee-223">下图显示了返回的视图**PWGasync**方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-223">The following image shows the view returned from the **PWGasync** method.</span></span>

![pwgAsync](using-asynchronous-methods-in-aspnet-mvc-4/_static/image3.png)

## <a id="CancelToken"></a>  <span data-ttu-id="eacee-225">使用取消标记</span><span class="sxs-lookup"><span data-stu-id="eacee-225">Using a Cancellation Token</span></span>

<span data-ttu-id="eacee-226">异步操作方法返回`Task<ActionResult>`是否可取消，即它们采用[CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx)参数在与提供[AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx)属性。</span><span class="sxs-lookup"><span data-stu-id="eacee-226">Asynchronous action methods returning `Task<ActionResult>`are cancelable, that is they take a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter when one is provided with the [AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx) attribute.</span></span> <span data-ttu-id="eacee-227">下面的代码演示`GizmosCancelAsync`具有的 150 毫秒的超时值的方法。</span><span class="sxs-lookup"><span data-stu-id="eacee-227">The following code shows the `GizmosCancelAsync` method with a timeout of 150 milliseconds.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample9.cs?highlight=1-3,5,10)]

<span data-ttu-id="eacee-228">下面的代码演示 GetGizmosAsync 重载，它采用[CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx)参数。</span><span class="sxs-lookup"><span data-stu-id="eacee-228">The following code shows the GetGizmosAsync overload, which takes a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample10.cs)]

<span data-ttu-id="eacee-229">中提供的示例应用程序，选择*取消令牌演示*链接调用`GizmosCancelAsync`方法，并演示异步调用的取消。</span><span class="sxs-lookup"><span data-stu-id="eacee-229">In the sample application provided, selecting the *Cancellation Token Demo* link calls the `GizmosCancelAsync` method and demonstrates the cancelation of the asynchronous call.</span></span>

## <a id="ServerConfig"></a>  <span data-ttu-id="eacee-230">针对高并发/高延迟 Web 服务调用的服务器配置</span><span class="sxs-lookup"><span data-stu-id="eacee-230">Server Configuration for High Concurrency/High Latency Web Service Calls</span></span>

<span data-ttu-id="eacee-231">若要实现异步 web 应用程序的好处，可能需要对默认服务器配置进行一些更改。</span><span class="sxs-lookup"><span data-stu-id="eacee-231">To realize the benefits of an asynchronous web application, you might need to make some changes to the default server configuration.</span></span> <span data-ttu-id="eacee-232">请记住以下配置时的考虑和压力测试异步 web 应用程序中。</span><span class="sxs-lookup"><span data-stu-id="eacee-232">Keep the following in mind when configuring and stress testing your asynchronous web application.</span></span>

- <span data-ttu-id="eacee-233">Windows 7、 Windows Vista 和所有 Windows 客户端操作系统具有最多 10 个并发请求。</span><span class="sxs-lookup"><span data-stu-id="eacee-233">Windows 7, Windows Vista and all Windows client operating systems have a maximum of 10 concurrent requests.</span></span> <span data-ttu-id="eacee-234">你将需要 Windows 服务器操作系统，若要查看在高负载下的异步方法的优势。</span><span class="sxs-lookup"><span data-stu-id="eacee-234">You'll need a Windows Server operating system to see the benefits of asynchronous methods under high load.</span></span>
- <span data-ttu-id="eacee-235">从提升的命令提示符向 IIS 注册.NET 4.5:</span><span class="sxs-lookup"><span data-stu-id="eacee-235">Register .NET 4.5 with IIS from an elevated command prompt:</span></span>  
  <span data-ttu-id="eacee-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis-i</span><span class="sxs-lookup"><span data-stu-id="eacee-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis -i</span></span>  
  <span data-ttu-id="eacee-237">请参阅[ASP.NET IIS 注册工具 (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span><span class="sxs-lookup"><span data-stu-id="eacee-237">See    [ASP.NET IIS Registration Tool (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span></span>
- <span data-ttu-id="eacee-238">你可能需要增加[HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture)队列限制从 1,000 到 5,000 个的默认值。</span><span class="sxs-lookup"><span data-stu-id="eacee-238">You might need to increase the [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) queue limit from the default value of 1,000 to 5,000.</span></span> <span data-ttu-id="eacee-239">如果设置得太低，可能会看到[HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture)拒绝请求的 HTTP 503 状态。</span><span class="sxs-lookup"><span data-stu-id="eacee-239">If the setting is too low, you may see [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) reject requests with a HTTP 503 status.</span></span> <span data-ttu-id="eacee-240">若要更改 HTTP.sys 队列限制：</span><span class="sxs-lookup"><span data-stu-id="eacee-240">To change the HTTP.sys queue limit:</span></span>

    - <span data-ttu-id="eacee-241">打开 IIS 管理器并导航到应用程序池窗格中。</span><span class="sxs-lookup"><span data-stu-id="eacee-241">Open IIS manager and navigate to the Application Pools pane.</span></span>
    - <span data-ttu-id="eacee-242">在目标应用程序池上右键单击并选择**高级设置**。</span><span class="sxs-lookup"><span data-stu-id="eacee-242">Right click on the target application pool and select **Advanced Settings**.</span></span>  
        <span data-ttu-id="eacee-243">![advanced](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="eacee-243">![advanced](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span></span>
    - <span data-ttu-id="eacee-244">在中**高级设置**对话框中，更改*队列长度*从 1,000 到 5,000。</span><span class="sxs-lookup"><span data-stu-id="eacee-244">In the **Advanced Settings** dialog box, change *Queue Length* from 1,000 to 5,000.</span></span>  
        <span data-ttu-id="eacee-245">![队列长度](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span><span class="sxs-lookup"><span data-stu-id="eacee-245">![Queue length](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span></span>  
  
  <span data-ttu-id="eacee-246">请注意，在更高版本，映像的.NET framework 被列为 v4.0，即使应用程序池使用的.NET 4.5 也是如此。</span><span class="sxs-lookup"><span data-stu-id="eacee-246">Note in the images above, the .NET framework is listed as v4.0, even though the application pool is using .NET 4.5.</span></span> <span data-ttu-id="eacee-247">若要了解这种差异，请参阅以下：</span><span class="sxs-lookup"><span data-stu-id="eacee-247">To understand this discrepancy, see the following:</span></span>

    - [<span data-ttu-id="eacee-248">.NET 版本控制和多重目标 —.NET 4.5 是.NET 4.0 的就地升级</span><span class="sxs-lookup"><span data-stu-id="eacee-248">.NET Versioning and Multi-Targeting - .NET 4.5 is an in-place upgrade to .NET 4.0</span></span>](http://www.hanselman.com/blog/NETVersioningAndMultiTargetingNET45IsAnInplaceUpgradeToNET40.aspx)
    - [<span data-ttu-id="eacee-249">如何设置要使用 ASP.NET 3.5 而不是 2.0 的 IIS 应用程序或应用程序池</span><span class="sxs-lookup"><span data-stu-id="eacee-249">How to set an IIS Application or AppPool to use ASP.NET 3.5 rather than 2.0</span></span>](http://www.hanselman.com/blog/HowToSetAnIISApplicationOrAppPoolToUseASPNET35RatherThan20.aspx)
    - <span data-ttu-id="eacee-250">[.NET Framework 版本和依赖关系](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span><span class="sxs-lookup"><span data-stu-id="eacee-250">[.NET Framework Versions and Dependencies](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span></span>
- <span data-ttu-id="eacee-251">如果你的应用程序使用 web 服务或 System.NET 使用后端通过 HTTP 进行通信，可能需要增加[connectionManagement/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx)元素。</span><span class="sxs-lookup"><span data-stu-id="eacee-251">If your application is using web services or System.NET to communicate with a backend over HTTP you may need to increase the [connectionManagement/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx) element.</span></span> <span data-ttu-id="eacee-252">对于 ASP.NET 应用程序，这被受 12 倍的 Cpu 数的自动配置功能。</span><span class="sxs-lookup"><span data-stu-id="eacee-252">For ASP.NET applications, this is limited by the autoConfig feature to 12 times the number of CPUs.</span></span> <span data-ttu-id="eacee-253">这意味着，在四处理器，您可以最多 12 \* 4 = 48 到 IP 终结点的并发连接。</span><span class="sxs-lookup"><span data-stu-id="eacee-253">That means that on a quad-proc, you can have at most 12 \* 4 = 48 concurrent connections to an IP end point.</span></span> <span data-ttu-id="eacee-254">因为这与[autoConfig](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx)，增加的最简单办法`maxconnection`应用程序是 ASP.NET 中设置[System.Net.ServicePointManager.DefaultConnectionLimit](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx)以编程方式在从`Application_Start`中的方法*global.asax*文件。</span><span class="sxs-lookup"><span data-stu-id="eacee-254">Because this is tied to [autoConfig](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx), the easiest way to increase `maxconnection` in an ASP.NET application is to set [System.Net.ServicePointManager.DefaultConnectionLimit](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx) programmatically in the from `Application_Start` method in the *global.asax* file.</span></span> <span data-ttu-id="eacee-255">请参阅下载有关示例的示例。</span><span class="sxs-lookup"><span data-stu-id="eacee-255">See the sample download for an example.</span></span>
- <span data-ttu-id="eacee-256">在.NET 4.5 中，默认值为 5000 [maxconcurrentrequestspercpu 配置](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx)可以正常工作。</span><span class="sxs-lookup"><span data-stu-id="eacee-256">In .NET 4.5, the default of 5000 for [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx) should be fine.</span></span>
